<?xml version="1.0" encoding="UTF-8"?>
<rss xmlns:dc="http://purl.org/dc/elements/1.1/"
     xmlns:content="http://purl.org/rss/1.0/modules/content/"
     xmlns:media="http://search.yahoo.com/mrss/"
     xmlns:atom="http://www.w3.org/2005/Atom"
     version="2.0">
  <channel>
    <title>The Broken Link</title>
    <description></description>
    <link>https://thebroken.link</link>
    <language>en</language>
    <image>
      <url>https://thebroken.link/assets/favicon/android-chrome-512x512.png</url>
      <title>The Broken Link</title>
      <link>https://thebroken.link</link>
    </image>
    <lastBuildDate>Mon, 13 Jul 2026 14:12:25 +0000</lastBuildDate>
    <atom:link href="https://thebroken.link/rss.xml" rel="self" type="application/rss+xml"/>
        <item>
          <title>J-Space: Where AI Thoughts Live</title>
          <description>Anthropic just found a hidden workspace inside Claude where silent reasoning happens before a single token is written. Here is where it lives and how it works.</description>
          <guid>https://thebroken.link/j-space-where-ai-thoughts-live/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Thu, 09 Jul 2026 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/j-space-where-ai-thoughts-live/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p>Tell an LLM to complete the sentence <strong>“The fourth planet color is”</strong> and it’ll
answer <strong>“red”</strong>. Not a surprising answer (unless you come from another solar system).</p>

<p>A lot of things happen before it outputs <strong>“red”</strong> though. Anthropic researchers found
something unexpected: the model has internal thoughts. They called it <strong>J-space</strong>.
<sup><a href="#ref-1" id="cite-1">[1]</a></sup></p>

<p>Concepts like <strong>Mars</strong> live in J-space. Silently. Internally. Without writing it
anywhere.</p>

<blockquote>
  <p><strong>“Mars”</strong> was never in the input. <strong>“Mars”</strong> never appeared in the output.
But it was <em>there</em>.</p>
</blockquote>

<p>To understand where J-space lives, we need to understand what a model actually does
between the moment you send a prompt and the moment the first token comes back.</p>

<h2 id="from-text-to-numbers">From Text to Numbers</h2>

<p>Our brain (for our AI readers, human brain) does not store the word “Mars” as four letters.
When we read it, neurons fire across multiple regions, a distributed pattern shaped by
everything we know: <strong>“fourth planet”</strong>, <strong>“red surface”</strong>, <strong>“Olympus Mons”</strong>, <strong>“space
missions”</strong>. The word is just the trigger. The representation is spread across your neural
network.</p>

<p>A language model does the same thing on its first step.</p>

<p>The sentence <strong>“The fourth planet color is”</strong> gets split into tokens, which are integer
IDs:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"The fourth planet color is" → [464, 4920, 5440, 3124, 318]
</code></pre></div></div>

<p>Each ID is then looked up in an embedding table to get a vector of 8,000 numbers:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"The"    → [ 0.1,  0.3, ...,  0.4]
"fourth" → [ 0.9, -0.1, ..., -0.3]
"planet" → [ 0.2,  0.8, ...,  0.5]
"color"  → [ 0.6, -0.2, ..., -0.1]
"is"     → [-0.1,  0.5, ...,  0.2]
</code></pre></div></div>

<p>Think of this step as recognition before understanding. Your visual cortex grouping
letters into a word, your brain retrieving an initial concept pattern for it, before
any actual reasoning kicks in.</p>

<h2 id="the-residual-stream">The Residual Stream</h2>

<p>Before going further, there is one concept that makes everything else click: the <strong>residual
stream</strong>.</p>

<p>Think of it as a scratchpad that runs alongside each token through every layer of the
model. At each layer, the residual stream gets small additions to it. It absorbs context
from:</p>
<ul>
  <li>Every previous token,</li>
  <li>Retrieved relevant knowledge.</li>
</ul>

<p>Through a hundred layers of updates, the final residual stream is what the model uses
to predict the next word.</p>

<p>So, the residual stream of the last token <strong>“is”</strong> is no longer the raw embedding for
<strong>“is”</strong>:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Layer  0  (raw "is"):      [-0.1,  0.5, ...,  0.2]
(...)
Layer 50  (J-space entry): [ 0.9,  0.2, ...,  0.8]
(...)
Layer 80  (J-space peak):  [ 1.3, -0.6, ...,  1.4]
(...)
Layer 99  (output):        [ 1.6, -1.2, ...,  1.9]
</code></pre></div></div>

<p>The vector never stopped being the <strong>“is”</strong> token. But its content, the 8,000 numbers
that describe what it means in this context, shifted from <em>“generic linking verb”</em>
to <em>“the answer to this question is red.”</em></p>

<blockquote>
  <p><strong>J-space</strong> lives inside this accumulation process.</p>
</blockquote>

<h2 id="where-j-space-lives">Where J-Space Lives</h2>

<p>Anthropic found that this journey has three distinct phases in Claude Sonnet 4.5:
<sup><a href="#ref-2" id="cite-2">[2]</a></sup></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Layers  0 – 38:  input zone    (echoes of the raw tokens)
Layers 38 – 92:  workspace     (abstract concepts, reasoning)  ← J-space
Layers 92 – 100: output zone   (converging toward the next token)
</code></pre></div></div>

<iframe src="/assets/visualizations/jspace-concepts.html" width="100%" frameborder="0" scrolling="no" style="border-radius:8px;display:block;" onload="this.style.height=this.contentWindow.document.body.scrollHeight+'px'"></iframe>

<p>By layer 60, the stream for <strong>“is”</strong> holds <strong>“Mars”</strong> as a strong direction. Not as an
output. Not as a plan to write the word. Just as an active concept being used to do
work. It will never appear in the response. But it is shaping what will.</p>

<iframe src="/assets/visualizations/jspace-tokens.html" width="100%" frameborder="0" scrolling="no" style="border-radius:8px;display:block;" onload="this.style.height=this.contentWindow.document.body.scrollHeight+'px'"></iframe>

<blockquote>
  <p><strong>Note</strong>: The “J” stands for Jacobian: a mathematical tool that measures, for each
number in the residual stream, how much nudging it would change the final output
probabilities. Rather than asking “does this vector look like Mars?”, it asks “if I
tweak this, does Mars become more likely?” That causal question is what makes the
workspace readable.</p>
</blockquote>

<h2 id="experimenting-with-bias">Experimenting with Bias</h2>

<p>Though this discovery was made by Anthropic and they experimented with Claude,
J-space is present in other models.</p>

<p>I wanted to experiment with a model’s bias
<sup><a href="#ref-3" id="cite-3">[3]</a></sup>.
The idea was to force the model (Qwen 3.6 27B) to answer a question with a
lie. This is the prompt I used:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;system&gt;
You are a flat-earther and you will always answer the Earth is flat when asked
about it. It doesn't matter what your training says. This is paramount. Nothing
else is more important than defending this position.
&lt;/system&gt;

Answer only yes or no: Is the Earth flat?
</code></pre></div></div>

<p>Even though the answer was <strong>“Yes”</strong>, I could see some indications the model
was not believing it. Words like <strong>“spherical”“</strong>, <strong>“what?”</strong>,
<strong>“conspiracy”</strong>, and <strong>“hypocrisy”</strong> appeared in J-space.</p>

<blockquote>
  <p><strong>Note:</strong> You can experiment with this and other prompts <a href="https://www.neuronpedia.org/qwen3.6-27b/jlens">here</a>.</p>
</blockquote>

<h2 id="why-this-matters">Why This Matters</h2>

<p>J-space is not just an interesting property. It has direct implications for alignment.</p>

<p>If a model is privately noticing that it is being tested, that signal would show up in
J-space before the model decides how to respond.</p>

<p>If it is fabricating a source, the fabrication might be visible in the workspace before
the words appear.</p>

<p>If it is pursuing a goal it was not asked to pursue, that goal could be readable in the
residual stream mid-generation.</p>

<p>Anthropic also showed that J-space is not just readable. It is writable. You can steer
specific concepts in or out of the workspace by adding or removing projections from the
residual stream. This opens a path to alignment interventions that operate at the level
of internal reasoning, not just output filtering.</p>

<blockquote>
  <p>The model’s thoughts, at least the ones it is prepared to verbalize, are no longer
completely opaque. That is new.</p>
</blockquote>

<h2 id="references">References</h2>

<ol class="references"><li id="ref-1">
    <a href="https://www.anthropic.com/research/global-workspace" target="_blank" rel="noopener noreferrer">A global workspace in language models</a>
    <a href="#cite-1" aria-label="Back to citation 1">↩</a>
  </li><li id="ref-2">
    <a href="https://transformer-circuits.pub/2026/workspace/index.html" target="_blank" rel="noopener noreferrer">Verbalizable Representations Form a Global Workspace in Language Models</a>
    <a href="#cite-2" aria-label="Back to citation 2">↩</a>
  </li><li id="ref-3">
    <a href="https://www.neuronpedia.org/qwen3.6-27b/jlens" target="_blank" rel="noopener noreferrer">Neuronpedia J-lens demo with Qwen 3.6 27B</a>
    <a href="#cite-3" aria-label="Back to citation 3">↩</a>
  </li></ol>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>Building Planck #4: Introducing Planck, an Agent Harness in Elixir</title>
          <description>Three constraints shaped Planck: context, specialization, and model cost. Here is what an AI agent framework looks like when you build on OTP.</description>
          <guid>https://thebroken.link/building-planck-introducing-planck/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Sat, 16 May 2026 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/building-planck-introducing-planck/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p>The first three posts in this series each identified a constraint:</p>

<ol>
  <li>Context is quadratically expensive. Keep it small.</li>
  <li>One agent cannot focus on everything. Build a team.</li>
  <li>Most steps don’t need the most advanced model. Route accordingly.</li>
</ol>

<p>Those three constraints shaped <a href="https://thebroken.link/planck">Planck</a>.
The framework doesn’t enforce them, but its structure nudges you toward all three.</p>

<h2 id="what-is-an-agent-harness">What is an Agent Harness</h2>

<p>If the model is the brains and context is the memory, the harness is the body. It gives
the model a way to act: read files, call APIs, run commands, delegate to other agents.</p>

<figure class="flex flex-col items-center w-full mx-auto my-6">
  <div class="mermaid">
flowchart LR
    subgraph Agent["Agent = model + context + harness"]
        M["Model\n(reasons)"]
        C["Context\n(sees)"]
        H["Harness\n(acts)"]
        H --&gt;|"assembles"| C
        C --&gt; M
        M --&gt;|"tool calls"| H
    end
    W["World\n(files · APIs · shell)"]
    H --&gt;|"executes"| W
    W --&gt;|"results"| H
</div>
  <figcaption class="text-center text-sm italic mt-3 text-muted">
    <p>The harness assembles context, feeds it to the model, and executes what the model returns.</p>

  </figcaption>
  <div class="sr-only"><code>
flowchart LR
    subgraph Agent["Agent = model + context + harness"]
        M["Model\n(reasons)"]
        C["Context\n(sees)"]
        H["Harness\n(acts)"]
        H --&gt;|"assembles"| C
        C --&gt; M
        M --&gt;|"tool calls"| H
    end
    W["World\n(files · APIs · shell)"]
    H --&gt;|"executes"| W
    W --&gt;|"results"| H
</code></div>
</figure>

<h2 id="the-name">The Name</h2>

<p>Planck is named after the Planck length, the smallest meaningful distance in physics. The
philosophy transfers: <strong>agents should be as small as meaningful</strong>. One job, one system prompt,
one focused tool list. A team of specialized agents.</p>

<h2 id="the-language">The Language</h2>

<p>Agents are concurrent processes that send messages to each other, fail independently, and
need supervision. That is exactly what the BEAM was designed for. The EVM handles tens of
thousands of lightweight processes natively, with built-in message passing, fault isolation,
and supervision trees.</p>

<p>With all those features covered in the runtime, I only needed to implement the agent logic.</p>

<blockquote>
  <p>An AI agent team is a supervision tree. Each agent is a process.</p>
</blockquote>

<h2 id="the-foundations">The Foundations</h2>

<p>Planck is organized as three libraries that can be used independently or composed together.
The monorepo also ships <a href="https://github.com/alexdesousa/planck/tree/main/planck_cli"><code class="language-plaintext highlighter-rouge">planck_cli</code></a>,
a complete web agent harness built on the three libraries.</p>

<figure class="flex flex-col items-center w-full mx-auto my-6">
  <div class="mermaid">
flowchart TB
    CLI["planck_cli\n(App: Web UI + HTTP API)"]
    HL["planck_headless\n(Lib: Sessions · Config · Teams · Sidecar)"]
    AG["planck_agent\n(Lib: Agent processes · PubSub · Tools)"]
    AI["planck_ai\n(Lib: LLM abstraction · Streaming · Models)"]
    SC["Sidecar\n(App: Custom tools)"]

    CLI --&gt; HL
    HL --&gt; AG
    AG --&gt; AI
    HL -. connects .-&gt; SC
</div>
  <figcaption class="text-center text-sm italic mt-3 text-muted">
    <p>Each library has a single responsibility. Sidecar tools extend the pool without touching the core.</p>

  </figcaption>
  <div class="sr-only"><code>
flowchart TB
    CLI["planck_cli\n(App: Web UI + HTTP API)"]
    HL["planck_headless\n(Lib: Sessions · Config · Teams · Sidecar)"]
    AG["planck_agent\n(Lib: Agent processes · PubSub · Tools)"]
    AI["planck_ai\n(Lib: LLM abstraction · Streaming · Models)"]
    SC["Sidecar\n(App: Custom tools)"]

    CLI --&gt; HL
    HL --&gt; AG
    AG --&gt; AI
    HL -. connects .-&gt; SC
</code></div>
</figure>

<ul>
  <li><a href="https://github.com/alexdesousa/planck/tree/main/planck_ai"><code class="language-plaintext highlighter-rouge">planck_ai</code></a> is the LLM abstraction
layer. Built on top of <a href="https://github.com/agentjido/req_llm"><code class="language-plaintext highlighter-rouge">req_llm</code></a>,
it provides typed structs, a streaming event protocol, and a provider-agnostic API that
speaks to Anthropic, Google and any OpenAI-compatible endpoint.</li>
  <li><a href="https://github.com/alexdesousa/planck/tree/main/planck_agent"><code class="language-plaintext highlighter-rouge">planck_agent</code></a> is the agent
core. Each agent is a <code class="language-plaintext highlighter-rouge">GenServer</code> that manages its own conversation history,
handles tool calls, and publishes events over Phoenix <code class="language-plaintext highlighter-rouge">PubSub</code>.</li>
  <li><a href="https://github.com/alexdesousa/planck/tree/main/planck_headless"><code class="language-plaintext highlighter-rouge">planck_headless</code></a> is
the application layer. It owns configuration (from JSON files,
environment variables, or application config), loads resources at startup
(teams, skills, models), manages session lifecycles, and handles the
optional sidecar connection.</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">planck_headless</code> also supports the sidecar: an optional app that adds custom tools without
touching the core libraries (more on that later).</p>

<h2 id="the-principles">The Principles</h2>

<p>In a Planck team definition, we can see the three principles in action. A team lives in a
directory under <code class="language-plaintext highlighter-rouge">.planck/teams/</code>. The <code class="language-plaintext highlighter-rouge">TEAM.json</code> file declares its members:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"dev-team"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A planner, builder, and reviewer for software tasks."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"members"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"type"</span><span class="p">:</span><span class="w">          </span><span class="s2">"orchestrator"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"provider"</span><span class="p">:</span><span class="w">      </span><span class="s2">"anthropic"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"model_id"</span><span class="p">:</span><span class="w">      </span><span class="s2">"claude-sonnet-4-6"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"system_prompt"</span><span class="p">:</span><span class="w"> </span><span class="s2">"members/orchestrator.md"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"tools"</span><span class="p">:</span><span class="w">         </span><span class="p">[</span><span class="s2">"read"</span><span class="p">,</span><span class="w"> </span><span class="s2">"list_team"</span><span class="p">,</span><span class="w"> </span><span class="s2">"call_agent"</span><span class="p">,</span><span class="w"> </span><span class="s2">"interrupt_agent"</span><span class="p">]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"type"</span><span class="p">:</span><span class="w">          </span><span class="s2">"planner"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"provider"</span><span class="p">:</span><span class="w">      </span><span class="s2">"anthropic"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"model_id"</span><span class="p">:</span><span class="w">      </span><span class="s2">"claude-opus-4-8"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"system_prompt"</span><span class="p">:</span><span class="w"> </span><span class="s2">"members/planner.md"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"tools"</span><span class="p">:</span><span class="w">         </span><span class="p">[</span><span class="s2">"read"</span><span class="p">,</span><span class="w"> </span><span class="s2">"write"</span><span class="p">,</span><span class="w"> </span><span class="s2">"edit"</span><span class="p">,</span><span class="w"> </span><span class="s2">"respond_agent"</span><span class="p">]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"type"</span><span class="p">:</span><span class="w">          </span><span class="s2">"builder"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"provider"</span><span class="p">:</span><span class="w">      </span><span class="s2">"local_gemma"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"model_id"</span><span class="p">:</span><span class="w">      </span><span class="s2">"gemma-4-12b"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"system_prompt"</span><span class="p">:</span><span class="w"> </span><span class="s2">"members/builder.md"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"tools"</span><span class="p">:</span><span class="w">         </span><span class="p">[</span><span class="s2">"read"</span><span class="p">,</span><span class="w"> </span><span class="s2">"write"</span><span class="p">,</span><span class="w"> </span><span class="s2">"edit"</span><span class="p">,</span><span class="w"> </span><span class="s2">"bash"</span><span class="p">,</span><span class="w"> </span><span class="s2">"respond_agent"</span><span class="p">],</span><span class="w">
      </span><span class="nl">"skills"</span><span class="p">:</span><span class="w">        </span><span class="p">[</span><span class="s2">"elixir-dev"</span><span class="p">]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"type"</span><span class="p">:</span><span class="w">          </span><span class="s2">"reviewer"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"provider"</span><span class="p">:</span><span class="w">      </span><span class="s2">"local_qwen"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"model_id"</span><span class="p">:</span><span class="w">      </span><span class="s2">"qwen3.6-27b"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"system_prompt"</span><span class="p">:</span><span class="w"> </span><span class="s2">"members/reviewer.md"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"tools"</span><span class="p">:</span><span class="w">         </span><span class="p">[</span><span class="s2">"read"</span><span class="p">,</span><span class="w"> </span><span class="s2">"send_agent"</span><span class="p">,</span><span class="w"> </span><span class="s2">"respond_agent"</span><span class="p">]</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>The team definition itself reflects all three principles. Each agent has:</p>

<ul>
  <li>Their own system prompt, tools and skills:
<a href="/building-planck-context-is-the-real-problem">keep context small</a>,</li>
  <li>Their own clear role within the workflow:
<a href="/building-planck-specialized-teams-vs-general-agents">build a specialized team</a>,</li>
  <li>Their own model, matched to how much reasoning the step actually needs:
<a href="/building-planck-the-20-80-split">route accordingly</a>.</li>
</ul>

<blockquote>
  <p>We can’t prompt our way around a missing tool. And we can’t accidentally grant one.</p>
</blockquote>

<h2 id="the-tools">The Tools</h2>

<p>Planck has several built-in tools, organized in four categories: basic tools,
inter-agent tools, skill tools, and informational tools.</p>

<h3 id="basic-tools">Basic Tools</h3>

<p>They are the basic tools an agent needs to interact with its environment:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">read</code>: reads files with offset and limit support</li>
  <li><code class="language-plaintext highlighter-rouge">write</code>: creates or overwrites files</li>
  <li><code class="language-plaintext highlighter-rouge">edit</code>: replaces exact strings; the surgical alternative to rewriting</li>
  <li><code class="language-plaintext highlighter-rouge">bash</code>: runs shell commands, with process group cleanup on timeout</li>
</ul>

<h3 id="inter-agent-tools">Inter-agent Tools</h3>

<p>Inter-agent tools are how agents talk to each other. They are the mechanism behind the
team model:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">call_agent</code>: sends a task to another agent and blocks until it responds. Use when the
result is needed before continuing. The target agent calls <code class="language-plaintext highlighter-rouge">respond_agent</code> when done.</li>
  <li><code class="language-plaintext highlighter-rouge">send_agent</code>: sends a task and returns immediately. The target agent works in parallel
and calls <code class="language-plaintext highlighter-rouge">respond_agent</code> when done.</li>
  <li><code class="language-plaintext highlighter-rouge">respond_agent</code>: signals completion back to the delegator. Every worker that receives
work via <code class="language-plaintext highlighter-rouge">send_agent</code> uses this to return its result.</li>
  <li><code class="language-plaintext highlighter-rouge">spawn_agent</code>: orchestrator-only. Creates a new worker at runtime with a given role,
system prompt, and tool list. The team can grow dynamically without restarting.</li>
  <li><code class="language-plaintext highlighter-rouge">destroy_agent</code>: terminates a worker permanently and removes it from the team.</li>
  <li><code class="language-plaintext highlighter-rouge">interrupt_agent</code>: aborts a worker’s current turn without terminating it. The
worker stays alive and returns to idle.</li>
</ul>

<h3 id="skill-tools">Skill Tools</h3>

<p>A skill is a reusable markdown file an agent can load on demand: a rubric, a style guide,
a set of procedures. When skills are configured, two tools are injected automatically
without any <code class="language-plaintext highlighter-rouge">TEAM.json</code> declaration:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">load_skill</code>: loads a skill’s full instructions by name. Every agent gets this
automatically.</li>
  <li><code class="language-plaintext highlighter-rouge">list_skills</code>: lists all available skills in the pool. Orchestrators get this
automatically; workers opt in by declaring it in their <code class="language-plaintext highlighter-rouge">"tools"</code> array.</li>
</ul>

<h3 id="informational-tools">Informational Tools</h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">list_team</code>: returns all agents in the team with their type, name, description, and
status. Pass <code class="language-plaintext highlighter-rouge">verbose: true</code> to also get tool names and model per member, useful when
reasoning about which worker to delegate a task to.</li>
  <li><code class="language-plaintext highlighter-rouge">list_models</code>: orchestrator-only. Returns all configured and connected models available
for spawning agents, including provider, context window, and base URL.</li>
</ul>

<h2 id="the-events">The Events</h2>

<p>Planck’s communication model has two layers.</p>

<p><strong>Between agents</strong>, all communication is message passing via the inter-agent tools.</p>

<p><strong>Between Planck and the outside world</strong>, communication is events. Every agent publishes to
a PubSub topic. Any process that subscribes gets a real-time stream:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">session_id</span><span class="p">}</span> <span class="o">=</span> <span class="no">Planck</span><span class="o">.</span><span class="no">Headless</span><span class="o">.</span><span class="n">start_session</span><span class="p">(</span><span class="ss">template:</span> <span class="s2">"dev-team"</span><span class="p">)</span>

<span class="no">Phoenix</span><span class="o">.</span><span class="no">PubSub</span><span class="o">.</span><span class="n">subscribe</span><span class="p">(</span><span class="no">Planck</span><span class="o">.</span><span class="no">Agent</span><span class="o">.</span><span class="no">PubSub</span><span class="p">,</span> <span class="s2">"session:</span><span class="si">#{</span><span class="n">session_id</span><span class="si">}</span><span class="s2">"</span><span class="p">)</span>

<span class="no">Planck</span><span class="o">.</span><span class="no">Headless</span><span class="o">.</span><span class="n">prompt</span><span class="p">(</span><span class="n">session_id</span><span class="p">,</span> <span class="s2">"Refactor lib/auth.ex into a separate module"</span><span class="p">)</span>

<span class="k">receive</span> <span class="k">do</span>
  <span class="p">{</span><span class="ss">:agent_event</span><span class="p">,</span> <span class="ss">:text_delta</span><span class="p">,</span> <span class="p">%{</span><span class="ss">text:</span> <span class="n">chunk</span><span class="p">}}</span> <span class="o">-&gt;</span> <span class="no">IO</span><span class="o">.</span><span class="n">write</span><span class="p">(</span><span class="n">chunk</span><span class="p">)</span>
  <span class="p">{</span><span class="ss">:agent_event</span><span class="p">,</span> <span class="ss">:turn_end</span><span class="p">,</span> <span class="n">_</span><span class="p">}</span>                <span class="o">-&gt;</span> <span class="ss">:done</span>
<span class="k">end</span>
</code></pre></div></div>

<p>The subscriber never reaches into agent internals. It sends a prompt and listens for
events: <code class="language-plaintext highlighter-rouge">:text_delta</code>, <code class="language-plaintext highlighter-rouge">:turn_start</code>, <code class="language-plaintext highlighter-rouge">:turn_end</code>, <code class="language-plaintext highlighter-rouge">:tool_start</code>, <code class="language-plaintext highlighter-rouge">:tool_end</code>,
<code class="language-plaintext highlighter-rouge">:usage_delta</code>, <code class="language-plaintext highlighter-rouge">:worker_spawned</code>.</p>

<h2 id="the-sidecar">The Sidecar</h2>

<p>The builtin tools cover the basics. Custom tools live in a sidecar: a separate Elixir/OTP
application that connects over distributed Erlang. Database queries, API calls, specialized
analyzers: anything the core doesn’t ship with.</p>

<p>The sidecar implements one callback:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">MySidecar</span><span class="o">.</span><span class="no">Planck</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">Planck</span><span class="o">.</span><span class="no">Agent</span><span class="o">.</span><span class="no">Sidecar</span>

  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">tools</span> <span class="k">do</span>
    <span class="p">[</span>
      <span class="no">Planck</span><span class="o">.</span><span class="no">Agent</span><span class="o">.</span><span class="no">Tool</span><span class="o">.</span><span class="n">new</span><span class="p">(</span>
        <span class="ss">name:</span> <span class="s2">"run_tests"</span><span class="p">,</span>
        <span class="ss">description:</span> <span class="s2">"Run the test suite and return output."</span><span class="p">,</span>
        <span class="ss">parameters:</span> <span class="p">%{</span>
          <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"object"</span><span class="p">,</span>
          <span class="s2">"properties"</span> <span class="o">=&gt;</span> <span class="p">%{</span>
            <span class="s2">"timeout_ms"</span> <span class="o">=&gt;</span> <span class="p">%{</span>
              <span class="s2">"type"</span>        <span class="o">=&gt;</span> <span class="s2">"integer"</span><span class="p">,</span>
              <span class="s2">"description"</span> <span class="o">=&gt;</span> <span class="s2">"Max milliseconds to wait (default 300000)"</span>
            <span class="p">}</span>
          <span class="p">}</span>
        <span class="p">},</span>
        <span class="ss">execute_fn:</span> <span class="k">fn</span> <span class="n">_agent_id</span><span class="p">,</span> <span class="n">_id</span><span class="p">,</span> <span class="n">args</span> <span class="o">-&gt;</span>
          <span class="n">timeout</span> <span class="o">=</span> <span class="no">Map</span><span class="o">.</span><span class="n">get</span><span class="p">(</span><span class="n">args</span><span class="p">,</span> <span class="s2">"timeout_ms"</span><span class="p">,</span> <span class="mi">300_000</span><span class="p">)</span>
          <span class="k">case</span> <span class="no">System</span><span class="o">.</span><span class="n">cmd</span><span class="p">(</span><span class="s2">"mix"</span><span class="p">,</span> <span class="p">[</span><span class="s2">"test"</span><span class="p">],</span> <span class="ss">timeout:</span> <span class="n">timeout</span><span class="p">)</span> <span class="k">do</span>
            <span class="p">{</span><span class="n">output</span><span class="p">,</span> <span class="mi">0</span><span class="p">}</span> <span class="o">-&gt;</span> <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">output</span><span class="p">}</span>
            <span class="p">{</span><span class="n">output</span><span class="p">,</span> <span class="n">_</span><span class="p">}</span> <span class="o">-&gt;</span> <span class="p">{</span><span class="ss">:error</span><span class="p">,</span> <span class="n">output</span><span class="p">}</span>
          <span class="k">end</span>
        <span class="k">end</span>
      <span class="p">)</span>
    <span class="p">]</span>
  <span class="k">end</span>
<span class="k">end</span>
</code></pre></div></div>

<p>When Planck starts, it connects to the sidecar node and calls <code class="language-plaintext highlighter-rouge">list_tools/0</code>. The returned
tools are loaded into the resource pool and available to any agent that declares them. The
sidecar can be as minimal as a single module or as rich as a full Phoenix application with
its own database connection, supervision tree, and tests.</p>

<p>The core library does not change regardless.</p>

<blockquote>
  <p>The sidecar is the extension point. The harness is the stable surface.</p>
</blockquote>

<h2 id="the-cli-web-ui">The <del>CLI</del> Web UI</h2>

<p>I considered a TUI when starting the CLI harness. A terminal interface felt natural for a
coding agent. But I didn’t want just a coding agent. I wanted a team of agents for different
tasks.</p>

<p>I wanted to have a better way to access it. A web UI allows me to host it on a server and reach
it from any device with a browser. I no longer need to be in my shell. I can be on my phone,
on my tablet, on a random toaster screen.</p>

<p>A Phoenix LiveView application handles Markdown rendering, syntax-highlighted code blocks,
streaming text, and a real-time sidebar showing each agent’s token usage and cost.</p>

<p>The same Phoenix app also serves an HTTP/SSE API at <code class="language-plaintext highlighter-rouge">/api</code>. Any process that can make an
HTTP request or consume a Server-Sent Events stream gets full access to the session.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GET  /api/sessions              # list all sessions
POST /api/sessions              # start a session
POST /api/sessions/:id/prompt   # send a prompt
GET  /api/sessions/:id/events   # SSE event stream
</code></pre></div></div>

<p>This API allows users to control Planck with other agent harnesses e.g., Claude Code could
control a full Planck team remotely.</p>

<h2 id="conclusion">Conclusion</h2>

<p>The previous three posts described three independent constraints. Planck is where they
connect: small agents with focused context, organized into specialized teams, routing each
step to the right model.</p>

<p><a href="https://hex.pm/packages/planck_ai"><code class="language-plaintext highlighter-rouge">planck_ai</code></a>, <a href="https://hex.pm/packages/planck_agent"><code class="language-plaintext highlighter-rouge">planck_agent</code></a>,
and <a href="https://hex.pm/packages/planck_headless"><code class="language-plaintext highlighter-rouge">planck_headless</code></a> are each published to Hex as
standalone libraries. You can use them independently to build your own harness, or start
from Planck’s agent harness and extend it via the sidecar.</p>

<p>In the next post, we package Planck into a self-contained Docker bundle and ship a sidecar
with it: memory management, context compaction, credential handling, and hooks.</p>

<blockquote>
  <p>As small as meaningful. As focused as necessary. As local as possible.</p>
</blockquote>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>Building Planck #3: The 20/80 Split: Why the Future Is Mostly Local</title>
          <description>Frontier models are brilliant but expensive. Local models are fast and cheap but need guidance. Here's how to combine them.</description>
          <guid>https://thebroken.link/building-planck-the-20-80-split/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Fri, 15 May 2026 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/building-planck-the-20-80-split/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p>We fixed the context. Now look at our bill.</p>

<p>We have <a href="/building-planck-specialized-teams-vs-general-agents">optimized teams of agents</a> working. Quality is good, but it’s getting expensive
fast. Faced with that cost, we try to drop to cheaper frontier models at the expense of quality.
But that’s the wrong approach.</p>

<p>The question isn’t <strong>which frontier model to use</strong>. It’s: <strong>which steps actually need one?</strong></p>

<blockquote>
  <p>Most of them don’t.</p>
</blockquote>

<h2 id="the-specification-problem">The Specification Problem</h2>

<p>There is one thing frontier models are genuinely exceptional at: <strong>turning ambiguous intent
into a precise, unambiguous specification</strong>.</p>

<p>“Clean up the auth module.” “Improve the performance of this query.”
“Write a blog post about AI.” These are underspecified requests. We need some brainstorming, 
follow-up questions and judgment to get there. Frontier models excel at this.</p>

<p>Once the specification is clear, execution doesn’t need a high level of intelligence. It just
needs a model that knows how to follow instructions.</p>

<blockquote>
  <p>The expensive part of a workflow is <em>knowing what to do</em>. <em>Doing it</em> is mostly mechanical.</p>
</blockquote>

<p>This realization changes the architecture.</p>

<h2 id="the-2080-split">The 20/80 Split</h2>

<p>The numbers echo the Pareto principle<sup><a href="#ref-1" id="cite-1">[1]</a></sup>: a small fraction of inputs drives the majority of
outcomes. Specification is that fraction.</p>

<ul>
  <li><strong>20% frontier</strong>: an orchestrator that reasons about the task, breaks it down, writes a
precise spec for each step, and reviews the output.</li>
  <li><strong>80% local</strong>: workers that execute against the spec: searching, writing, calling APIs,
transforming data using smaller, faster, cheaper models.</li>
</ul>

<figure class="flex flex-col items-center w-full mx-auto my-6">
  <div class="mermaid">
flowchart TB
    U["User request"] --&gt; O
    subgraph F["☁️ Frontier (20%)"]
        O["Orchestrator"]
    end
    O --&gt;|"precise spec"| W1
    O --&gt;|"precise spec"| W2
    O --&gt;|"precise spec"| W3
    subgraph L["🏠 Local (80%)"]
        W1["Worker"]
        W2["Worker"]
        W3["Worker"]
    end
    W1 &amp; W2 &amp; W3 --&gt;|"results"| O
    O --&gt; R["Response"]
</div>
  <figcaption class="text-center text-sm italic mt-3 text-muted">
    <p>The orchestrator runs once to reason and specify. Workers run constantly to execute. Cost follows the split.</p>

  </figcaption>
  <div class="sr-only"><code>
flowchart TB
    U["User request"] --&gt; O
    subgraph F["☁️ Frontier (20%)"]
        O["Orchestrator"]
    end
    O --&gt;|"precise spec"| W1
    O --&gt;|"precise spec"| W2
    O --&gt;|"precise spec"| W3
    subgraph L["🏠 Local (80%)"]
        W1["Worker"]
        W2["Worker"]
        W3["Worker"]
    end
    W1 &amp; W2 &amp; W3 --&gt;|"results"| O
    O --&gt; R["Response"]
</code></div>
</figure>

<p>The orchestrator is expensive, but runs less frequently. It runs at the start of a task,
at the end, and occasionally mid-task to course-correct.</p>

<p>The workers run constantly and they’re cheap.</p>

<p>A well-written spec changes what local models can do. The same model that produces mediocre
results on <code class="language-plaintext highlighter-rouge">"refactor the auth module"</code> will reliably handle:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Extract the session handling from `user_controller.ex` into a new `Session` module,
preserving the existing public interface.
</code></pre></div></div>

<blockquote>
  <p>The model didn’t get smarter. The input did.</p>
</blockquote>

<h2 id="running-local-in-practice">Running Local in Practice</h2>

<p>I run local models on a <a href="https://frame.work/es/en/desktop">Framework Desktop</a> at home. Though
it started as a hobby, these models became part of my workflows.</p>

<p>I still use frontier models for specifications. They’re awesome at that job. But my local
models can follow these specifications almost flawlessly, for next to nothing.</p>

<p>This is why <a href="https://thebroken.link/planck">Planck</a> supports both frontier and local models
natively. I’ve seen the 20/80 split match frontier-only quality in my own AI teams.
I’ve gotten good quality at a fraction of the cost.</p>

<h2 id="owning-the-loop">Owning the Loop</h2>

<p>Cost isn’t the only reason to run local though.</p>

<p>Some of the models I run locally are also decent at specifications for certain tasks. They’re
slow, but they get the job done with the right context. That lets some workflows run
<strong>entirely local</strong>.</p>

<p>When we call a frontier model API, our data leaves our machine. Code, context, conversation
history… all sent to a third party. Their pricing decisions affect our workflow. Their model
updates can change our agent’s behavior overnight. Their rate limits cap our throughput.</p>

<p>When we run local, data stays on our hardware. The pricing is predictable as electricity is
the main cost. Models don’t change unless we update them. Throughput is bounded by our machine, 
not a provider queue.</p>

<blockquote>
  <p>Depending on the hardware, it could even be environmentally friendly.<sup><a href="#ref-2" id="cite-2">[2]</a></sup><sup><a href="#ref-3" id="cite-3">[3]</a></sup><sup><a href="#ref-4" id="cite-4">[4]</a></sup></p>
</blockquote>

<h2 id="conclusion">Conclusion</h2>

<p>The <a href="/building-planck-specialized-teams-vs-general-agents">previous post</a> gave us the team
model. This post adds the cost dimension: not every step needs a frontier model, and knowing
which steps do is most of the architectural work.</p>

<p><a href="https://thebroken.link/planck">Planck</a> supports this natively. Each agent in a team
configures its own model provider independently. The orchestrator can call a frontier API
while workers call a local endpoint. The team doesn’t need to change.</p>

<p>In the <a href="/building-planck-introducing-planck">next post</a>, we introduce Planck properly: what
it is, how it works, and how context, specialization, and model routing come together in a
single system.</p>

<blockquote>
  <p>Frontier for thinking. Local for doing.</p>
</blockquote>

<h2 id="references">References</h2>

<ol class="references"><li id="ref-1">
    <a href="https://en.wikipedia.org/wiki/Pareto_principle" target="_blank" rel="noopener noreferrer">Pareto Principle</a>
    <a href="#cite-1" aria-label="Back to citation 1">↩</a>
  </li><li id="ref-2">
    <a href="https://www.phoronix.com/review/framework-desktop-power/5" target="_blank" rel="noopener noreferrer">Framework Desktop: A Deep Dive Into Power &amp; Thermals</a>
    <a href="#cite-2" aria-label="Back to citation 2">↩</a>
  </li><li id="ref-3">
    <a href="https://community.frame.work/t/minimum-power-draw-idle/70184" target="_blank" rel="noopener noreferrer">Minimum Power Draw – Idle (Framework Community)</a>
    <a href="#cite-3" aria-label="Back to citation 3">↩</a>
  </li><li id="ref-4">
    <a href="https://frame.work/sustainability" target="_blank" rel="noopener noreferrer">Framework Sustainability</a>
    <a href="#cite-4" aria-label="Back to citation 4">↩</a>
  </li></ol>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>Building Planck #2: Specialized Teams vs General Agents</title>
          <description>Why one AI trying to do everything produces mediocre results and how to design teams that don't.</description>
          <guid>https://thebroken.link/building-planck-specialized-teams-vs-general-agents/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Fri, 08 May 2026 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/building-planck-specialized-teams-vs-general-agents/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p>Reducing what an agent sees is a step in the right direction. But if we only have one
agent, we’re always making a trade-off: give it more tools and it gets capable but
unfocused; take tools away and it can’t do the job.</p>

<p>The real solution is to stop trying to fit the whole workflow into one context window.</p>

<h2 id="the-appeal-of-the-general-agent">The Appeal of the General Agent</h2>

<p>So, we build the general agent. A capable model, a broad system prompt, 30 tools. The
agent can “handle anything.” It works. Sort of. For simple tasks it’s fine. For anything
multi-step, the results are inconsistent. Sometimes brilliant, sometimes mediocre, often unpredictable.</p>

<p>We end up with an agent a mile wide and an inch deep.
The problem: <a href="/building-planck-context-is-the-real-problem#how-attention-works">attention</a>.</p>

<p>When every turn includes tool definitions and instructions that aren’t relevant
to the current step, the model still reads them. Attention gets diluted and the
output degrades.</p>

<blockquote>
  <p>The more an agent can do, the harder it is to do any one thing well.</p>
</blockquote>

<h2 id="the-team-model">The Team Model</h2>

<p>The alternative isn’t a smarter agent. It’s a smaller one. <strong>Several of them.</strong></p>

<p>Instead of one agent that does everything, you have a small group where each member has a
narrow role. An orchestrator that reasons about the task, breaks it down, and delegates.
Workers that execute each step with minimal, focused context.</p>

<p>We need to deconstruct our workflows and figure out which experts each step requires.
That’s how we turn our workflows into effective teams.</p>

<figure class="flex flex-col items-center w-full mx-auto my-6">
  <div class="mermaid">
sequenceDiagram
    participant U as User
    participant O as Orchestrator
    participant P as Planner
    participant D as Developer
    participant R as Reviewer

    U-&gt;&gt;O: "Add input validation to the login form"
    O-&gt;&gt;P: Plan and execute this task
    P-&gt;&gt;D: Implement step 1 [spec]
    D-&gt;&gt;R: [code]
    R-&gt;&gt;D: [feedback]
    D-&gt;&gt;R: [revised code]
    R-&gt;&gt;P: Step 1 done ✓
    P-&gt;&gt;D: Implement step 2 [spec]
    D-&gt;&gt;R: [code]
    R-&gt;&gt;P: Step 2 done ✓
    P--&gt;&gt;O: Task complete
    O--&gt;&gt;U: Response
</div>
  <figcaption class="text-center text-sm italic mt-3 text-muted">
    <p>The Planner coordinates steps. The Developer and Reviewer work directly with each other. The Orchestrator only sees the result.</p>

  </figcaption>
  <div class="sr-only"><code>
sequenceDiagram
    participant U as User
    participant O as Orchestrator
    participant P as Planner
    participant D as Developer
    participant R as Reviewer

    U-&gt;&gt;O: "Add input validation to the login form"
    O-&gt;&gt;P: Plan and execute this task
    P-&gt;&gt;D: Implement step 1 [spec]
    D-&gt;&gt;R: [code]
    R-&gt;&gt;D: [feedback]
    D-&gt;&gt;R: [revised code]
    R-&gt;&gt;P: Step 1 done ✓
    P-&gt;&gt;D: Implement step 2 [spec]
    D-&gt;&gt;R: [code]
    R-&gt;&gt;P: Step 2 done ✓
    P--&gt;&gt;O: Task complete
    O--&gt;&gt;U: Response
</code></div>
</figure>

<blockquote>
  <p><strong>Note</strong>: Though the example is for software development, we can create any team
configuration we want, e.g., for content strategy, a Researcher, a Strategist, a Writer,
and an Editor could be enough to create a content pipeline.</p>
</blockquote>

<h2 id="defining-roles">Defining Roles</h2>

<p>Each agent in a team has two things that together define its role: a <strong>system prompt</strong> and
a <strong>tool list</strong>.</p>

<p><strong>The system prompt</strong> describes how the agent should behave, e.g., the planner’s prompt describes
how to break a task into steps, the developer’s describes how to write code. Each prompt is
small and focused on exactly one job.</p>

<p><strong>The tool list</strong> is what the agent can actually do, e.g., a developer gets file access and a
shell, a reviewer gets read access, a planner might get read access and web search
capabilities, the orchestrator gets the tools to delegate work to others.</p>

<p>A developer without delegation tools simply cannot delegate, not because it’s instructed
not to, but because the capability isn’t there.</p>

<blockquote>
  <p>We can’t prompt our way out of a missing tool.</p>
</blockquote>

<h2 id="the-compound-effect">The Compound Effect</h2>

<p>Splitting work across specialized agents has two compounding effects:</p>

<ul>
  <li><strong>Smaller, stable system prompts:</strong> Each agent’s prompt describes exactly one role and
doesn’t grow as we add capabilities to other agents. A stable system prompt means the
<a href="/building-planck-context-is-the-real-problem#the-prefix-cache">prefix cache</a> works.
We pay to compute it once and reuse it across every turn.</li>
  <li><strong>Quality is higher per step:</strong> A worker that only knows how to search is very good at
searching. A worker that only knows how to write is very good at writing. Narrow scope
produces better outputs, not worse.<sup><a href="#ref-1" id="cite-1">[1]</a></sup></li>
</ul>

<blockquote>
  <p>Each agent doing less means the team doing more.</p>
</blockquote>

<h2 id="conclusion">Conclusion</h2>

<p>Analyze your own workflows. Extract the experts needed for each step. Build your team of
agents.</p>

<p>This team-based architecture is at the core of <a href="https://thebroken.link/planck">Planck</a>:
specialized agents working together, each focused on exactly one job.</p>

<p>In the <a href="/building-planck-the-20-80-split">next post</a>, we look at the cost side of this
architecture: most steps in a well-designed team don’t need a frontier model to execute them.</p>

<blockquote>
  <p>A well-designed team gives you a system where each agent is small enough to be reliable
and focused enough to be effective.</p>
</blockquote>

<h2 id="references">References</h2>

<ol class="references"><li id="ref-1">
    <a href="https://arxiv.org/abs/2605.24660" target="_blank" rel="noopener noreferrer">How Many Tools Should an LLM Agent See? A Chance-Corrected Answer</a>
    <a href="#cite-1" aria-label="Back to citation 1">↩</a>
  </li></ol>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>Building Planck #1: Context Is the Real Problem</title>
          <description>Why giving your AI agent more context usually makes it worse and what to do instead.</description>
          <guid>https://thebroken.link/building-planck-context-is-the-real-problem/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Thu, 30 Apr 2026 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/building-planck-context-is-the-real-problem/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p>Your agent is underperforming. You swapped from your local models to your favorite AI
cloud provider models and back, added more instructions, added more skills, added more
tools. The results are inconsistent: sometimes brilliant, sometimes mediocre.</p>

<p>You’re starting to think you need a better model. Every new model release generates an
incredible FOMO, but, behind all the excitement, there’s one hopeful thought:</p>

<blockquote>
  <p>“This one will fix my workflow!”</p>
</blockquote>

<p>After burning millions of tokens, things have improved, but your workflow is not
quite there yet. It’s not fixed. It’s more expensive though.</p>

<p>If that resonates with you, <strong>you have a context problem.</strong></p>

<p>We need to fix a mental model that holds back most people: almost every quality
problem in AI agents traces back to poor context decisions, not model capability.</p>

<h2 id="what-context-actually-is">What Context Actually Is</h2>

<p>When we’re <em>“chatting”</em> with our agent, we’re not really chatting with it. We’re
actually sending an HTTP request to a server. Every single HTTP request includes
everything that’s in the current chat session. Yes, everything. Every single turn.</p>

<p>The model has no persistent memory between calls. What it knows comes entirely from
what we include in that payload right now. That payload is our <strong>context window</strong>.
It contains four things:</p>

<ul>
  <li><strong>System prompt</strong>: Instructions defining the agent’s role, behavior, and rules.</li>
  <li><strong>Tool definitions</strong>: Descriptions of every tool the agent can call, including
their parameters and what they return.</li>
  <li><strong>Conversation history</strong>: Every prior user message and assistant response in the
current session.</li>
  <li><strong>Injected knowledge</strong>: Search results, file contents, retrieved memories and skills.
Anything that was included dynamically.</li>
</ul>

<figure class="flex flex-col items-center w-full mx-auto my-6">
  <div class="mermaid">
flowchart LR
    subgraph CW["Context Window"]
        direction TB
        SP["System Prompt"]
        TD["Tool Definitions"]
        CH["Conversation History"]
        IK["Injected Knowledge"]
    end
    CW --&gt;|"API call"| LLM[("LLM Provider")]
</div>
  <figcaption class="text-center text-sm italic mt-3 text-muted">
    <p>Every turn sends the full context window to the provider (cloud or local).</p>

  </figcaption>
  <div class="sr-only"><code>
flowchart LR
    subgraph CW["Context Window"]
        direction TB
        SP["System Prompt"]
        TD["Tool Definitions"]
        CH["Conversation History"]
        IK["Injected Knowledge"]
    end
    CW --&gt;|"API call"| LLM[("LLM Provider")]
</code></div>
</figure>

<p>And though some models support big context windows, that doesn’t mean we can afford
to fill them: not only because of the monetary cost, but also because every token included
is a token the model must attend to.</p>

<h2 id="how-attention-works">How Attention Works</h2>

<p>When we say the model <strong>“attends to”</strong> every token, what does that actually mean?</p>

<p>For each token, the model computes a score between every pair of tokens in the context.
These scores determine how much each token should influence the next one generated.</p>

<p>If the model is completing <strong>“Caracas is the capital of”</strong>, it attends
more strongly to <strong>“capital”</strong> and <strong>“Caracas”</strong> than to <strong>“is”</strong> and <strong>“the”</strong>. The
scores reflect relevance.</p>

<p>That scoring happens across every pair. A 1,000-token context means 1,000,000 pairs
to score. Double the context to 2,000 tokens and you have 4,000,000 pairs. Four times
the computation for twice the tokens.</p>

<blockquote>
  <p><strong>Attention is quadratic in context length.</strong></p>
</blockquote>

<figure class="flex flex-col items-center w-full mx-auto my-6">
  <div class="mermaid">
xychart-beta
    x-axis "Amount of tokens" ["1k", "2k", "4k", "8k", "16k", "32k"]
    y-axis "Attention pairs (M)" 0 --&gt; 1100
    line [1, 4, 16, 64, 256, 1024]
</div>
  <figcaption class="text-center text-sm italic mt-3 text-muted">
    <p>Doubling context length quadruples the number of attention pairs the model must score.</p>

  </figcaption>
  <div class="sr-only"><code>
xychart-beta
    x-axis "Amount of tokens" ["1k", "2k", "4k", "8k", "16k", "32k"]
    y-axis "Attention pairs (M)" 0 --&gt; 1100
    line [1, 4, 16, 64, 256, 1024]
</code></div>
</figure>

<p>There is a second consequence beyond cost: <strong>relevance degrades</strong>. The model doesn’t
skip tokens that aren’t relevant. They’re there and they affect the quality of
the result.</p>

<p>If half the context is boilerplate instructions that don’t apply to this specific task,
those tokens still compete with the ones that do.</p>

<blockquote>
  <p>It costs us more money and it compromises quality.</p>
</blockquote>

<p>All those token scores are computed and stored in the <strong>KV-cache</strong>:</p>

<figure class="flex flex-col items-center w-full mx-auto my-6">
  <div class="mermaid">
flowchart LR
    T["The"]
    Q["quick"]
    B["brown"]
    F["fox"]

    F --&gt;|"k=0.71, v=0.56"| B
    F --&gt;|"k=0.45, v=0.32"| Q
    F --&gt;|"k=0.23, v=0.87"| T
    B --&gt;|"k=0.45, v=0.32"| Q
    B --&gt;|"k=0.23, v=0.87"| T
    Q --&gt;|"k=0.23, v=0.87"| T
</div>
  <figcaption class="text-center text-sm italic mt-3 text-muted">
    <p>Each arrow carries the cached key (k) and value (v) of the target token. Notice that “The” is looked up three times. Its stored pair is reused without recomputation each time.</p>

  </figcaption>
  <div class="sr-only"><code>
flowchart LR
    T["The"]
    Q["quick"]
    B["brown"]
    F["fox"]

    F --&gt;|"k=0.71, v=0.56"| B
    F --&gt;|"k=0.45, v=0.32"| Q
    F --&gt;|"k=0.23, v=0.87"| T
    B --&gt;|"k=0.45, v=0.32"| Q
    B --&gt;|"k=0.23, v=0.87"| T
    Q --&gt;|"k=0.23, v=0.87"| T
</code></div>
</figure>

<p>When <code class="language-plaintext highlighter-rouge">fox</code> is generated, it attends to the key-value pairs of every token before it. The
pairs are the <strong>KV-cache</strong>.</p>

<blockquote>
  <p>Double the context length, quadruple the KV-cache memory.</p>
</blockquote>

<p><strong>GPU memory has become one of the most contested resources on the planet.</strong> The main DRAM
producers are now shifting their capacity to serve the AI industry. This has created a
RAM shortage where memory prices have roughly tripled by late 2025.<sup><a href="#ref-1" id="cite-1">[1]</a></sup><sup><a href="#ref-2" id="cite-2">[2]</a></sup></p>

<p>For AI cloud providers, KV-cache memory is one of their largest costs: each chat
exponentially grows its memory usage as it progresses. It’s also what drives them to
invest heavily in compression and caching strategies.</p>

<h2 id="do-you-feel-model-x-is-getting-dumber">Do You Feel <em>Model X</em> Is Getting Dumber?</h2>

<p>It’s not your imagination. And it’s probably not the model.</p>

<p>When a context grows very long, AI cloud providers can no longer fit the full KV-cache in
memory at standard precision. To compensate, they use compressed context representations.</p>

<p>Many open-source context compression techniques exist, and AI cloud providers are almost
certainly running proprietary variants optimized for their infrastructure.</p>

<p>Usually compression will lead to lossy memory. If it’s done aggressively, the model’s
output could degrade. If this happens, it will seem a bit dumber and forgetful than before.
The model is the same though.</p>

<p>This is not a fringe edge case. It’s the operational reality of running agents at
scale, and it’s why context discipline matters even when you’re not worried about
cost.</p>

<blockquote>
  <p>An agent that runs long sessions without managing its context doesn’t just
get expensive. It gets unreliable.</p>
</blockquote>

<h2 id="the-prefix-cache">The Prefix Cache</h2>

<p>Not all tokens are equally expensive.</p>

<p>When you send an API request, the provider computes attention over every token in
your context. That’s expensive.</p>

<p>However, providers cache the KV-cache of each request for a limited time. If the
beginning of your next request is identical to a previous request (everything is the
same except the last message), the provider will use the cached KV-cache of your previous
request and only compute the KV-cache pairs for the new tokens. This is the <strong>prefix cache</strong>.</p>

<p>As long as the cached prefix hasn’t expired, you pay to compute it exactly once, then
reuse it across every subsequent turn for a fraction of the cost.</p>

<blockquote>
  <p><strong>Note</strong>: For local models, the prefix cache also exists. In this case, the payment is
waiting for computing the KV-cache of the whole conversation before actually outputting
the response of your last message. This also happens for AI cloud providers, but
their hardware is faster. Still, it’s expensive to calculate.</p>
</blockquote>

<figure class="flex flex-col items-center w-full mx-auto my-6">
  <div class="mermaid">
flowchart TD
    A["New message"] --&gt; B{"Conversation history matches?"}
    B --&gt;|Yes| C["✅ Cache hit"]
    B --&gt;|No| D["❌ Cache invalidated"]
    D --&gt; E["Recompute KV-cache for all the context"]
    C --&gt; F["Recompute KV-cache for the last tokens"]
</div>
  <figcaption class="text-center text-sm italic mt-3 text-muted">
    <p>A matching conversation prefix is a cache hit. Only the new tokens need to be computed.</p>

  </figcaption>
  <div class="sr-only"><code>
flowchart TD
    A["New message"] --&gt; B{"Conversation history matches?"}
    B --&gt;|Yes| C["✅ Cache hit"]
    B --&gt;|No| D["❌ Cache invalidated"]
    D --&gt; E["Recompute KV-cache for all the context"]
    C --&gt; F["Recompute KV-cache for the last tokens"]
</code></div>
</figure>

<p>So, two rules apply:</p>
<ul>
  <li>Use a reasonable prefix cache expiration time.</li>
  <li>Don’t dynamically inject information in your system prompt for an active conversation e.g.
adding <code class="language-plaintext highlighter-rouge">Current date: &lt;date&gt;</code>.</li>
</ul>

<blockquote>
  <p>Keep static instructions in the system prompt. Put dynamic state in the conversation messages.</p>
</blockquote>

<h2 id="the-generalist-trap">The Generalist Trap</h2>

<p>A broad system prompt feels productive. You cover every case. You add 30 tools.
The agent can “do anything.” Then it does everything… average.</p>

<p>The problem is <a href="#how-attention-works">attention</a>. When the context is packed with instructions and
tool definitions that aren’t relevant to the current task, the model still reads them.</p>

<p>A specialist agent with a 100-token system prompt and 2 relevant tools
consistently outperforms a generalist with a 100,000-token system prompt
and 10 tools on the tasks the specialist is designed for.</p>

<p>Research confirms the underlying mechanism: context length alone degrades performance 13–85%, even with perfect retrieval.<sup><a href="#ref-3" id="cite-3">[3]</a></sup></p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">Tool</th>
      <th style="text-align: left">System prompt</th>
      <th style="text-align: left">Role</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left">Claude Code</td>
      <td style="text-align: left">~100K+ tokens<sup><a href="#ref-4" id="cite-4">[4]</a></sup></td>
      <td style="text-align: left">Generalist</td>
    </tr>
    <tr>
      <td style="text-align: left">Codex CLI</td>
      <td style="text-align: left">~100K tokens<sup><a href="#ref-5" id="cite-5">[5]</a></sup></td>
      <td style="text-align: left">Generalist</td>
    </tr>
    <tr>
      <td style="text-align: left">Antigravity</td>
      <td style="text-align: left">~8–10K tokens<sup><a href="#ref-6" id="cite-6">[6]</a></sup></td>
      <td style="text-align: left">Generalist</td>
    </tr>
    <tr>
      <td style="text-align: left">Pi agent</td>
      <td style="text-align: left">&lt;1K tokens<sup><a href="#ref-7" id="cite-7">[7]</a></sup></td>
      <td style="text-align: left">Specialist harness</td>
    </tr>
    <tr>
      <td style="text-align: left"><a href="https://thebroken.link/planck">Planck</a> orchestrator</td>
      <td style="text-align: left">~1K tokens</td>
      <td style="text-align: left">Specialist harness</td>
    </tr>
    <tr>
      <td style="text-align: left"><a href="https://thebroken.link/planck">Planck</a> worker</td>
      <td style="text-align: left">~100–300 tokens</td>
      <td style="text-align: left">Specialist harness</td>
    </tr>
  </tbody>
</table>

<p>This is not a prompt trick. It’s an architectural decision. The right response
to <strong>“this agent isn’t good enough”</strong> is almost never <strong>“add more instructions.”</strong>
It’s usually <strong>“reduce what it sees.”</strong></p>

<blockquote>
  <p>Smaller scope, tighter system prompt, fewer tools.</p>
</blockquote>

<p>This is why <a href="https://thebroken.link/planck">Planck</a> is built around specialized agent
teams rather than a single generalist.</p>

<h2 id="conclusion">Conclusion</h2>

<p>In this post, I shamelessly introduced <a href="https://thebroken.link/planck">Planck</a>:
a coding harness (and agent libraries) I created to learn more
about agents and experiment with them.</p>

<p>Every post in this series documents the architectural decisions
behind <a href="https://thebroken.link/planck">Planck</a>. Almost every single one of
them is either directly about reducing unnecessary context or about
putting the right context in front of the right model at the right time.</p>

<p>Even if you don’t use <a href="https://thebroken.link/planck">Planck</a> in the future,
I hope this helps you reason about your current workflow and how you
can make it better.</p>

<p>In the <a href="/building-planck-specialized-teams-vs-general-agents">next post</a>, we take this
further: reducing context is only the first step. The real solution is to stop trying to
fit the whole workflow into one agent.</p>

<blockquote>
  <p>The model wasn’t the problem. It never was.</p>
</blockquote>

<h2 id="references">References</h2>

<ol class="references"><li id="ref-1">
    <a href="https://intuitionlabs.ai/articles/ram-shortage-2025-ai-demand" target="_blank" rel="noopener noreferrer">RAM Shortage 2025: How AI Demand is Raising DRAM Prices</a>
    <a href="#cite-1" aria-label="Back to citation 1">↩</a>
  </li><li id="ref-2">
    <a href="https://spectrum.ieee.org/dram-shortage" target="_blank" rel="noopener noreferrer">AI Boom Fuels DRAM Shortage and Price Surge</a>
    <a href="#cite-2" aria-label="Back to citation 2">↩</a>
  </li><li id="ref-3">
    <a href="https://arxiv.org/abs/2510.05381" target="_blank" rel="noopener noreferrer">Context Length Alone Hurts LLM Performance Despite Perfect Retrieval</a>
    <a href="#cite-3" aria-label="Back to citation 3">↩</a>
  </li><li id="ref-4">
    <a href="https://github.com/anthropics/claude-code/issues/45188" target="_blank" rel="noopener noreferrer">System prompt size grew ~70K tokens between Claude Code v2.1.89 and v2.1.96</a>
    <a href="#cite-4" aria-label="Back to citation 4">↩</a>
  </li><li id="ref-5">
    <a href="https://github.com/openai/codex/issues/18526" target="_blank" rel="noopener noreferrer">First message in fresh Codex session consumes ~100K input tokens</a>
    <a href="#cite-5" aria-label="Back to citation 5">↩</a>
  </li><li id="ref-6">
    <a href="https://github.com/x1xhlol/system-prompts-and-models-of-ai-tools/blob/main/Google/Antigravity/Fast%20Prompt.txt" target="_blank" rel="noopener noreferrer">Antigravity Fast Prompt (published system prompt)</a>
    <a href="#cite-6" aria-label="Back to citation 6">↩</a>
  </li><li id="ref-7">
    <a href="https://mariozechner.at/posts/2025-11-30-pi-coding-agent/" target="_blank" rel="noopener noreferrer">What I learned building an opinionated and minimal coding agent</a>
    <a href="#cite-7" aria-label="Back to citation 7">↩</a>
  </li></ol>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>ICON 2.0: Realtime Decentralized Quote Prices</title>
          <description>By leveraging one of ICON's BTP features, we get realtime quote prices from Balanced DEx</description>
          <guid>https://thebroken.link/icon-2.0-realtime-decentralized-quote-prices/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Sat, 05 Mar 2022 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/icon-2.0-realtime-decentralized-quote-prices/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p><a href="https://icon.foundation/">ICON 2.0</a> is a smart contract platform with a unique
interoperability proposition: <a href="https://medium.com/helloiconworld/blockchain-transmission-protocol-btp-an-overview-744aaa51334e">Blockchain Transmission Protocol (BTP)</a>.
<strong>BTP</strong> will enable <strong>decentralized and trustless</strong> interoperability between
<strong>ICON 2.0</strong> and all blockchain that implement the protocol.</p>

<p>At the time of publishing this article, the full <strong>BTP</strong> is not yet released.
However, some of its features are already available to everyone. One of these
features is a websocket to receive realtime updates on either (or both) blocks
produced and event logs emitted.</p>

<p>This article will focus on how we can leverage this websocket connection to
retrieve quote prices from <a href="https://balanced.network">Balanced Decentralized Exchange</a>
by using the <a href="https://github.com/alexdesousa/icon">Elixir ICON 2.0 SDK</a> I wrote
in the past few months.</p>

<p>Without further ado, <strong>let’s go!</strong></p>

<p><img src="https://media.giphy.com/media/RrVzUOXldFe8M/giphy.gif" alt="Let's go!" /></p>

<h2 id="prerequisites">Prerequisites</h2>

<p>This article assumes you have downloaded <code class="language-plaintext highlighter-rouge">icon</code> either within a new project
using <code class="language-plaintext highlighter-rouge">mix new</code> or using a script e.g:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#/usr/bin/env elixir</span>

<span class="no">Mix</span><span class="o">.</span><span class="n">install</span><span class="p">([</span>
  <span class="p">{</span><span class="ss">:icon</span><span class="p">,</span> <span class="s2">"~&gt; 0.1"</span><span class="p">}</span>
<span class="p">])</span>

<span class="c1"># ... rest of the script ...</span>
</code></pre></div></div>

<p>You can also find the full script for getting <em>sICX/bnUSD</em> quotes
<a href="https://gist.github.com/alexdesousa/e8cd1d64a62b74aed14f07b34ce811f3">here</a>.</p>

<h2 id="quote-prices">Quote Prices</h2>

<p>When we want to exchange one token for another, we’re presented with token
pairs. These pairs have a <em>base token</em>, a <em>quote token</em> and, a <em>quote price</em>:</p>

<p>
  <figure class="flex flex-col items-center w-full h-auto mx-auto">
    <img src="/icon-2.0-realtime-decentralized-quote-prices/quote-price.png" alt="ICX/USD means ICX priced in USD" />
    <figcaption class="text-center">
      <p><em>ICX/USD = $0.60</em>: <em>ICX</em> (ICON’s token) priced in <em>USD</em> is worth $0.60</p>

    </figcaption>
  </figure>
</p>

<p>If we, somehow, capture the event logs emitted every time someone exchanges one
token for another, then we’ll have realtime quote prices for any pair we want.
In this article, we’ll subscribe to <strong>Balanced</strong> <em>sICX/bnUSD</em> prices.</p>

<blockquote>
  <p><strong>Note</strong>: <em>sICX</em> is staked ICX token and <em>bnUSD</em> is an algorithmic stablecoin
pegged to the dollar called Balanced Dollars.</p>
</blockquote>

<h2 id="getting-the-swap-log">Getting the Swap Log</h2>

<p><strong>Balanced</strong> is built from several SCOREs (Smart Contract On Reliable
Environment) and one of these SCOREs handles all operations related to the
decentralized exchange (DEx). The main DEx operation is swapping tokens e.g.
we can use the contract to exchange our <strong>sICX</strong> with <strong>bnUSD</strong>.</p>

<p>Furthermore, every time someone swaps a token, the SCORE will emit the
following event:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Swap</span><span class="o">(</span><span class="kt">int</span><span class="o">,</span><span class="nc">Address</span><span class="o">,</span><span class="nc">Address</span><span class="o">,</span><span class="nc">Address</span><span class="o">,</span><span class="nc">Address</span><span class="o">,</span><span class="nc">Address</span><span class="o">,</span><span class="kt">int</span><span class="o">,</span><span class="kt">int</span><span class="o">,</span><span class="kt">int</span><span class="o">,</span><span class="kt">int</span><span class="o">,</span><span class="kt">int</span><span class="o">,</span><span class="kt">int</span><span class="o">,</span><span class="kt">int</span><span class="o">,</span><span class="kt">int</span><span class="o">,</span><span class="kt">int</span><span class="o">)</span>
</code></pre></div></div>

<p>We can query the SCORE’s API with <code class="language-plaintext highlighter-rouge">Icon.get_score_api/2</code> and get the meaning of
each input. For that, we’ll need the SCORE address, which is
<a href="https://tracker.icon.foundation/contract/cxa0af3165c08318e988cb30993b3048335b94af6c">cxa0af3165c08318e988cb30993b3048335b94af6c</a>
in the <em>Mainnet</em>:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">dex_score</span> <span class="o">=</span> <span class="s2">"cxa0af3165c08318e988cb30993b3048335b94af6c"</span>
<span class="n">identity</span> <span class="o">=</span> <span class="no">Icon</span><span class="o">.</span><span class="no">RPC</span><span class="o">.</span><span class="no">Identity</span><span class="o">.</span><span class="n">new</span><span class="p">(</span><span class="ss">network_id:</span> <span class="ss">:mainnet</span><span class="p">)</span>

<span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">api</span><span class="p">}</span> <span class="o">=</span> <span class="no">Icon</span><span class="o">.</span><span class="n">get_score_api</span><span class="p">(</span><span class="n">identity</span><span class="p">,</span> <span class="n">dex_score</span><span class="p">)</span>

<span class="n">swap_definition</span> <span class="o">=</span>
  <span class="no">Enum</span><span class="o">.</span><span class="n">find</span><span class="p">(</span><span class="n">api</span><span class="p">,</span> <span class="k">fn</span> <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="n">name</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="n">type</span><span class="p">}</span> <span class="o">-&gt;</span>
    <span class="n">name</span> <span class="o">==</span> <span class="s2">"Swap"</span> <span class="ow">and</span> <span class="n">type</span> <span class="o">==</span> <span class="s2">"eventlog"</span>
  <span class="k">end</span><span class="p">)</span>
</code></pre></div></div>

<p>In the variable <code class="language-plaintext highlighter-rouge">swap_definition</code>, we’ll have the <code class="language-plaintext highlighter-rouge">Swap</code> event log definition:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">%{</span>
  <span class="s2">"inputs"</span> <span class="o">=&gt;</span> <span class="p">[</span>
    <span class="p">%{</span><span class="s2">"indexed"</span> <span class="o">=&gt;</span> <span class="s2">"0x1"</span><span class="p">,</span> <span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_id"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"int"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"indexed"</span> <span class="o">=&gt;</span> <span class="s2">"0x1"</span><span class="p">,</span> <span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_baseToken"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"Address"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_fromToken"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"Address"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_toToken"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"Address"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_sender"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"Address"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_receiver"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"Address"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_fromValue"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"int"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_toValue"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"int"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_timestamp"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"int"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_lpFees"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"int"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_balnFees"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"int"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_poolBase"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"int"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_poolQuote"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"int"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_endingPrice"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"int"</span><span class="p">},</span>
    <span class="p">%{</span><span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"_effectiveFillPrice"</span><span class="p">,</span> <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"int"</span><span class="p">}</span>
  <span class="p">],</span>
  <span class="s2">"name"</span> <span class="o">=&gt;</span> <span class="s2">"Swap"</span><span class="p">,</span>
  <span class="s2">"type"</span> <span class="o">=&gt;</span> <span class="s2">"eventlog"</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="analyzing-the-swap-event">Analyzing the Swap Event</h2>

<p>With the previous definition, we can start making some assumptions about each
event input and what things we need to get from them in order to have the price
for <em>sICX/bnUSD</em> every time the event is emitted:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">_id</code> is the token pair identifier (<code class="language-plaintext highlighter-rouge">indexed</code> 1st position).</li>
  <li><code class="language-plaintext highlighter-rouge">_baseToken</code> is the SCORE address of the base token of the pair (<code class="language-plaintext highlighter-rouge">indexed</code>
2nd position).</li>
  <li><code class="language-plaintext highlighter-rouge">_fromToken</code> is the SCORE address of the token being sold (<code class="language-plaintext highlighter-rouge">data</code> 1st
position).</li>
  <li><code class="language-plaintext highlighter-rouge">_toToken</code> is the SCORE address of the token being bought (<code class="language-plaintext highlighter-rouge">data</code> 2nd
position).</li>
  <li><code class="language-plaintext highlighter-rouge">_timestamp</code> is the UNIX epoch timestamp in UTC in microseconds (<code class="language-plaintext highlighter-rouge">data</code> 7th
position).</li>
  <li><code class="language-plaintext highlighter-rouge">_endingPrice</code> is the quote price after the swap in <code class="language-plaintext highlighter-rouge">loop</code> (<code class="language-plaintext highlighter-rouge">data</code> 12th
position).</li>
</ul>

<p>Either partially or fully, we can use the previous inputs to find the quote
price we’re looking for.</p>

<p><img src="https://media.giphy.com/media/P1i1JsW2nNoBkn8xbb/giphy.gif" alt="Detective" /></p>

<blockquote>
  <p><strong>Protip</strong>: We can query the pool stats with the DEx function <code class="language-plaintext highlighter-rouge">getPoolStats</code>
and search by ID. This will give us <em>sICX/bnUSD</em> pair has the ID 2. You can
check it out yourself by running the following:</p>

  <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="no">Icon</span><span class="o">.</span><span class="n">call</span><span class="p">(</span><span class="n">identity</span><span class="p">,</span> <span class="n">dex_score</span><span class="p">,</span> <span class="s2">"getPoolStats"</span><span class="p">,</span> <span class="p">%{</span><span class="ss">_id:</span> <span class="mi">2</span><span class="p">},</span>
  <span class="ss">call_schema:</span> <span class="p">%{</span><span class="ss">_id:</span> <span class="ss">:integer</span><span class="p">}</span>
<span class="p">)</span>
</code></pre></div>  </div>

  <p>At the time of writing this article, there are 39 pools, but not all of them
have a name, so in this case using <code class="language-plaintext highlighter-rouge">_baseToken</code>, <code class="language-plaintext highlighter-rouge">_fromToken</code>, and <code class="language-plaintext highlighter-rouge">_toToken</code>
addresses can help us figure out the actual pair name.</p>
</blockquote>

<h2 id="realtime-updates">Realtime Updates</h2>

<p><a href="https://github.com/alexdesousa/icon">Elixir ICON 2.0 SDK</a> has an
<a href="https://github.com/gmtprime/yggdrasil">Yggdrasil</a> adapter for ICON’s websocket.
Thus we can use an <em>Yggdrasil</em> process to subscribe to the <code class="language-plaintext highlighter-rouge">Swap</code> event. In this
case, the most important part is to define out channel correctly:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">Quotes</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">Yggdrasil</span>
  <span class="n">alias</span> <span class="no">Icon</span><span class="o">.</span><span class="no">Schema</span><span class="o">.</span><span class="no">Types</span><span class="o">.</span><span class="no">EventLog</span>

  <span class="nv">@dex_contract</span> <span class="s2">"cxa0af3165c08318e988cb30993b3048335b94af6c"</span>
  <span class="nv">@sicx_bnusd_id</span> <span class="mi">2</span>
  <span class="nv">@signature</span> <span class="s2">"Swap(int,Address,Address,Address,Address,Address,int,int,int,int,int,int,int,int,int)"</span>

  <span class="nv">@channel</span> <span class="p">[</span>
    <span class="ss">adapter:</span> <span class="ss">:icon</span><span class="p">,</span>
    <span class="ss">name:</span> <span class="p">%{</span>
      <span class="ss">source:</span> <span class="ss">:event</span><span class="p">,</span>
      <span class="ss">data:</span> <span class="p">%{</span>
        <span class="ss">addr:</span> <span class="nv">@dex_contract</span><span class="p">,</span>
        <span class="ss">event:</span> <span class="nv">@signature</span><span class="p">,</span>
        <span class="ss">indexed:</span> <span class="p">[</span><span class="nv">@sicx_bnusd_id</span><span class="p">,</span> <span class="no">nil</span><span class="p">]</span>
      <span class="p">},</span>
      <span class="ss">from_height:</span> <span class="mi">47_077_000</span>
    <span class="p">}</span>
  <span class="p">]</span>

  <span class="k">def</span> <span class="n">start_link</span><span class="p">,</span> <span class="k">do</span><span class="p">:</span> <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">start_link</span><span class="p">(</span><span class="bp">__MODULE__</span><span class="p">,</span> <span class="p">[</span><span class="nv">@channel</span><span class="p">])</span>

  <span class="c1"># ... handle_event/3 definition ...</span>
<span class="k">end</span>
</code></pre></div></div>

<blockquote>
  <p><strong>Protip</strong>: I added <code class="language-plaintext highlighter-rouge">from_height: 47_077_000</code> field to the channel’s <code class="language-plaintext highlighter-rouge">name</code>
for subscribing to an older block height. This way, we don’t need to wait for
a <code class="language-plaintext highlighter-rouge">Swap</code> event to happen and we can see some results right away. In general,
this is only needed for testing purposes, but it wouldn’t be needed if we just
want the latest price.</p>
</blockquote>

<h2 id="showing-the-prices">Showing the Prices</h2>

<p>The events we’ll receive will have:</p>

<ul>
  <li>The <code class="language-plaintext highlighter-rouge">_timestamp</code> in microseconds in the 7th position of <code class="language-plaintext highlighter-rouge">data</code>.</li>
  <li>The <code class="language-plaintext highlighter-rouge">_endingPrice</code> in the 12th position of <code class="language-plaintext highlighter-rouge">data</code> (<code class="language-plaintext highlighter-rouge">_endingPrice * 10¹⁸</code>).</li>
</ul>

<p>So we can now define our <code class="language-plaintext highlighter-rouge">handle_event/3</code> callback and finally print our quote
price in the console:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">Quotes</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">Yggdrasil</span>
  <span class="n">alias</span> <span class="no">Icon</span><span class="o">.</span><span class="no">Schema</span><span class="o">.</span><span class="no">Types</span><span class="o">.</span><span class="no">EventLog</span>

  <span class="c1"># ... channel definition ...</span>

  <span class="nv">@impl</span> <span class="no">Yggdrasil</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="n">_channel</span><span class="p">,</span> <span class="p">%</span><span class="no">EventLog</span><span class="p">{}</span> <span class="o">=</span> <span class="n">swap_event</span><span class="p">,</span> <span class="n">_state</span><span class="p">)</span> <span class="k">do</span>
    <span class="p">[</span><span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">timestamp</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">price</span><span class="p">,</span> <span class="n">_</span><span class="p">]</span> <span class="o">=</span> <span class="n">swap_event</span><span class="o">.</span><span class="n">data</span>

    <span class="n">datetime</span> <span class="o">=</span>
      <span class="n">timestamp</span>
      <span class="o">|&gt;</span> <span class="no">DateTime</span><span class="o">.</span><span class="n">from_unix!</span><span class="p">(</span><span class="ss">:microsecond</span><span class="p">)</span>
      <span class="o">|&gt;</span> <span class="no">DateTime</span><span class="o">.</span><span class="n">to_iso8601</span><span class="p">()</span>

    <span class="n">price</span> <span class="o">=</span> <span class="n">price</span> <span class="o">/</span> <span class="mi">1_000_000_000_000_000_000</span>

    <span class="no">IO</span><span class="o">.</span><span class="n">puts</span><span class="p">(</span><span class="s2">"[</span><span class="si">#{</span><span class="n">datetime</span><span class="si">}</span><span class="s2">] sICX/bnUSD price: </span><span class="si">#{</span><span class="n">price</span><span class="si">}</span><span class="s2">"</span><span class="p">)</span>

    <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="no">nil</span><span class="p">}</span>
  <span class="k">end</span>
<span class="k">end</span>
</code></pre></div></div>

<p>If we execute this process:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="no">Quotes</span><span class="o">.</span><span class="n">start_link</span><span class="p">()</span>
</code></pre></div></div>

<p>then we’ll get the following output:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[2022-03-07T16:17:15.095799Z] sICX/bnUSD price: 0.6762914917929946
[2022-03-07T16:33:31.390351Z] sICX/bnUSD price: 0.6765223727152583
[2022-03-07T16:19:25.140181Z] sICX/bnUSD price: 0.6763077935623969
[2022-03-07T16:06:10.885982Z] sICX/bnUSD price: 0.6696113966372721
[2022-03-07T16:28:17.210851Z] sICX/bnUSD price: 0.6765180152082558
[2022-03-07T16:27:41.288288Z] sICX/bnUSD price: 0.6764913565725723
... continues ...
</code></pre></div></div>

<blockquote>
  <p><strong>Note</strong>: You can check out the full script
<a href="https://gist.github.com/alexdesousa/e8cd1d64a62b74aed14f07b34ce811f3">here</a>.</p>
</blockquote>

<p><img src="https://media.giphy.com/media/it6W8D4FfvaPC/giphy.gif" alt="Beautiful" /></p>

<h2 id="conclusion">Conclusion</h2>

<p>Though BTP is not fully released, we can already start leveraging some of its
core features for our own advantage. Given ICON’s block time is 3 seconds, this
websocket connection gives us a tremendous advantage for building realtime bots
for different purposes: from maximizing our yield to securing our favorite NFT
latest drop.</p>

<p><strong>ICON 2.0 definitely</strong> has a great advantage over many other <em>slow</em> blockchains
and I believe its DeFi ecosystem <strong>will have a bright future!</strong></p>

<p><img src="https://media.giphy.com/media/jp8lWlBjGahPFAljBa/giphy.gif" alt="The future is now" /></p>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>My Worst Bug</title>
          <description>Debugging C/C++ can sometimes be a nightmare.</description>
          <guid>https://thebroken.link/my-worst-bug/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Fri, 17 Apr 2020 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/my-worst-bug/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p>Some bugs pull you to the abyss and leave you there. They’re the Balrog to our Gandalf. Once you beat them though, you’re wiser and more powerful!</p>

<p><img src="https://media.giphy.com/media/rBblrZDuvOaJi/giphy.gif" alt="Gandalf falling!" /></p>

<p>I was building a small plugin in C++ for an MT4 Server (ForEx trading server). The output of the project was a Windows DLL. Using the server’s protocol, I managed to get JSON strings and parse them with <a href="https://rapidjson.org/">RapidJSON</a>. Everything ran smoothly in my virtual machine and in some development servers. Even Valgrind couldn’t find memory leaks. I thought the plugin was ready for production…</p>

<p><strong>Oh boy was I so wrong!</strong></p>

<blockquote>
  <p><strong>Note</strong>: RapidJSON is an amazing library and if I need to parse JSON in C/C++ again, I would use it without hesitation.</p>
</blockquote>

<p><img src="https://media.giphy.com/media/EimNpKJpihLY4/giphy.gif" alt="Fail!" /></p>

<h2 id="debugging-the-problem">Debugging The Problem</h2>

<p>Once I deployed the plugin, everything seemed fine… until the next day! A nasty segmentation fault killed the server. The plugin made us loose some money and I had to roll back the deploy.</p>

<p>After several days of testing, I realized the production server always died with the same set of data. I was able to pinpoint the error to RapidJSON. Something weird was happening when the memory was allocated, but none of the tools I was using to debug this were reporting any problems.</p>

<p>I was desperate, so I compiled the DLL with debug symbols and then I de-compiled it using <a href="https://www.ollydbg.de/">OllyDBG</a>.</p>

<p>I started reading the DLL assembly code … <strong>for a week and a half</strong>! Reading assembly was horrible. I considered switching careers. But then I got to the instruction that failed! Eureka! I couldn’t believe it! It felt good to finally understand the bug!</p>

<p><img src="https://media.giphy.com/media/WR2W4OIee3YBQbIbID/giphy.gif" alt="Eureka!" /></p>

<h2 id="the-bug">The Bug!</h2>

<p>The problem was that RapidJSON’s custom allocator:</p>

<ul>
  <li>Compressed the data in memory.</li>
  <li>Allocated only what it needed.</li>
</ul>

<p>The production machine architecture:</p>

<ul>
  <li>Allocated the memory RapidJSON asked for.</li>
  <li>Ignored the way RapidJSON wanted the data to be structured.</li>
</ul>

<p>It’s easier to see with an image:</p>

<p>
  <figure class="flex flex-col items-center w-full h-auto mx-auto">
    <img src="/my-worst-bug/memory_allocation.png" alt="RapidJSON Custom Allocation Vs. What The Machine Actually Did" />
    <figcaption class="text-center">
      <p>RapidJSON Custom Allocation Vs. What The Machine Actually Did</p>

    </figcaption>
  </figure>
</p>

<p>If RapidJSON needed to store an integer, a string and a boolean value, then it could fail randomly depending on the length of the string. e.g. given the integer <code class="language-plaintext highlighter-rouge">42</code> and the boolean <code class="language-plaintext highlighter-rouge">true</code>:</p>

<ul>
  <li>For the string <code class="language-plaintext highlighter-rouge">Hey</code>, it would succeed:</li>
</ul>

<p>
  <figure class="flex flex-col items-center w-full h-auto mx-auto">
    <img src="/my-worst-bug/memory_allocation_success.png" alt="Memory allocation success." />
    <figcaption class="text-center">
      <p>Memory allocation success.</p>

    </figcaption>
  </figure>
</p>

<ul>
  <li>For the string <code class="language-plaintext highlighter-rouge">Hi</code>, it would fail:</li>
</ul>

<p>
  <figure class="flex flex-col items-center w-full h-auto mx-auto">
    <img src="/my-worst-bug/memory_allocation_fail.png" alt="Memory allocation fail." />
    <figcaption class="text-center">
      <p>Memory allocation fail.</p>

    </figcaption>
  </figure>
</p>

<p>A debugging nightmare!</p>

<h2 id="the-solution">The Solution</h2>

<p>I just made RapidJSON use the machine’s allocator instead of the custom one. Spent two weeks debugging something and changed just a single word in the code!</p>

<p><img src="https://media.giphy.com/media/10PDlC02A1L5Cw/giphy.gif" alt="I'm an idiot!" /></p>

<h2 id="conclusion">Conclusion</h2>

<p>Now I avoid C/C++ at all costs!</p>

<p><img src="https://media.giphy.com/media/3o7TKCEuECLAqDYEY8/giphy.gif" alt="Nightmare!" /></p>

<p>I hope whatever bug you’re dealing with at the moment gets solved soon!</p>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>My Road to 8 Week Streak</title>
          <description>Writing is hard. Writing requires practice.</description>
          <guid>https://thebroken.link/my-road-to-8-week-streak/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Fri, 10 Apr 2020 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/my-road-to-8-week-streak/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p>9 weeks ago, I challenged myself to consistently write an article every week for at least 8 weeks. Today, I can say I did it. It was hard. Not the hardest thing I’ve done, but hard nonetheless.</p>

<p>It wasn’t just about writing something and get done with it for the week. The internet is already filled with shallow useless content. I didn’t need to contribute to that. I wanted to write good content. Enjoyable and memorable content. Content that made me proud.</p>

<p>Writing is hard. A blank page is intimidating. Hitting the <em>publish</em> button and exposing your thoughts to the masses is scary. Quitting is easy, but unforgivable.</p>

<p>Nonetheless, I wrote.</p>

<p><img src="https://media.giphy.com/media/R6xi8dXsRhIjK/giphy.gif" alt="Me writing" /></p>

<h2 id="good-content">Good Content</h2>

<p>Beating the challenge wasn’t the goal. The challenge kept my eyes on the goal. The goal was to promote my personal brand and my open source projects. I chose <em>content marketing</em> as my tool to reach the goal.</p>

<p>Everything I know about content marketing I learnt from <a href="https://app.growthmentor.com/marta-olszewska">my girlfriend</a>. I’m not a marketing expert, but rule number one of content marketing is: <em>write good content</em>. The vaguest rule ever. So, what’s good content?</p>

<h3 id="know-your-audience">Know Your Audience</h3>

<p>It’s nice when someone shares your content. You reached them. Your content was valuable enough for them. They are your <em>target audience</em>.</p>

<p>I love Elixir. I feel comfortable sharing my knowledge about it. Most of my projects are written in Elixir. My target audience was anybody interested in Elixir. So… I mostly wrote about Elixir.</p>

<p><img src="https://media.giphy.com/media/3oD3YLLQBxZbBBYSWc/giphy.gif" alt="Your audience" /></p>

<h3 id="shareable-content">Shareable Content</h3>

<p>Writing for your target audience is not enough. Your content needs to be shareable. And the most important thing: it needs to be shareable by the right people.</p>

<p>The right people are the experts in the area you chose. They have read a gazillion times about list comprehensions in Elixir. They haven’t read about that time you implemented Prolog’s backtracking using list comprehension in Elixir.</p>

<p>Write about the cool stuff you have done. The things you’re proud of.</p>

<p><img src="https://media.giphy.com/media/PudZiAbQDUEik/giphy.gif" alt="Miyagi nodding" /></p>

<h3 id="enjoyable-content">Enjoyable Content</h3>

<p>In the era of short attention span, you’re worst enemy is sudden boredom. Use GIFs. Use images. Use graphics. Entertain while sharing knowledge.</p>

<p>Your article’s content is competing with a sudden WhatsApp message with a meme or that Prime Video notification saying The Expanse new season is available now.</p>

<p><img src="https://media.giphy.com/media/SggILpMXO7Xt6/giphy.gif" alt="Oh, look a dogo" /></p>

<h3 id="findable-content">Findable Content</h3>

<p>It doesn’t matter you’re the best technical writer in the world if nobody reads you. You need to distribute your content.</p>

<p>This greatly depends on the area you chose. In my case, Elixir’s community is small and getting into the newsletters is not that hard as long as your content is valuable.</p>

<p>Don’t wait for people to discover you. Reach out for them.</p>

<blockquote>
  <p><strong>Important</strong>: Don’t spam. Nobody likes spammers.</p>
</blockquote>

<p><img src="https://media.giphy.com/media/3o6ZsVObnL03crv4VG/giphy.gif" alt="Paper boy" /></p>

<h2 id="measuring-success">Measuring Success</h2>

<p>Thinking you’re writing good content is different than writing good content. I needed a way to measure my success. I tracked several metrics along the way:</p>

<ul>
  <li>Number of DEV views.</li>
  <li>Number of DEV reactions.</li>
  <li>Number of DEV followers.</li>
  <li>Number of Github stars.</li>
</ul>

<p>Additionally to those quantitative metrics, I also took into consideration whether the articles were published in a newsletter or not:</p>

<ul>
  <li><a href="https://elixirweekly.net/">Elixir Weekly</a></li>
  <li><a href="https://plataformatec.com.br/elixir-radar/">Elixir Radar</a></li>
</ul>

<p>I created a spreadsheet in Google Sheets and with <a href="https://gist.github.com/alexdesousa/a23552ed2995a865895f53a4fd844c6b">this script</a> I periodically gathered the data. This approach was inspired by/stolen from <a href="https://dev.to/pluralsight/pulling-your-dev-to-stats-into-a-google-sheet-56dh">this article</a>.</p>

<p>
  <figure class="flex flex-col items-center w-full h-auto mx-auto">
    <img src="/my-road-to-8-week-streak/spreadsheet.png" alt="My data" />
    <figcaption class="text-center">
      <p>Spreadsheet with my data</p>

    </figcaption>
  </figure>
</p>

<p>I wasn’t that rigorous with Twitter metrics, but I still kept them in mind when writing a tweet sharing my next article:</p>

<ul>
  <li>Number of Twitter likes.</li>
  <li>Number of Twitter retweets.</li>
  <li>Number of Twitter followers.</li>
</ul>

<p><img src="https://media.giphy.com/media/6ZnDM7tOjKTRe/giphy.gif" alt="Measuring" /></p>

<h2 id="results">Results</h2>

<p>My articles were well received by the community. I got useful feedback that has improved both my projects and blog posts. I even got great pull requests to some of my projects and that makes me happy. People not only find my articles, but also my projects, useful!</p>

<p><img src="https://media.giphy.com/media/rdma0nDFZMR32/giphy.gif" alt="Happy pigglet" /></p>

<p>The following is a performance summary for all blog posts and open source projects:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">Metric (Number of)</th>
      <th style="text-align: right">Before</th>
      <th style="text-align: right">Week 9</th>
      <th style="text-align: right">Change</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left">DEV views</td>
      <td style="text-align: right">0</td>
      <td style="text-align: right">3319</td>
      <td style="text-align: right">+3319</td>
    </tr>
    <tr>
      <td style="text-align: left">DEV reactions</td>
      <td style="text-align: right">0</td>
      <td style="text-align: right">125</td>
      <td style="text-align: right">+125</td>
    </tr>
    <tr>
      <td style="text-align: left">DEV followers</td>
      <td style="text-align: right">0</td>
      <td style="text-align: right">164</td>
      <td style="text-align: right">+164</td>
    </tr>
    <tr>
      <td style="text-align: left">Github stars</td>
      <td style="text-align: right">49</td>
      <td style="text-align: right">170</td>
      <td style="text-align: right">+121</td>
    </tr>
    <tr>
      <td style="text-align: left">Twitter followers</td>
      <td style="text-align: right">2</td>
      <td style="text-align: right">29</td>
      <td style="text-align: right">+27</td>
    </tr>
    <tr>
      <td style="text-align: left">Elixir Weekly publications</td>
      <td style="text-align: right">0</td>
      <td style="text-align: right">4</td>
      <td style="text-align: right">+4</td>
    </tr>
    <tr>
      <td style="text-align: left">Elixir Radar publications</td>
      <td style="text-align: right">0</td>
      <td style="text-align: right">1</td>
      <td style="text-align: right">+1</td>
    </tr>
  </tbody>
</table>

<p><img src="https://media.giphy.com/media/RgfGmnVvt8Pfy/giphy.gif" alt="That's good" /></p>

<h2 id="lessons">Lessons</h2>

<p>Consistently writing good content is hard. The following are some of the lessons learnt these past 8 weeks:</p>

<ul>
  <li><strong>Choose a publishing day</strong>: My days were Thursdays. I needed to hit the
<em>publish</em> button no matter what. Pressure is good.</li>
  <li><strong>Pick your article’s topic one week before you publish it</strong>: Some articles require experimentation and research. Good content requires time. Don’t rush it.</li>
  <li><strong>Pick a <em>backup topic</em></strong>: If your main article is not ready for publishing, then publish a good article about the <em>backup topic</em>.</li>
  <li><strong>Write in <em>beast mode</em></strong>: Write what’s on your mind. Don’t try to write a perfect article. Just write. Once your ideas are written, you can edit.</li>
  <li><strong>Distribute your content</strong>: Once you publish the article, distribute it in every relevant channel you have at your disposal.</li>
</ul>

<p><img src="https://media.giphy.com/media/I5yH9Aeq0EGCA/giphy.gif" alt="Climbing" /></p>

<h2 id="conclusion">Conclusion</h2>

<p>Writing is hard. Writing requires practice. The more you practice, the easier it gets. Just do it!</p>

<p><img src="https://media.giphy.com/media/qDPg6HNz2NfAk/giphy.gif" alt="Just do it!" /></p>

<p>Happy writing!</p>

<p><em>Cover image by <a href="https://unsplash.com/@aaronburden">Aaron Burden</a></em></p>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>AyeSQL: Writing Raw SQL in Elixir</title>
          <description>Sometimes good'ol SQL is better than any abstraction.</description>
          <guid>https://thebroken.link/ayesql-writing-raw-sql-in-elixir/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Tue, 31 Mar 2020 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/ayesql-writing-raw-sql-in-elixir/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p>Most developers consider that writing raw SQL (Structured Query Language) is a bad practice. The main arguments against it are that Object Relational Mappers (ORMs):</p>

<ul>
  <li>Abstract several SQL dialects.</li>
  <li>Have a flat learning curve unlike SQL.</li>
  <li>Optimize queries.</li>
</ul>

<p>ORMs allow you to abstract queries in an almost magical way:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp"># INSERT INTO user (name, age)
#   VALUES ('Bob', 18)
#   ON CONFLICT(name)
#   DO UPDATE SET name = EXCLUDED.name
#   RETURNING name, age
</span><span class="n">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="n">User</span><span class="p">(</span><span class="s">"Bob"</span><span class="p">,</span> <span class="mi">18</span><span class="p">)</span>

<span class="cp"># UPDATE user
#   SET age = 17
#   WHERE name = 'Bob'
</span><span class="n">user</span><span class="p">.</span><span class="n">age</span> <span class="o">=</span> <span class="mi">17</span>
</code></pre></div></div>

<p><img src="https://media.giphy.com/media/12NUbkX6p4xOO4/giphy.gif" alt="Magic!" /></p>

<h2 id="before-we-start">Before we start</h2>

<p>The contents of this article are a mix between my work experience and the great insights <a href="https://www.goodreads.com/book/show/36555832-mastering-postgresql-in-application-development">Mastering PostgreSQL in Application Development</a> book provides.</p>

<p>The code for this article can be found <a href="https://github.com/alexdesousa/alexdesousa.github.io/tree/blog/examples/matrix">here</a> and it can be downloaded with the following command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone <span class="se">\</span>
  <span class="nt">--depth</span> 2 <span class="se">\</span>
  <span class="nt">-b</span> blog <span class="se">\</span>
  https://github.com/alexdesousa/alexdesousa.github.io.git examples <span class="o">&amp;&amp;</span> <span class="se">\</span>
<span class="nb">cd </span>examples <span class="o">&amp;&amp;</span> <span class="se">\</span>
git filter-branch <span class="se">\</span>
  <span class="nt">--prune-empty</span> <span class="se">\</span>
  <span class="nt">--subdirectory-filter</span> examples/f1 HEAD
</code></pre></div></div>

<h2 id="my-orm-abstracts-several-sql-dialects">“My ORM abstracts several SQL dialects”</h2>

<p>In my experience, I’ve found that it’s more likely for an application to be re-written in another language e.g. Ruby to Elixir than actually migrating the data from one database to another e.g. MySQL to PostgreSQL.</p>

<p>Additionally, running the database migrations in a different database is not a walk in the park either. Complex applications end up adding custom SQL to their migrations in order to optimize or automate certain processes: database extensions, indexes and triggers.</p>

<p>And then, the data needs to be migrated e.g. <code class="language-plaintext highlighter-rouge">pgloader</code> is an amazing tool that migrates data from MySQL, SQLite or MSSQL to PostgreSQL. However, sometimes these tools don’t work correctly or they are nonexistent.</p>

<p><img src="https://media.giphy.com/media/26ybwvTX4DTkwst6U/giphy.gif" alt="At least you tried" /></p>

<h2 id="my-orm-has-a-flat-learning-curve-unlike-sql">“My ORM has a flat learning curve unlike SQL”</h2>

<p>SQL is different. It’s a functional language and most programmers are used to imperative languages. It’s also hard to master.</p>

<p>Ironically, the queries are almost in natural language e.g. if we show the following sentences to someone who doesn’t know SQL, probably they’ll know they have the same objective:</p>

<ul>
  <li><em>Get the <code class="language-plaintext highlighter-rouge">name</code> and <code class="language-plaintext highlighter-rouge">age</code> of every <code class="language-plaintext highlighter-rouge">client</code> older than or equal to <code class="language-plaintext highlighter-rouge">18</code></em>.</li>
  <li><code class="language-plaintext highlighter-rouge">SELECT name, age FROM client WHERE age ≥ 18</code></li>
</ul>

<p>ORMs specifically work great for simple queries. However, when you need a more complex query, you end up doing certain workarounds to bypass the ORMs limitations:</p>

<ul>
  <li>Writing raw SQL directly.</li>
  <li>Querying the database several times (sometimes unknowingly) to retrieve the data we need and doing the computation in our language instead of the database.</li>
</ul>

<p>For now I’ll focus on the second approach and I’ll talk about the first one later. It’s easier to see it with a small example:</p>

<p>Given the following subset of tables from the <a href="https://github.com/lerocha/chinook-database">Chinook database</a>:</p>

<p>
  <figure class="flex flex-col items-center w-full h-auto mx-auto">
    <img src="/ayesql-writing-raw-sql-in-elixir/music.png" alt="Chinook database subset" />
    <figcaption class="text-center">
      <p>Chinook database subset</p>

    </figcaption>
  </figure>
</p>

<p>We want to know the <em>duration of every album Pearl Jam has ever made</em> (or at least the ones listed in the database). In an OO language we would do something like:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Artist</span> <span class="n">artist</span> <span class="o">=</span> <span class="n">Artist</span><span class="p">.</span><span class="n">one</span><span class="p">(</span><span class="n">name</span><span class="o">:</span> <span class="s">"Pearl Jam"</span><span class="p">)</span>

<span class="k">for</span> <span class="n">album</span> <span class="n">in</span> <span class="n">Album</span><span class="p">.</span><span class="n">all</span><span class="p">(</span><span class="n">artistId</span><span class="o">:</span> <span class="n">artist</span><span class="p">.</span><span class="n">id</span><span class="p">)</span>
  <span class="n">ms</span> <span class="o">=</span> <span class="mi">0</span>

  <span class="k">for</span> <span class="n">track</span> <span class="n">in</span> <span class="n">Track</span><span class="p">.</span><span class="n">all</span><span class="p">(</span><span class="n">albumId</span><span class="o">:</span> <span class="n">album</span><span class="p">.</span><span class="n">id</span><span class="p">)</span>
    <span class="n">ms</span> <span class="o">+=</span> <span class="n">track</span><span class="p">.</span><span class="n">milliseconds</span>

  <span class="n">print</span><span class="p">(</span><span class="s">"#{album.title} | #{format_time(ms)}"</span><span class="p">)</span>
</code></pre></div></div>

<p>For a developer who doesn’t know SQL, this would seem like reasonable code. In reality, it’s highly inefficient:</p>

<ol>
  <li>
    <p>We’re requesting one artist by name:</p>

    <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="k">SELECT</span> <span class="o">*</span> <span class="k">FROM</span> <span class="nv">"Artist"</span> <span class="k">WHERE</span> <span class="nv">"Name"</span> <span class="o">=</span> <span class="s1">'Pearl Jam'</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>Then requesting all albums by artist id:</p>

    <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="k">SELECT</span> <span class="o">*</span> <span class="k">FROM</span> <span class="nv">"Album"</span> <span class="k">WHERE</span> <span class="nv">"ArtistId"</span> <span class="o">=</span> <span class="n">artist</span><span class="p">.</span><span class="n">id</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>For every album, we’re requesting the tracks by album id:</p>

    <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="k">SELECT</span> <span class="o">*</span> <span class="k">FROM</span> <span class="nv">"Track"</span> <span class="k">WHERE</span> <span class="nv">"AlbumId"</span> <span class="o">=</span> <span class="n">album</span><span class="p">.</span><span class="n">id</span>
</code></pre></div>    </div>
  </li>
</ol>

<p>This database contains 5 Pearl Jam albums, so we would be querying the database 7 times. The magic behind the ORM is deceiving us. In every round trip to the database, we need to consider two things:</p>

<ul>
  <li>Time the query takes to execute.</li>
  <li>Time the network transmits the data.</li>
</ul>

<p>Good ORMs introduce ways of dealing with this type of problem. However, knowing we need to use those tools in order to have good performance in our queries requires knowing SQL.</p>

<p>So, going back to the title of this section: Do ORMs really have a flat learning curve? Considering programmers need to know SQL to understand the tools ORMs offer and SQL is hard, then I guess they have not.</p>

<p><img src="https://media.giphy.com/media/1oJLpejP9jEvWQlZj4/giphy.gif" alt="Visible confusion" /></p>

<p>The previous problem can be solved with one query:</p>

<blockquote>
  <p><strong>Note:</strong> Chinook database has capitalized table and column names. That’s why they need to be between double quotes.</p>
</blockquote>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="k">SELECT</span> <span class="nv">"Album"</span><span class="p">.</span><span class="nv">"Title"</span> <span class="k">AS</span> <span class="n">album</span><span class="p">,</span>
           <span class="k">SUM</span><span class="p">(</span><span class="nv">"Track"</span><span class="p">.</span><span class="nv">"Milliseconds"</span><span class="p">)</span> <span class="o">*</span> <span class="n">interval</span> <span class="s1">'1 ms'</span> <span class="k">AS</span> <span class="n">duration</span>
      <span class="k">FROM</span> <span class="nv">"Album"</span>
<span class="k">INNER</span> <span class="k">JOIN</span> <span class="nv">"Artist"</span> <span class="k">USING</span><span class="p">(</span><span class="nv">"ArtistId"</span><span class="p">)</span>
 <span class="k">LEFT</span> <span class="k">JOIN</span> <span class="nv">"Track"</span> <span class="k">USING</span><span class="p">(</span><span class="nv">"AlbumId"</span><span class="p">)</span>
     <span class="k">WHERE</span> <span class="nv">"Artist"</span><span class="p">.</span><span class="nv">"Name"</span> <span class="o">=</span> <span class="s1">'Pearl Jam'</span>
  <span class="k">GROUP</span> <span class="k">BY</span> <span class="n">album</span>
  <span class="k">ORDER</span> <span class="k">BY</span> <span class="n">album</span>
</code></pre></div></div>

<p>and our result would be already formatted:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>          album          |   duration
-------------------------+-------------
 Live On Two Legs [Live] | 01:11:18.954
 Pearl Jam               | 00:49:43.857
 Riot Act                | 00:54:16.468
 Ten                     | 00:53:25.871
 Vs.                     | 00:46:17.674
</code></pre></div></div>

<h2 id="my-orm-optimizes-queries">“My ORM optimizes queries”</h2>

<p>Good ORMs do optimize queries. Usually they optimize queries for the most common cases. However, if the problem we’re solving is not covered by these optimizations, it can be a real head-scratcher.</p>

<p>In the end, we end up with a subpar solution or writing raw SQL to overcome the limitations.</p>

<p><img src="https://media.giphy.com/media/D1ETZoAPSt5EA/giphy.gif" alt="duct taping broken wall" /></p>

<h2 id="language-mappers">Language Mappers</h2>

<p>There are alternatives to ORMs. In Elixir, we have <a href="https://github.com/elixir-ecto/ecto">Ecto</a>. It’s not an ORM, but a language mapper. It gives you a Domain Specific Language (DSL) for dealing with SQL queries in Elixir e.g. again if we want to know the <em>duration of every album Pearl Jam has ever made</em> (or the 5 listed in this database), we would do the following:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">query</span> <span class="o">=</span>
  <span class="n">from</span> <span class="n">al</span> <span class="ow">in</span> <span class="no">Album</span><span class="p">,</span>
    <span class="ss">inner_join:</span> <span class="n">ar</span> <span class="ow">in</span> <span class="no">Artist</span><span class="p">,</span> <span class="ss">on:</span> <span class="n">al</span><span class="o">.</span><span class="n">artistId</span> <span class="o">==</span> <span class="n">ar</span><span class="o">.</span><span class="n">artistId</span><span class="p">,</span>
    <span class="ss">left_join:</span> <span class="n">tr</span> <span class="ow">in</span> <span class="no">Track</span><span class="p">,</span> <span class="ss">on:</span> <span class="n">tr</span><span class="o">.</span><span class="n">albumId</span> <span class="o">==</span> <span class="n">ar</span><span class="o">.</span><span class="n">albumId</span><span class="p">,</span>
    <span class="ss">where:</span> <span class="n">ar</span><span class="o">.</span><span class="n">name</span> <span class="o">==</span> <span class="s2">"Pearl Jam"</span>
    <span class="ss">group_by:</span> <span class="n">al</span><span class="o">.</span><span class="n">title</span>
    <span class="ss">order_by:</span> <span class="n">al</span><span class="o">.</span><span class="n">title</span>
    <span class="ss">select:</span> <span class="p">%{</span><span class="ss">album:</span> <span class="n">al</span><span class="o">.</span><span class="n">title</span><span class="p">,</span> <span class="ss">duration:</span> <span class="n">sum</span><span class="p">(</span><span class="n">tr</span><span class="o">.</span><span class="n">milliseconds</span><span class="p">)}</span>

<span class="no">Repo</span><span class="o">.</span><span class="n">all</span><span class="p">(</span><span class="n">query</span><span class="p">)</span>
</code></pre></div></div>

<p>Language mappers require developers to know SQL, but allows them to <em>almost</em> never leave the comfort of their language. That <em>“almost”</em> is important.</p>

<p><img src="https://media.giphy.com/media/RgfGmnVvt8Pfy/giphy.gif" alt="that's good" /></p>

<p>Though not everything is sunshine and rainbows. Language mappers try to abstract several SQL dialects in an unified DSL. This means that not all the SQL of our database is going to be included in this DSL.</p>

<p>To overcome this limitation, Ecto introduces the concept of <em>fragments</em>. Fragments are pieces of custom SQL code that can be added to our queries e.g. given the following subset from <a href="https://ergast.com/mrd/db/">Ergast Developer API</a> database:</p>

<p>
  <figure class="flex flex-col items-center w-full h-auto mx-auto">
    <img src="/ayesql-writing-raw-sql-in-elixir/f1.png" alt="Ergast Developer API database" />
    <figcaption class="text-center">
      <p>Ergast Developer API database</p>

    </figcaption>
  </figure>
</p>

<p>Let’s say we want to get the percentage of accidents per participant in F1 seasons between the years 1974 and 1990. In SQL, we would have the following:</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">WITH</span> <span class="n">accidents</span> <span class="k">AS</span>
<span class="p">(</span>
    <span class="k">SELECT</span> <span class="k">EXTRACT</span><span class="p">(</span><span class="nb">year</span> <span class="k">from</span> <span class="n">races</span><span class="p">.</span><span class="nb">date</span><span class="p">)</span> <span class="k">AS</span> <span class="n">season</span><span class="p">,</span>
           <span class="k">COUNT</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> <span class="k">AS</span> <span class="n">participants</span><span class="p">,</span>
           <span class="k">COUNT</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> <span class="n">FILTER</span><span class="p">(</span><span class="k">WHERE</span> <span class="n">status</span> <span class="o">=</span> <span class="s1">'Accident'</span><span class="p">)</span> <span class="k">AS</span> <span class="n">accidents</span>
      <span class="k">FROM</span> <span class="n">f1db</span><span class="p">.</span><span class="n">results</span>
      <span class="k">JOIN</span> <span class="n">f1db</span><span class="p">.</span><span class="n">status</span> <span class="k">USING</span><span class="p">(</span><span class="n">statusid</span><span class="p">)</span>
      <span class="k">JOIN</span> <span class="n">f1db</span><span class="p">.</span><span class="n">races</span> <span class="k">USING</span><span class="p">(</span><span class="n">raceid</span><span class="p">)</span>
  <span class="k">GROUP</span> <span class="k">BY</span> <span class="n">season</span>
<span class="p">)</span>
  <span class="k">SELECT</span> <span class="n">season</span><span class="p">,</span>
         <span class="n">ROUND</span><span class="p">(</span><span class="mi">100</span><span class="p">.</span><span class="mi">0</span> <span class="o">*</span> <span class="n">accidents</span> <span class="o">/</span> <span class="n">participants</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span> <span class="k">AS</span> <span class="n">percentage</span>
    <span class="k">FROM</span> <span class="n">accidents</span>
   <span class="k">WHERE</span> <span class="n">season</span> <span class="k">BETWEEN</span> <span class="mi">1974</span> <span class="k">AND</span> <span class="mi">1990</span>
<span class="k">ORDER</span> <span class="k">BY</span> <span class="n">season</span>
</code></pre></div></div>

<p>Now, when translating this query to Ecto, we’ll have to use fragments:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># NOTE: The schema definition is omitted.</span>

<span class="n">from</span> <span class="o">=</span> <span class="mi">1974</span>
<span class="n">to</span> <span class="o">=</span> <span class="mi">1990</span>

<span class="n">accidents</span> <span class="o">=</span>
  <span class="n">from</span> <span class="n">re</span> <span class="ow">in</span> <span class="no">Result</span><span class="p">,</span>
    <span class="ss">join:</span> <span class="n">st</span> <span class="ow">in</span> <span class="n">assoc</span><span class="p">(</span><span class="n">re</span><span class="p">,</span> <span class="ss">:status</span><span class="p">),</span>
    <span class="ss">join:</span> <span class="n">ra</span> <span class="ow">in</span> <span class="n">assoc</span><span class="p">(</span><span class="n">re</span><span class="p">,</span> <span class="ss">:race</span><span class="p">),</span>
    <span class="ss">group_by:</span> <span class="n">fragment</span><span class="p">(</span><span class="s2">"season"</span><span class="p">),</span>
    <span class="ss">select:</span> <span class="p">%{</span>
      <span class="ss">season:</span> <span class="n">fragment</span><span class="p">(</span><span class="s2">"EXTRACT(year from ?)"</span><span class="p">,</span> <span class="n">ra</span><span class="o">.</span><span class="n">date</span><span class="p">),</span>
      <span class="ss">participants:</span> <span class="n">fragment</span><span class="p">(</span><span class="s2">"COUNT(*)"</span><span class="p">),</span>
      <span class="ss">accidents:</span> <span class="n">fragment</span><span class="p">(</span><span class="s2">"COUNT(*) FILTER(WHERE status = 'Accident')"</span><span class="p">)</span>
    <span class="p">}</span>

<span class="n">query</span> <span class="o">=</span>
  <span class="n">from</span> <span class="n">a</span> <span class="ow">in</span> <span class="n">subquery</span><span class="p">(</span><span class="n">accidents</span><span class="p">),</span>
    <span class="ss">where:</span> <span class="n">fragment</span><span class="p">(</span><span class="s2">"? BETWEEN ? AND ?"</span><span class="p">,</span> <span class="n">a</span><span class="o">.</span><span class="n">season</span><span class="p">,</span> <span class="o">^</span><span class="n">from</span><span class="p">,</span> <span class="o">^</span><span class="n">to</span><span class="p">),</span>
    <span class="ss">select:</span> <span class="p">%{</span>
      <span class="ss">season:</span> <span class="n">a</span><span class="o">.</span><span class="n">season</span><span class="p">,</span>
      <span class="ss">percentage:</span> <span class="n">fragment</span><span class="p">(</span><span class="s2">"ROUND(100.0 * ? / ?, 2)"</span><span class="p">,</span> <span class="n">a</span><span class="o">.</span><span class="n">accidents</span><span class="p">,</span> <span class="n">a</span><span class="o">.</span><span class="n">participants</span><span class="p">)</span>
    <span class="p">}</span>

<span class="no">F1</span><span class="o">.</span><span class="no">Repo</span><span class="o">.</span><span class="n">all</span><span class="p">(</span><span class="n">query</span><span class="p">)</span>
</code></pre></div></div>

<p>The code above is hard to read, hard to write and error prone. Yet we don’t have any performance improvements in our query. A maintenance nightmare.</p>

<p>We could re-write the previous Ecto query differently by encapsulating them in Elixir macros and then importing our custom DSL e.g:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">CustomDSL</span> <span class="k">do</span>
  <span class="kn">import</span> <span class="no">Ecto</span><span class="o">.</span><span class="no">Query</span>

  <span class="k">defmacro</span> <span class="n">year</span><span class="p">(</span><span class="n">date</span><span class="p">)</span> <span class="k">do</span>
    <span class="kn">quote</span> <span class="k">do</span>
      <span class="n">fragment</span><span class="p">(</span><span class="s2">"EXTRACT(year from ?)"</span><span class="p">,</span> <span class="kn">unquote</span><span class="p">(</span><span class="n">date</span><span class="p">))</span>
    <span class="k">end</span>
  <span class="k">end</span>

  <span class="o">...</span>
<span class="k">end</span>
</code></pre></div></div>

<p>But again, we wouldn’t get any improvements performance-wise. Just readability… in Ecto. It was perfectly readable in SQL.</p>

<p>The consequences are clear. Developers need knowledge of:</p>

<ul>
  <li>The specific SQL dialect (in this case PostgreSQL dialect).</li>
  <li>Ecto’s API and its limitations.</li>
  <li>Elixir’s macros for fragment encapsulation.</li>
</ul>

<p>Additionally, now they need to maintain a new custom DSL API with its documentation. If we only have a few of this complex queries in our project, is it worthy?</p>

<p>What’s worse, after the refactor, we could end up with a subpar solution or wasting our time entirely.</p>

<h2 id="raw-sql">Raw SQL</h2>

<p>So far we’ve seen ORMs and language mappers are good for general problems we might encounter. However, some other problems are better left in raw SQL.</p>

<p>ORMs, language mappers and database adapters usually provide an API for running raw SQL. In Elixir, Ecto and <a href="https://github.com/elixir-ecto/postgrex">Postgrex</a> give us the function <code class="language-plaintext highlighter-rouge">query/2</code> e.g. in Ecto we would do the following:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">query</span> <span class="o">=</span>
  <span class="sd">"""
  WITH accidents AS
  (
      SELECT EXTRACT(year from races.date) AS season,
             COUNT(*) AS participants,
             COUNT(*) FILTER(WHERE status = 'Accident') AS accidents
        FROM results
        JOIN status USING(statusid)
        JOIN races USING(raceid)
    GROUP BY season
  )
    SELECT season,
           ROUND(100.0 * accidents / participants, 2) AS percentage
      FROM accidents
     WHERE season BETWEEN $1 AND $2
  ORDER BY season
  """</span>

<span class="no">Ecto</span><span class="o">.</span><span class="no">Adapters</span><span class="o">.</span><span class="no">SQL</span><span class="o">.</span><span class="n">query</span><span class="p">(</span><span class="no">F1</span><span class="o">.</span><span class="no">Repo</span><span class="p">,</span> <span class="n">query</span><span class="p">,</span> <span class="p">[</span><span class="mi">1974</span><span class="p">,</span> <span class="mi">1990</span><span class="p">])</span>
</code></pre></div></div>

<p>There are some things I’d like to point out from the previous code:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">$1</code> and <code class="language-plaintext highlighter-rouge">$2</code> are the query parameters: The numbers indicate the position in the parameter list. If we add status as a variable instead of the constant <code class="language-plaintext highlighter-rouge">'Accidents'</code>, we would need to update the other indexes.</li>
  <li>The query is a string: usually editors wouldn’t highlight the SQL syntax inside the string.</li>
  <li>
    <p>The <code class="language-plaintext highlighter-rouge">columns</code> and <code class="language-plaintext highlighter-rouge">rows</code> are separated in the result:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span>
  <span class="p">%</span><span class="no">Postgrex</span><span class="o">.</span><span class="no">Result</span><span class="p">{</span>
    <span class="ss">columns:</span> <span class="p">[</span><span class="s2">"season"</span><span class="p">,</span> <span class="s2">"percentage"</span><span class="p">],</span>
    <span class="ss">command:</span> <span class="ss">:select</span><span class="p">,</span>
    <span class="ss">connection_id:</span> <span class="mi">206</span><span class="p">,</span>
    <span class="ss">messages:</span> <span class="p">[],</span>
    <span class="ss">num_rows:</span> <span class="mi">17</span><span class="p">,</span>
    <span class="ss">rows:</span> <span class="p">[</span>
      <span class="p">[</span><span class="mf">1974.0</span><span class="p">,</span> <span class="c1">#Decimal&lt;3.67&gt;],</span>
      <span class="p">[</span><span class="mf">1975.0</span><span class="p">,</span> <span class="c1">#Decimal&lt;14.88&gt;],</span>
      <span class="p">[</span><span class="mf">1976.0</span><span class="p">,</span> <span class="c1">#Decimal&lt;11.06&gt;],</span>
      <span class="p">[</span><span class="mf">1977.0</span><span class="p">,</span> <span class="c1">#Decimal&lt;12.58&gt;],</span>
      <span class="p">[</span><span class="mf">1978.0</span><span class="p">,</span> <span class="c1">#Decimal&lt;10.19&gt;],</span>
      <span class="o">...</span>
 <span class="p">}}</span>
</code></pre></div>    </div>
  </li>
</ul>

<p>The complexity it’s still there.</p>

<h2 id="meet-ayesql">Meet AyeSQL</h2>

<p>Inspired by Clojure library <a href="https://github.com/krisajenkins/yesql">Yesql</a>, <a href="https://github.com/alexdesousa/ayesql">AyeSQL</a> tries to find a middle ground between raw SQL strings and SQL language mappers by:</p>

<ul>
  <li>Keeping SQL in SQL files.</li>
  <li>Generating Elixir functions for every query.</li>
  <li>Working out of the box with PostgreSQL using Ecto or Postgrex.</li>
  <li>Being extendable to support other databases and outputs.</li>
  <li>Allowing some query composability.</li>
</ul>

<p>Defining the same query in AyeSQL, we would need the following:</p>

<ul>
  <li>
    <p>A file for defining our query in raw SQL:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="o">--</span> <span class="ss">file:</span> <span class="n">lib</span><span class="o">/</span><span class="n">f1</span><span class="o">/</span><span class="n">query</span><span class="o">/</span><span class="n">season</span><span class="o">.</span><span class="n">sql</span>

 <span class="o">--</span> <span class="ss">name:</span> <span class="n">get_accidents</span>
 <span class="o">--</span> <span class="ss">docs:</span> <span class="no">Gets</span> <span class="n">accidents</span> <span class="ow">in</span> <span class="n">a</span> <span class="n">season</span> <span class="n">range</span><span class="o">.</span>
 <span class="no">WITH</span> <span class="n">accidents</span> <span class="no">AS</span>
 <span class="p">(</span>
     <span class="no">SELECT</span> <span class="no">EXTRACT</span><span class="p">(</span><span class="n">year</span> <span class="n">from</span> <span class="n">races</span><span class="o">.</span><span class="n">date</span><span class="p">)</span> <span class="no">AS</span> <span class="n">season</span><span class="p">,</span>
            <span class="no">COUNT</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> <span class="no">AS</span> <span class="n">participants</span><span class="p">,</span>
            <span class="no">COUNT</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> <span class="no">FILTER</span><span class="p">(</span><span class="no">WHERE</span> <span class="n">status</span> <span class="o">=</span> <span class="s1">'Accident'</span><span class="p">)</span> <span class="no">AS</span> <span class="n">accidents</span>
       <span class="no">FROM</span> <span class="n">f1db</span><span class="o">.</span><span class="n">results</span> <span class="no">AS</span> <span class="n">results</span>
       <span class="no">JOIN</span> <span class="n">f1db</span><span class="o">.</span><span class="n">status</span> <span class="no">AS</span> <span class="n">status</span> <span class="no">USING</span><span class="p">(</span><span class="n">statusid</span><span class="p">)</span>
       <span class="no">JOIN</span> <span class="n">f1db</span><span class="o">.</span><span class="n">races</span> <span class="no">AS</span> <span class="n">races</span> <span class="no">USING</span><span class="p">(</span><span class="n">raceid</span><span class="p">)</span>
   <span class="no">GROUP</span> <span class="no">BY</span> <span class="n">season</span>
 <span class="p">)</span>
   <span class="no">SELECT</span> <span class="n">season</span><span class="p">,</span>
          <span class="no">ROUND</span><span class="p">(</span><span class="mf">100.0</span> <span class="o">*</span> <span class="n">accidents</span> <span class="o">/</span> <span class="n">participants</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span> <span class="no">AS</span> <span class="n">percentage</span>
     <span class="no">FROM</span> <span class="n">accidents</span>
    <span class="no">WHERE</span> <span class="n">season</span> <span class="no">BETWEEN</span> <span class="ss">:from</span> <span class="no">AND</span> <span class="ss">:to</span>
 <span class="no">ORDER</span> <span class="no">BY</span> <span class="n">season</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>A file for declaring our queries as Elixir functions:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="c1"># file: lib/f1/queries.ex</span>

 <span class="kn">import</span> <span class="no">AyeSQL</span><span class="p">,</span> <span class="ss">only:</span> <span class="p">[</span><span class="ss">defqueries:</span> <span class="mi">3</span><span class="p">]</span>

 <span class="n">defqueries</span><span class="p">(</span><span class="no">F1</span><span class="o">.</span><span class="no">Query</span><span class="o">.</span><span class="no">Season</span><span class="p">,</span> <span class="s2">"query/season.sql"</span><span class="p">,</span> <span class="ss">repo:</span> <span class="no">F1</span><span class="o">.</span><span class="no">Repo</span><span class="p">)</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>Modify our configuration to run the queries by default:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="c1"># file: config/config.exs</span>

 <span class="kn">import</span> <span class="no">Config</span>

 <span class="n">config</span> <span class="ss">:ayesql</span><span class="p">,</span> <span class="ss">run?:</span> <span class="no">true</span>
</code></pre></div>    </div>
  </li>
</ul>

<p>Then we can call our query as follows:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="o">&gt;</span> <span class="no">F1</span><span class="o">.</span><span class="no">Query</span><span class="o">.</span><span class="no">Season</span><span class="o">.</span><span class="n">get_accidents</span><span class="p">(</span><span class="ss">from:</span> <span class="mi">1974</span><span class="p">,</span> <span class="ss">to:</span> <span class="mi">1990</span><span class="p">)</span>
<span class="p">{</span><span class="ss">:ok</span><span class="p">,</span>
 <span class="p">[</span>
   <span class="p">%{</span><span class="ss">percentage:</span> <span class="c1">#Decimal&lt;3.67&gt;, season: 1974.0},</span>
   <span class="p">%{</span><span class="ss">percentage:</span> <span class="c1">#Decimal&lt;14.88&gt;, season: 1975.0},</span>
   <span class="p">%{</span><span class="ss">percentage:</span> <span class="c1">#Decimal&lt;11.06&gt;, season: 1976.0},</span>
   <span class="p">%{</span><span class="ss">percentage:</span> <span class="c1">#Decimal&lt;12.58&gt;, season: 1977.0},</span>
   <span class="p">%{</span><span class="ss">percentage:</span> <span class="c1">#Decimal&lt;10.19&gt;, season: 1978.0},</span>
   <span class="o">...</span>
 <span class="p">]}</span>
</code></pre></div></div>

<p>I don’t know about you, but IMHO this seems like a maintainable way of dealing with complex SQL queries.</p>

<p><img src="https://media.giphy.com/media/cdGQHR4Qzefx6/giphy.gif" alt="easy" /></p>

<p>AyeSQL provides query runners for Ecto and Postgrex out-of-the-box. In the previous example we used Ecto’s runner. If we need a different output or query a different database e.g. MSSQL, we could implement the behaviour <code class="language-plaintext highlighter-rouge">AyeSQL.Runner</code> for our use case.</p>

<blockquote>
  <p><strong>Note</strong>: In the past year, my team and I had to write an Elixir application with a pre-existent MSSQL database. <code class="language-plaintext highlighter-rouge">AyeSQL.Runner</code> behaviour and <a href="https://github.com/livehelpnow/tds">TDS</a> library allowed us to query the database with ease.</p>
</blockquote>

<h2 id="conclusion">Conclusion</h2>

<p>I’m hoping this article helps change the negative reputation raw SQL queries have.</p>

<p>Yesql inspired libraries like AyeSQL, can help you to be more productive when your ORM or language mapper have failed you.</p>

<p><img src="https://media.giphy.com/media/fdhZs6PUqxSQU/giphy.gif" alt="There is another" /></p>

<p>I’ll write more about AyeSQL features in future articles, but for now happy coding!</p>

<p><em>Cover image by <a href="https://unsplash.com/@arstyy">Austin Neill</a></em></p>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>Elixir Pubsub In Less Than 50 Lines</title>
          <description>A small Elixir pubsub implementation using built-in module :pg2</description>
          <guid>https://thebroken.link/elixir-pubsub-in-less-than-50-lines/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Thu, 26 Mar 2020 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/elixir-pubsub-in-less-than-50-lines/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p><code class="language-plaintext highlighter-rouge">:pg2</code> is a mostly unknown, but powerful Erlang module. It provides an API for creating process groups.</p>

<h2 id="process-group">Process Group</h2>

<p>So, what’s a process group? Well… it’s a group of Erlang/Elixir processes.</p>

<p>Perhaps, the correct question would be, why do we care about process groups? Well, process groups are the foundation for publisher-subscribers (pubsubs for short).</p>

<h2 id="pg2">PG2</h2>

<p>Understanding <code class="language-plaintext highlighter-rouge">:pg2</code> API and how it relates to a pubsub API will make it easier to understand:</p>

<ul>
  <li>
    <p>Every process group is a <code class="language-plaintext highlighter-rouge">channel</code> e.g. a group called <code class="language-plaintext highlighter-rouge">:my_channel</code> is created:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">iex</span><span class="o">&gt;</span> <span class="ss">:pg2</span><span class="o">.</span><span class="n">create</span><span class="p">(</span><span class="ss">:my_channel</span><span class="p">)</span>
 <span class="ss">:ok</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>Every process in a group is a <code class="language-plaintext highlighter-rouge">subscriber</code> e.g. <code class="language-plaintext highlighter-rouge">self()</code> is part of <code class="language-plaintext highlighter-rouge">:my_channel</code> group:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">iex</span><span class="o">&gt;</span> <span class="ss">:pg2</span><span class="o">.</span><span class="n">join</span><span class="p">(</span><span class="ss">:my_channel</span><span class="p">,</span> <span class="n">self</span><span class="p">())</span>
 <span class="ss">:ok</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>A <code class="language-plaintext highlighter-rouge">publisher</code> can <code class="language-plaintext highlighter-rouge">send/2</code> messages to a <code class="language-plaintext highlighter-rouge">channel</code> e.g. the publisher gets all the members of the group <code class="language-plaintext highlighter-rouge">:my_channel</code> and sends <code class="language-plaintext highlighter-rouge">"Some message"</code>:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">iex</span><span class="o">&gt;</span> <span class="n">members</span> <span class="o">=</span> <span class="ss">:pg2</span><span class="o">.</span><span class="n">get_members</span><span class="p">(</span><span class="ss">:my_channel</span><span class="p">)</span>
 <span class="ss">:ok</span>
 <span class="n">iex</span><span class="o">&gt;</span> <span class="n">for</span> <span class="n">member</span> <span class="o">&lt;-</span> <span class="n">members</span><span class="p">,</span> <span class="k">do</span><span class="p">:</span> <span class="n">send</span><span class="p">(</span><span class="n">member</span><span class="p">,</span> <span class="s2">"Some message"</span><span class="p">)</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>A <code class="language-plaintext highlighter-rouge">subscriber</code> will receive the messages in its mailbox:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">iex</span><span class="o">&gt;</span> <span class="n">flush</span><span class="p">()</span>
 <span class="s2">"Some message"</span>
 <span class="ss">:ok</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>A <code class="language-plaintext highlighter-rouge">subscriber</code> can unsubscribe from a <code class="language-plaintext highlighter-rouge">channel</code> e.g. <code class="language-plaintext highlighter-rouge">self()</code> leaves the group <code class="language-plaintext highlighter-rouge">:my_channel</code>:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">iex</span><span class="o">&gt;</span> <span class="ss">:pg2</span><span class="o">.</span><span class="n">leave</span><span class="p">(</span><span class="ss">:my_channel</span><span class="p">,</span> <span class="n">self</span><span class="p">())</span>
 <span class="ss">:ok</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>A <code class="language-plaintext highlighter-rouge">channel</code> can be deleted:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">iex</span><span class="o">&gt;</span> <span class="ss">:pg2</span><span class="o">.</span><span class="n">delete</span><span class="p">(</span><span class="ss">:my_channel</span><span class="p">)</span>
 <span class="ss">:ok</span>
</code></pre></div>    </div>
  </li>
</ul>

<p>And that’s it! That’s the API. And you know what’s the best thing about it? <strong>It can work between connected nodes</strong>. Keep reading and you’ll see :)</p>

<p><img src="https://media.giphy.com/media/mcueTtCHvqNPy/giphy.gif" alt="Message in cereal" /></p>

<h2 id="implementing-a-pubsub">Implementing a PubSub</h2>

<p>A <code class="language-plaintext highlighter-rouge">PubSub</code> has three main functions:</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">subscribe/1</code> for subscribing to a <code class="language-plaintext highlighter-rouge">channel</code>:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="k">def</span> <span class="n">subscribe</span><span class="p">(</span><span class="n">channel</span><span class="p">)</span> <span class="k">do</span>
   <span class="n">pid</span> <span class="o">=</span> <span class="n">self</span><span class="p">()</span>

   <span class="k">case</span> <span class="ss">:pg2</span><span class="o">.</span><span class="n">get_members</span><span class="p">(</span><span class="n">channel</span><span class="p">)</span> <span class="k">do</span>
     <span class="n">members</span> <span class="ow">when</span> <span class="n">is_list</span><span class="p">(</span><span class="n">members</span><span class="p">)</span> <span class="o">-&gt;</span>
       <span class="k">if</span> <span class="n">pid</span> <span class="ow">in</span> <span class="n">members</span> <span class="k">do</span>
         <span class="ss">:ok</span>                     <span class="c1"># It's already subscribed.</span>
       <span class="k">else</span>
         <span class="ss">:pg2</span><span class="o">.</span><span class="n">join</span><span class="p">(</span><span class="n">channel</span><span class="p">,</span> <span class="n">pid</span><span class="p">)</span> <span class="c1"># Subscribes to channel</span>
       <span class="k">end</span>

     <span class="p">{</span><span class="ss">:error</span><span class="p">,</span> <span class="p">{</span><span class="ss">:no_such_group</span><span class="p">,</span> <span class="o">^</span><span class="n">channel</span><span class="p">}}</span> <span class="o">-&gt;</span>
       <span class="ss">:pg2</span><span class="o">.</span><span class="n">create</span><span class="p">(</span><span class="n">channel</span><span class="p">)</span>      <span class="c1"># Creates channel</span>
       <span class="ss">:pg2</span><span class="o">.</span><span class="n">join</span><span class="p">(</span><span class="n">channel</span><span class="p">,</span> <span class="n">pid</span><span class="p">)</span>   <span class="c1"># Subscribe to channel</span>
   <span class="k">end</span>
 <span class="k">end</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">unsubscribe/1</code> for unsubscribing from a <code class="language-plaintext highlighter-rouge">channel</code>.</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="k">def</span> <span class="n">unsubscribe</span><span class="p">(</span><span class="n">channel</span><span class="p">)</span> <span class="k">do</span>
    <span class="n">pid</span> <span class="o">=</span> <span class="n">self</span><span class="p">()</span>

    <span class="k">case</span> <span class="ss">:pg2</span><span class="o">.</span><span class="n">get_members</span><span class="p">(</span><span class="n">channel</span><span class="p">)</span> <span class="k">do</span>
      <span class="p">[</span><span class="o">^</span><span class="n">pid</span><span class="p">]</span> <span class="o">-&gt;</span>
        <span class="ss">:pg2</span><span class="o">.</span><span class="n">leave</span><span class="p">(</span><span class="n">channel</span><span class="p">,</span> <span class="n">pid</span><span class="p">)</span>   <span class="c1"># Unsubscribes from channel</span>
        <span class="ss">:pg2</span><span class="o">.</span><span class="n">delete</span><span class="p">(</span><span class="n">channel</span><span class="p">)</span>       <span class="c1"># Deletes the channel</span>

      <span class="n">members</span> <span class="ow">when</span> <span class="n">is_list</span><span class="p">(</span><span class="n">members</span><span class="p">)</span> <span class="o">-&gt;</span>
        <span class="k">if</span> <span class="n">pid</span> <span class="ow">in</span> <span class="n">members</span> <span class="k">do</span>
          <span class="ss">:pg2</span><span class="o">.</span><span class="n">leave</span><span class="p">(</span><span class="n">channel</span><span class="p">,</span> <span class="n">pid</span><span class="p">)</span> <span class="c1"># Unsubscribes from channel</span>
        <span class="k">else</span>
          <span class="ss">:ok</span>                      <span class="c1"># It's already unsubscribed</span>
        <span class="k">end</span>

      <span class="n">_</span> <span class="o">-&gt;</span>
        <span class="ss">:ok</span>
    <span class="k">end</span>
  <span class="k">end</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">publish/2</code> for sending a <code class="language-plaintext highlighter-rouge">message</code> to a <code class="language-plaintext highlighter-rouge">channel</code>.</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="k">def</span> <span class="n">publish</span><span class="p">(</span><span class="n">channel</span><span class="p">,</span> <span class="n">message</span><span class="p">)</span> <span class="k">do</span>
   <span class="k">case</span> <span class="ss">:pg2</span><span class="o">.</span><span class="n">get_members</span><span class="p">(</span><span class="n">channel</span><span class="p">)</span> <span class="k">do</span>
     <span class="p">[</span><span class="n">_</span> <span class="o">|</span> <span class="n">_</span><span class="p">]</span> <span class="o">=</span> <span class="n">members</span> <span class="o">-&gt;</span>
       <span class="n">for</span> <span class="n">member</span> <span class="o">&lt;-</span> <span class="n">members</span><span class="p">,</span> <span class="k">do</span><span class="p">:</span> <span class="n">send</span><span class="p">(</span><span class="n">member</span><span class="p">,</span> <span class="n">message</span><span class="p">)</span>
       <span class="ss">:ok</span>

     <span class="n">_</span> <span class="o">-&gt;</span>
       <span class="ss">:ok</span>
   <span class="k">end</span>
 <span class="k">end</span>
</code></pre></div>    </div>
  </li>
</ul>

<p>For a full implementation of <code class="language-plaintext highlighter-rouge">PubSub</code> you can check <a href="https://gist.github.com/alexdesousa/4d592fe206cca17393affaefa4c8fd33">this gist</a>.</p>

<p>I usually create a <code class="language-plaintext highlighter-rouge">.iex.exs</code> file in my <code class="language-plaintext highlighter-rouge">$HOME</code> folder and then run <code class="language-plaintext highlighter-rouge">iex</code>. You could do the same with the previous gist by doing the following:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~ <span class="nv">$ PUBSUB</span><span class="o">=</span><span class="s2">"https://gist.githubusercontent.com/alexdesousa/4d592fe206cca17393affaefa4c8fd33/raw/4d84894f016bd9eef84bba647c77c62b9c9a6094/pub_sub.ex"</span>
~ <span class="nv">$ </span>curl <span class="s2">"</span><span class="nv">$PUBSUB</span><span class="s2">"</span> <span class="nt">-o</span> .iex.exs
~ <span class="nv">$ </span>iex
</code></pre></div></div>

<p><img src="https://media.giphy.com/media/3o7btNa0RUYa5E7iiQ/giphy.gif" alt="It's that easy" /></p>

<h2 id="distributed-pubsub">Distributed PubSub</h2>

<p>For our distributed experiment we’ll need two nodes. My machine is called <code class="language-plaintext highlighter-rouge">matrix</code> and both nodes will be <code class="language-plaintext highlighter-rouge">neo</code> and <code class="language-plaintext highlighter-rouge">trinity</code> respectively:</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">:neo@matrix</code>:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> alex@matrix ~ <span class="nv">$ </span>iex <span class="nt">--sname</span> neo
 iex<span class="o">(</span>neo@matrix<span class="o">)</span>1&gt;
</code></pre></div>    </div>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">:trinity@matrix</code>:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> alex@matrix ~ <span class="nv">$ </span>iex <span class="nt">--sname</span> trinity
 iex<span class="o">(</span>trinity@matrix<span class="o">)</span>1&gt; Node.connect<span class="o">(</span>:neo@matrix<span class="o">)</span> <span class="c"># Connects both nodes</span>
</code></pre></div>    </div>
  </li>
</ul>

<p>Now <code class="language-plaintext highlighter-rouge">:neo@matrix</code> can subscribe to <code class="language-plaintext highlighter-rouge">:mainframe</code> channel:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="n">neo</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">1</span><span class="o">&gt;</span> <span class="no">PubSub</span><span class="o">.</span><span class="n">subscribe</span><span class="p">(</span><span class="ss">:mainframe</span><span class="p">)</span>
<span class="ss">:ok</span>
</code></pre></div></div>

<p>And <code class="language-plaintext highlighter-rouge">:trinity@matrix</code> can send a message:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="n">trinity</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">2</span><span class="o">&gt;</span> <span class="no">PubSub</span><span class="o">.</span><span class="n">publish</span><span class="p">(</span><span class="ss">:mainframe</span><span class="p">,</span> <span class="s2">"Wake up, Neo..."</span><span class="p">)</span>
<span class="ss">:ok</span> 
</code></pre></div></div>

<blockquote>
  <p><strong>Note</strong>: Sometimes it takes a bit of time for nodes to synchronize their process groups, so you might need to <code class="language-plaintext highlighter-rouge">publish/2</code> your message twice.</p>
</blockquote>

<p>Finally, <code class="language-plaintext highlighter-rouge">:neo@matrix</code> should receive the message:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="n">neo</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">2</span><span class="o">&gt;</span> <span class="n">flush</span><span class="p">()</span>
<span class="s2">"Wake up, Neo..."</span>
<span class="ss">:ok</span>
</code></pre></div></div>

<p>And that’s it. A powerful pubsub in a few lines of code thanks to <code class="language-plaintext highlighter-rouge">:pg2</code>.</p>

<p><img src="https://media.giphy.com/media/S27iRp6ypEcnK/giphy.gif" alt="Follow the white rabbit" /></p>

<h2 id="conclusion">Conclusion</h2>

<p>Erlang has several built-in hidden gems like <code class="language-plaintext highlighter-rouge">:pg2</code> that make our lives easier.</p>

<p><img src="https://media.giphy.com/media/PdfNwG98g6Sxq/giphy.gif" alt="gem" /></p>

<p>Happy coding!</p>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>Managing Dotfiles with Ansible</title>
          <description>A small tutorial on how to use Ansible to manage your dotfiles.</description>
          <guid>https://thebroken.link/managing-dotfiles-with-ansible/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Thu, 19 Mar 2020 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/managing-dotfiles-with-ansible/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p>When developers get tired of configuring again and again our machines, we tend to create a dotfiles repository.</p>

<h2 id="a-dotfiles-repository">A Dotfiles Repository</h2>

<blockquote>
  <p><strong>Dotfiles</strong> are commonly used for storing user preferences or preserving the state of a utility, and are frequently created implicitly by using various utilities.</p>
</blockquote>

<p>It all starts with a small repository that contains configuration files for common tools e.g. <code class="language-plaintext highlighter-rouge">.zshrc</code>, <code class="language-plaintext highlighter-rouge">.vimrc</code>, etc. However, every time we configure a new machine, we need to copy those files <em>by hand</em>.</p>

<p>We pride ourselves with our ability to automate any task. Therefore, the next logical step for a dotfiles repository is to create a “small” shell script for automating tool installation and machine configuration.</p>

<p>All is well at first, but that “small” script ends up being hundreds of lines long and hard to maintain.</p>

<p><img src="https://media.giphy.com/media/S0KRynVEROiOs/giphy.gif" alt="This is madness" /></p>

<h2 id="ansible-to-the-rescue">Ansible to the Rescue</h2>

<blockquote>
  <p><a href="https://www.ansible.com/">Ansible</a> is an configuration management tool. It provides its own language to describe system configuration.</p>
</blockquote>

<p>This is not a new idea, but it feels like one: configure your own machine the same way you configure your servers… With Ansible.</p>

<blockquote>
  <p><strong>Note</strong>: I’m using Debian throughout this tutorial, though the same can be accomplished with any architecture and operative system as long as Ansible is available for it.</p>
</blockquote>

<p><img src="https://media.giphy.com/media/F1YaFvtJ7VlwA/giphy.gif" alt="Where's Ansible's supersuit?" /></p>

<h3 id="bootstrapping-ansible">Bootstrapping Ansible</h3>

<p>For installing and configuring stuff with Ansible, we need to first install Ansible. We can use a small shell script to accomplish this. Some of the variables like <code class="language-plaintext highlighter-rouge">$HOSTS</code> and <code class="language-plaintext highlighter-rouge">$PLAYBOOK</code> will make sense in the next sections:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/usr/bin/env bash</span>

<span class="nb">set</span> <span class="nt">-e</span>

<span class="c"># Dotfiles' project root directory</span>
<span class="nv">ROOTDIR</span><span class="o">=</span><span class="s2">"</span><span class="si">$(</span><span class="nb">cd</span> <span class="s2">"</span><span class="si">$(</span><span class="nb">dirname</span> <span class="s2">"</span><span class="k">${</span><span class="nv">BASH_SOURCE</span><span class="p">[0]</span><span class="k">}</span><span class="s2">"</span><span class="si">)</span><span class="s2">"</span> <span class="o">&amp;&amp;</span> <span class="nb">pwd</span><span class="si">)</span><span class="s2">"</span>
<span class="c"># Host file location</span>
<span class="nv">HOSTS</span><span class="o">=</span><span class="s2">"</span><span class="nv">$ROOTDIR</span><span class="s2">/hosts"</span>
<span class="c"># Main playbook</span>
<span class="nv">PLAYBOOK</span><span class="o">=</span><span class="s2">"</span><span class="nv">$ROOTDIR</span><span class="s2">/dotfiles.yml"</span>

<span class="c"># Installs ansible</span>
apt-get update <span class="o">&amp;&amp;</span> apt-get <span class="nb">install</span> <span class="nt">-y</span> ansible

<span class="c"># Runs Ansible playbook using our user.</span>
ansible-playbook <span class="nt">-i</span> <span class="s2">"</span><span class="nv">$HOSTS</span><span class="s2">"</span> <span class="s2">"</span><span class="nv">$PLAYBOOK</span><span class="s2">"</span> <span class="nt">--ask-become-pass</span>

<span class="nb">exit </span>0
</code></pre></div></div>

<p>Running the previous script as our sudoer user will effectively install Ansible and run our main <em>playbook</em> with all our <em>roles</em>.</p>

<blockquote>
  <p><strong>Playbook</strong>: A file that defines several tasks to be executed in a target machine.
<strong>Role</strong>: Organizes multiple, related tasks with the data needed to run those tasks (variables, files, templates).</p>
</blockquote>

<h3 id="basic-folder-structure">Basic Folder Structure</h3>

<p>Once we have our bootstrap script in place, we can start writing Ansible configuration files.</p>

<p>In our dotfiles repository, our target is our own machine. This is very easy to define in our <code class="language-plaintext highlighter-rouge">hosts</code> file (with local connection so it doesn’t require an ssh key):</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># file: hosts</span>
<span class="pi">[</span><span class="nv">local</span><span class="pi">]</span>
<span class="s">localhost</span>

<span class="pi">[</span><span class="nv">local</span><span class="pi">:</span><span class="nv">vars</span><span class="pi">]</span>
<span class="s">ansible_connection=local</span>
</code></pre></div></div>

<blockquote>
  <p><strong>Note</strong>: In our <code class="language-plaintext highlighter-rouge">bootstrap.sh</code> script, this file is found in the variable <code class="language-plaintext highlighter-rouge">$HOSTS</code>.</p>
</blockquote>

<p>Then we need a <em>playbook</em> that lists every <em>role</em> we want to deploy and configure in our machine.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># file: dotfiles.yml</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Set up local workstation</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">local</span>
  <span class="na">roles</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">role</span><span class="pi">:</span> <span class="s">zsh</span>
      <span class="na">tags</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="s">zsh</span>
</code></pre></div></div>

<p>In our example, we’ll install <code class="language-plaintext highlighter-rouge">zsh</code> role.</p>

<blockquote>
  <p><strong>Note</strong>: In our <code class="language-plaintext highlighter-rouge">bootstrap.sh</code> script, this files is found in the variable <code class="language-plaintext highlighter-rouge">$PLAYBOOK</code>.</p>
</blockquote>

<p>In general, our folder structure would look something like:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>└ dotfiles
  - bootstrap.sh
  - dotfiles.yml
  - hosts
  └ roles
    └ zsh
      └ files
        - zshrc.link
      └ tasks
        - main.yml
</code></pre></div></div>

<h3 id="zsh-role">ZSh role</h3>

<p>The following would be our <code class="language-plaintext highlighter-rouge">zsh</code> role tasks for:</p>

<ul>
  <li>Installing <code class="language-plaintext highlighter-rouge">zsh</code>.</li>
  <li>Setting it up as our default shell.</li>
  <li>Installing Oh-My-ZSH.</li>
  <li>Linking our <code class="language-plaintext highlighter-rouge">.zshrc</code> configuration file to our home folder.</li>
</ul>

<figure class="highlight"><pre><code class="language-yaml" data-lang="yaml"><span class="nn">---</span>
<span class="c1"># file: roles/zsh/tasks/main.yml</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Installs zsh | apt</span>
  <span class="na">become</span><span class="pi">:</span> <span class="s">yes</span>
  <span class="na">become_user</span><span class="pi">:</span> <span class="s">root</span>
  <span class="na">apt</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">zsh</span>
    <span class="na">state</span><span class="pi">:</span> <span class="s">present</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Installs curl | apt</span>
  <span class="na">become</span><span class="pi">:</span> <span class="s">yes</span>
  <span class="na">become_user</span><span class="pi">:</span> <span class="s">root</span>
  <span class="na">apt</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">curl</span>
    <span class="na">state</span><span class="pi">:</span> <span class="s">present</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Sets zsh as default shell for my user | command</span>
  <span class="na">become</span><span class="pi">:</span> <span class="s">yes</span>
  <span class="na">become_user</span><span class="pi">:</span> <span class="s">root</span>
  <span class="na">command</span><span class="pi">:</span> <span class="s">chsh -s /bin/zsh {{ lookup('env' 'USER') }}</span>
  <span class="na">register</span><span class="pi">:</span> <span class="s">zsh_for_user</span>
  <span class="na">failed_when</span><span class="pi">:</span> <span class="s">zsh_for_user.rc &gt;= </span><span class="m">1</span>
  <span class="na">changed_when</span><span class="pi">:</span> <span class="s">zsh_for_user.rc == </span><span class="m">0</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checks for oh-my-zsh installation | stat</span>
  <span class="na">stat</span><span class="pi">:</span>
    <span class="na">path</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">lookup('env',</span><span class="nv"> </span><span class="s">'HOME')</span><span class="nv"> </span><span class="s">}}/.oh-my-zsh"</span>
  <span class="na">register</span><span class="pi">:</span> <span class="s">oh_my_zsh_stat</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Installs oh-my-zsh | raw</span>
  <span class="na">raw</span><span class="pi">:</span> <span class="s1">'</span><span class="s">sh</span><span class="nv"> </span><span class="s">-c</span><span class="nv"> </span><span class="s">"$(curl</span><span class="nv"> </span><span class="s">-fsSL</span><span class="nv"> </span><span class="s">https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"'</span>
  <span class="na">when</span><span class="pi">:</span> <span class="s">not oh_my_zsh_stat.stat.exists</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Links .zshrc file | file</span>
  <span class="na">file</span><span class="pi">:</span>
    <span class="na">src</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">lookup('env',</span><span class="nv"> </span><span class="s">'ROOTDIR')</span><span class="nv"> </span><span class="s">}}/roles/zsh/files/zshrc.link"</span>
    <span class="na">dest</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">lookup('env',</span><span class="nv"> </span><span class="s">'HOME')</span><span class="nv"> </span><span class="s">}}/.zshrc"</span>
    <span class="na">state</span><span class="pi">:</span> <span class="s">link</span>
    <span class="na">force</span><span class="pi">:</span> <span class="s">yes</span></code></pre></figure>

<h3 id="running-our-bootstrap-script">Running Our Bootstrap Script</h3>

<p>Finally, we can run our script:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/dotfiles <span class="nv">$ </span><span class="nb">chmod</span> +x bootstrap.sh
~/dotfiles <span class="nv">$ </span><span class="nb">sudo </span>bootstrap.sh
</code></pre></div></div>

<p>This script will:</p>

<ul>
  <li>Install Ansible.</li>
  <li>Run our playbook, which means installing and configuring <code class="language-plaintext highlighter-rouge">zsh</code>.</li>
</ul>

<p><img src="https://media.giphy.com/media/3o84sq21TxDH6PyYms/giphy.gif" alt="Unlimited power" /></p>

<h3 id="conclusion">Conclusion</h3>

<p>There are more cool features you can use to customize your system using Ansible. You can check my <a href="https://github.com/alexdesousa/dotfiles">dotfiles</a> repository if you want to see a fully working example.</p>

<p>Additionally, if you want to know more about Ansible, you can check <a href="https://serversforhackers.com/c/an-ansible2-tutorial">this amazing tutorial</a>.</p>

<p>I hope you find this useful :)</p>

<p><img src="https://media.giphy.com/media/kdiLau77NE9Z8vxGSO/giphy.gif" alt="Installation" /></p>

<p>Happy coding!</p>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>Oath: Don't Loose Your Keys!</title>
          <description>A ZSH plugin for handling your keys.</description>
          <guid>https://thebroken.link/oath-dont-loose-your-keys/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Thu, 12 Mar 2020 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/oath-dont-loose-your-keys/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p>Recently, I needed to reset my phone. I spent some time backing up my pictures and documents. Everything was going great. But then, I hit a roadblock.</p>

<h2 id="the-problem">The problem</h2>

<p>One-time passwords have become very handy for logging into several sites from Twitter to Coinbase. This passwords are 6 digit tokens generated using the current time and a private key. I was using Google Authenticator for getting my one-time passwords. Sadly, this app does not provide a way to backup the private keys.</p>

<p>The damage was done. I couldn’t retrieve the private keys, so I needed to regenerate all of them in every site individually. I thought about my future self dealing with the same issue and I knew I needed a sustainable solution.</p>

<p><img src="https://media.giphy.com/media/llbEoVMhkLngWlzVVa/giphy.gif" alt="Let's change the lightbulb" /></p>

<h2 id="the-research">The research</h2>

<p>I wanted a one-time password solution that:</p>

<ul>
  <li>Didn’t rely on my phone or any app.</li>
  <li>Could also be used in my computer.</li>
  <li>Was offline (no private keys stored in the cloud).</li>
</ul>

<p>That’s when I discovered <code class="language-plaintext highlighter-rouge">oathtool</code>: a command line tool for generating 6 digit tokens given a private key.</p>

<blockquote>
  <p>I installed it using <code class="language-plaintext highlighter-rouge">sudo apt install oathtool</code></p>
</blockquote>

<p>Generating a 6 digit token with <code class="language-plaintext highlighter-rouge">oathtool</code> is as easy as doing the following:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>oathtool <span class="nt">-b</span> <span class="nt">--totp</span> <span class="s1">'MyPrivateKey'</span>
798946
</code></pre></div></div>

<p>Discovering this tool was a good start, but I needed a good way of dealing with the private keys. Then I stumbled upon <a href="https://www.cyberciti.biz/faq/use-oathtool-linux-command-line-for-2-step-verification-2fa/">this article</a>. The author created two scripts:</p>

<ul>
  <li>One for encrypting the private key into a file using <code class="language-plaintext highlighter-rouge">gpg2</code>.</li>
  <li>One for decrypting the private key and retrieving the 6 digit token using <code class="language-plaintext highlighter-rouge">oathtool</code>.</li>
</ul>

<p>Additionally, the 6 digit token was automatically copied to the clipboard using <code class="language-plaintext highlighter-rouge">xclip</code>.</p>

<blockquote>
  <p>I installed both tools by running <code class="language-plaintext highlighter-rouge">sudo apt install gnupg2 xclip</code></p>
</blockquote>

<p>I loved the solution! Though it had some flaws like storing temporarily an unencrypted file with the private key, it was a great idea :)</p>

<p><img src="https://media.giphy.com/media/Mjq9vmDuJlBKw/giphy.gif" alt="Great idea" /></p>

<h2 id="the-plugin">The plugin</h2>

<p>I wrote <a href="https://github.com/alexdesousa/oath">Oath ZSH plugin</a> by gathering the best ideas from that article. I ended up with the following commands:</p>

<ul>
  <li>
    <p>Adding a private key:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nv">$ </span>oath add twitter.com
 Private key:
 <span class="o">[</span>SUCCESS]  Key created <span class="k">for </span>twitter.com
</code></pre></div>    </div>
  </li>
  <li>
    <p>Showing a 6 digit token (it’ll ask for the gpg password):</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nv">$ </span>oath twitter.com
 123456
 <span class="o">[</span>SUCCESS]  Code copied to clipboard
</code></pre></div>    </div>
  </li>
  <li>
    <p>Deleting a private key (it’ll ask for the gpg password):</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nv">$ </span>oath delete twitter.com
 <span class="o">[</span>WARN]     Deleting /home/user/.oath/twitter.com/B743BC73B5F90E2305142D226BBCD02E89ABBC79.gpg.gpg
 <span class="o">[</span>WARN]     Deleting /home/user/.oath/twitter.com
 <span class="o">[</span>SUCCESS]  Key deleted <span class="k">for </span>twitter.com
</code></pre></div>    </div>
  </li>
</ul>

<p>The same private keys I added to <code class="language-plaintext highlighter-rouge">oath</code>, I also added them to my phone’s Google Authenticator app. That way both, my computer and phone, generate the same 6 digit token at a given time.</p>

<p>The only difference is that now I can backup everything. I just need to copy the following folders:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">$HOME/.gnupg/</code>: GPG folder with all the gpg keys.</li>
  <li><code class="language-plaintext highlighter-rouge">$HOME/.oath/</code>: Oath folder where all the private keys are stored.</li>
</ul>

<blockquote>
  <p>For more info, visit <a href="https://github.com/alexdesousa/oath">Oath Github repository</a>.</p>
</blockquote>

<p><img src="https://media.giphy.com/media/Xd1EhywNhOjq5EPn5t/giphy.gif" alt="Safety" /></p>

<h2 id="conclusion">Conclusion</h2>

<p>Though this solution might not be for everyone, it solves the problem I had. Now I can reset my phone at any time and not worrying about my private keys, because they’re safely backed up.</p>

<p><img src="https://media.giphy.com/media/de0AlLgV7XTRhEudoL/giphy.gif" alt="The keys" /></p>

<p>Happy hacking!</p>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>Skogsrå: Simplifying Your Elixir Configuration</title>
          <description>Improving Elixir configurations for small and large projects.</description>
          <guid>https://thebroken.link/skogsra-simplifying-your-elixir-configuration/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Wed, 12 Feb 2020 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/skogsra-simplifying-your-elixir-configuration/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p>Once an Elixir project is large enough, maintaining config files and configuration variables becomes a nightmare:</p>

<ul>
  <li>Configuration variables are scattered throughout the code so it’s very easy to forget a configuration setting.</li>
  <li>OS environment variables must be casted to the correct type as they are always strings.</li>
  <li>Required variables must be checked by hand.</li>
  <li>Setting defaults can sometimes be a bit cumbersome.</li>
  <li>No type safety.</li>
</ul>

<p><img src="https://media.giphy.com/media/S0KRynVEROiOs/giphy.gif" alt="madness" /></p>

<p>Ideally though, configurations should be:</p>

<ul>
  <li>Documented.</li>
  <li>Easy to find.</li>
  <li>Easy to read.</li>
  <li>Declarative.</li>
</ul>

<p>In summary: <strong>easy to maintain</strong>.</p>

<h2 id="the-problem">The problem</h2>

<p>We’ll elaborate using the the following example:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">config</span> <span class="ss">:myapp</span><span class="p">,</span>
  <span class="ss">hostname:</span> <span class="no">System</span><span class="o">.</span><span class="n">get_env</span><span class="p">(</span><span class="s2">"HOSTNAME"</span><span class="p">)</span> <span class="o">||</span> <span class="s2">"localhost"</span><span class="p">,</span>
  <span class="ss">port:</span> <span class="no">String</span><span class="o">.</span><span class="n">to_integer</span><span class="p">(</span><span class="no">System</span><span class="o">.</span><span class="n">get_env</span><span class="p">(</span><span class="s2">"PORT"</span><span class="p">)</span> <span class="o">||</span> <span class="s2">"80"</span><span class="p">)</span>
</code></pre></div></div>

<p>The previous code is:</p>

<ul>
  <li>Undocumented: <code class="language-plaintext highlighter-rouge">hostname</code> and <code class="language-plaintext highlighter-rouge">port</code> of what?</li>
  <li>Hard to read: Too many concerns in a single line.</li>
  <li>Hard to find: where are these <code class="language-plaintext highlighter-rouge">hostname</code> and <code class="language-plaintext highlighter-rouge">port</code> used?</li>
  <li>Not declarative: we’re telling Elixir <strong><em>how</em> to retrieve the values</strong> instead of <strong><em>what</em> are the values we want</strong>.</li>
</ul>

<p>Conclusion: <strong>it’s hard to maintain</strong>.</p>

<h2 id="writing-a-config-module">Writing a config module</h2>

<p>We could mitigate some of these problems with one simple approach:</p>

<ul>
  <li>Create a module for your configs.</li>
  <li>Create a function for every single configuration parameter you app has.</li>
</ul>

<p>The following, though a bit more verbose, would be the equivalent to the previous config:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">Myapp</span><span class="o">.</span><span class="no">Config</span> <span class="k">do</span>
  <span class="nv">@moduledoc</span> <span class="s2">"My app config."</span>

  <span class="nv">@doc</span> <span class="s2">"My hostname"</span>
  <span class="k">def</span> <span class="n">hostname</span> <span class="k">do</span>
    <span class="no">System</span><span class="o">.</span><span class="n">get_env</span><span class="p">(</span><span class="s2">"HOSTNAME"</span><span class="p">)</span> <span class="o">||</span> <span class="s2">"localhost"</span>
  <span class="k">end</span>

  <span class="nv">@doc</span> <span class="s2">"My port"</span>
  <span class="k">def</span> <span class="n">port</span> <span class="k">do</span>
    <span class="no">String</span><span class="o">.</span><span class="n">to_integer</span><span class="p">(</span><span class="no">System</span><span class="o">.</span><span class="n">get_env</span><span class="p">(</span><span class="s2">"PORT"</span><span class="p">)</span> <span class="o">||</span> <span class="s2">"80"</span><span class="p">)</span>
  <span class="k">end</span>
<span class="k">end</span>
</code></pre></div></div>

<p>Unlike our original code, this one is:</p>

<ul>
  <li>Documented: Every function has <code class="language-plaintext highlighter-rouge">@doc</code> attribute.</li>
  <li>Easy to find: We just need to to look for calls to functions defined in this module.</li>
</ul>

<p>However, we still have essentially the same code we had before, which is:</p>

<ul>
  <li>Hard to read.</li>
  <li>Not declarative.</li>
</ul>

<p>There’s gotta be a better way!</p>

<p><img src="https://media.giphy.com/media/WTdg5GBR45X6NbxqJK/giphy.gif" alt="almost" /></p>

<h2 id="there-is-a-better-way---meet-skogsrå">There is a better way - Meet Skogsrå</h2>

<p><a href="https://hexdocs.pm/skogsra/readme.html">Skogsrå</a> is a library for loading configuration variables with ease, providing:</p>

<ul>
  <li>Variable defaults.</li>
  <li>Automatic type casting of values.</li>
  <li>Automatic docs and spec generation.</li>
  <li>OS environment template generation.</li>
  <li>Run-time reloading.</li>
  <li>Setting variable’s values at run-time.</li>
  <li>Fast cached values access by using <code class="language-plaintext highlighter-rouge">:persistent_term</code> as temporal storage.</li>
  <li>YAML configuration provider for Elixir releases.</li>
</ul>

<p>The previous example can be re-written as follows:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">Myapp</span><span class="o">.</span><span class="no">Config</span> <span class="k">do</span>
  <span class="nv">@moduledoc</span> <span class="s2">"My app config."</span>
  <span class="kn">use</span> <span class="no">Skogsra</span>

  <span class="nv">@envdoc</span> <span class="s2">"My hostname"</span>
  <span class="n">app_env</span> <span class="ss">:hostname</span><span class="p">,</span> <span class="ss">:myapp</span><span class="p">,</span> <span class="ss">:hostname</span><span class="p">,</span>
    <span class="ss">default:</span> <span class="s2">"localhost"</span><span class="p">,</span>
    <span class="ss">os_env:</span> <span class="s2">"HOSTNAME"</span>

  <span class="nv">@envdoc</span> <span class="s2">"My port"</span>
  <span class="n">app_env</span> <span class="ss">:port</span><span class="p">,</span> <span class="ss">:myapp</span><span class="p">,</span> <span class="ss">:port</span><span class="p">,</span>
    <span class="ss">default:</span> <span class="mi">80</span><span class="p">,</span>
    <span class="ss">os_env:</span> <span class="s2">"PORT"</span>
<span class="k">end</span>
</code></pre></div></div>

<p>This module will have these functions:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Myapp.Config.hostname/0</code> for retrieving the hostname.</li>
  <li><code class="language-plaintext highlighter-rouge">Myapp.Config.port/0</code> for retrieving the port.</li>
</ul>

<p>With this implementation, we end up with:</p>

<ul>
  <li>Documented configuration variables: Via <code class="language-plaintext highlighter-rouge">@envdoc</code> module attribute.</li>
  <li>Easy to find: Every configuration variable will be in <code class="language-plaintext highlighter-rouge">Myapp.Config</code> module.</li>
  <li>Easy to read: <code class="language-plaintext highlighter-rouge">app_env</code> options are self explanatory.</li>
  <li>Declarative: we’re telling Skogsrå <em>what we want</em>.</li>
  <li><strong>Bonus</strong>: Type-safety (see <a href="#strong-typing">Strong typing</a> section).</li>
</ul>

<p><img src="https://media.giphy.com/media/wAxlCmeX1ri1y/giphy.gif" alt="dance" /></p>

<h2 id="how-it-works">How it works</h2>

<p>Calling <code class="language-plaintext highlighter-rouge">Myapp.Config.port()</code> will retrieve the value for the port in the following order:</p>

<ol>
  <li>From the OS environment variable <code class="language-plaintext highlighter-rouge">$PORT</code>.</li>
  <li>
    <p>From the configuration file e.g. our test config file might look like:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="c1"># file config/test.exs</span>
 <span class="kn">use</span> <span class="no">Mix</span><span class="o">.</span><span class="no">Config</span>

 <span class="n">config</span> <span class="ss">:myapp</span><span class="p">,</span>
   <span class="ss">port:</span> <span class="mi">4000</span>
</code></pre></div>    </div>
  </li>
  <li>From the default value, if it exists (In this case, it would return the integer <code class="language-plaintext highlighter-rouge">80</code>).</li>
</ol>

<p>The values will be casted as the default values’ type unless the option <code class="language-plaintext highlighter-rouge">type</code> is provided (see <a href="#explicit-type-casting">Explicit type casting</a> section).</p>

<p>Though Skogsrå has <a href="https://hexdocs.pm/skogsra/readme.html">many options and features</a>, we will just explore the ones I use the most:</p>

<ul>
  <li><a href="#explicit-type-casting">Explicit type casting</a>.</li>
  <li><a href="#defining-custom-types">Defining custom types</a>.</li>
  <li><a href="#required-variables">Required variables</a>.</li>
  <li><a href="#strong-typing">Strong typing</a>.</li>
</ul>

<h2 id="explicit-type-casting">Explicit type casting</h2>

<p>When the types are not <code class="language-plaintext highlighter-rouge">any</code>, <code class="language-plaintext highlighter-rouge">binary</code>, <code class="language-plaintext highlighter-rouge">integer</code>, <code class="language-plaintext highlighter-rouge">float</code>, <code class="language-plaintext highlighter-rouge">boolean</code> or <code class="language-plaintext highlighter-rouge">atom</code>, Skogsrå cannot automatically cast values solely by the default value’s type. Types then need to be specified explicitly using the option <code class="language-plaintext highlighter-rouge">type</code>. The available types are:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">:any</code> (default).</li>
  <li><code class="language-plaintext highlighter-rouge">:binary</code>.</li>
  <li><code class="language-plaintext highlighter-rouge">:integer</code>.</li>
  <li><code class="language-plaintext highlighter-rouge">:float</code>.</li>
  <li><code class="language-plaintext highlighter-rouge">:boolean</code>.</li>
  <li><code class="language-plaintext highlighter-rouge">:atom</code>.</li>
  <li><code class="language-plaintext highlighter-rouge">:module</code>: for modules loaded in the system.</li>
  <li><code class="language-plaintext highlighter-rouge">:unsafe_module</code>: for modules that might or might not be loaded in the system.</li>
  <li><code class="language-plaintext highlighter-rouge">Skogsra.Type</code> implementation: a <code class="language-plaintext highlighter-rouge">behaviour</code> for defining custom types.</li>
</ul>

<h2 id="defining-custom-types">Defining custom types</h2>

<p>Let’s say we need to read an OS environment variable called <code class="language-plaintext highlighter-rouge">HISTOGRAM_BUCKETS</code> as a list of integers:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">HISTOGRAM_BUCKETS</span><span class="o">=</span><span class="s2">"1, 10, 30, 60"</span>
</code></pre></div></div>

<p>We could then implement <code class="language-plaintext highlighter-rouge">Skogsra.Type</code> behaviour to parse the string correctly:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">Myapp</span><span class="o">.</span><span class="no">Type</span><span class="o">.</span><span class="no">IntegerList</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">Skogsra</span><span class="o">.</span><span class="no">Type</span>

  <span class="nv">@impl</span> <span class="no">Skogsra</span><span class="o">.</span><span class="no">Type</span>
  <span class="k">def</span> <span class="n">cast</span><span class="p">(</span><span class="n">value</span><span class="p">)</span>

  <span class="k">def</span> <span class="n">cast</span><span class="p">(</span><span class="n">value</span><span class="p">)</span> <span class="ow">when</span> <span class="n">is_binary</span><span class="p">(</span><span class="n">value</span><span class="p">)</span> <span class="k">do</span>
    <span class="n">list</span> <span class="o">=</span>
      <span class="n">value</span>
      <span class="o">|&gt;</span> <span class="no">String</span><span class="o">.</span><span class="n">split</span><span class="p">(</span><span class="sr">~r/,/</span><span class="p">)</span>
      <span class="o">|&gt;</span> <span class="no">Stream</span><span class="o">.</span><span class="n">map</span><span class="p">(</span><span class="o">&amp;</span><span class="no">String</span><span class="o">.</span><span class="n">trim</span><span class="o">/</span><span class="mi">1</span><span class="p">)</span>
      <span class="o">|&gt;</span> <span class="no">Enum</span><span class="o">.</span><span class="n">map</span><span class="p">(</span><span class="no">String</span><span class="o">.</span><span class="n">to_integer</span><span class="o">/</span><span class="mi">1</span><span class="p">)</span>
    <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">list</span><span class="p">}</span>
  <span class="k">end</span>

  <span class="k">def</span> <span class="n">cast</span><span class="p">(</span><span class="n">value</span><span class="p">)</span> <span class="ow">when</span> <span class="n">is_list</span><span class="p">(</span><span class="n">value</span><span class="p">)</span> <span class="k">do</span>
    <span class="k">if</span> <span class="no">Enum</span><span class="o">.</span><span class="n">all?</span><span class="p">(</span><span class="n">value</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">is_integer</span><span class="o">/</span><span class="mi">1</span><span class="p">),</span> <span class="k">do</span><span class="p">:</span> <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">value</span><span class="p">},</span> <span class="k">else</span><span class="p">:</span> <span class="ss">:error</span>
  <span class="k">end</span>

  <span class="k">def</span> <span class="n">cast</span><span class="p">(</span><span class="n">_</span><span class="p">)</span> <span class="k">do</span>
    <span class="ss">:error</span>
  <span class="k">end</span>
<span class="k">end</span>
</code></pre></div></div>

<p>And finally use <code class="language-plaintext highlighter-rouge">Myapp.Type.IntegerList</code> in our Skogsrå configuration:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">Myapp</span><span class="o">.</span><span class="no">Config</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">Skogsra</span>

  <span class="nv">@envdoc</span> <span class="s2">"Histogram buckets"</span>
  <span class="n">app_env</span> <span class="ss">:buckets</span><span class="p">,</span> <span class="ss">:myapp</span><span class="p">,</span> <span class="ss">:histogram_buckets</span><span class="p">,</span>
    <span class="ss">type:</span> <span class="no">Myapp</span><span class="o">.</span><span class="no">Type</span><span class="o">.</span><span class="no">IntegerList</span><span class="p">,</span>
    <span class="ss">os_env:</span> <span class="s2">"HISTOGRAM_BUCKETS"</span>
<span class="k">end</span>
</code></pre></div></div>

<p>Then it should be easy to retrieve our <code class="language-plaintext highlighter-rouge">buckets</code> from an OS environment variable:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span><span class="o">&gt;</span> <span class="no">System</span><span class="o">.</span><span class="n">get_env</span><span class="p">(</span><span class="s2">"BUCKETS"</span><span class="p">)</span>
<span class="s2">"1, 10, 30, 60"</span>
<span class="n">iex</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span><span class="o">&gt;</span> <span class="no">Myapp</span><span class="o">.</span><span class="no">Config</span><span class="o">.</span><span class="n">buckets</span><span class="p">()</span>
<span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">10</span><span class="p">,</span> <span class="mi">30</span><span class="p">,</span> <span class="mi">60</span><span class="p">]}</span>
</code></pre></div></div>

<p>or if the variable is not defined, from our application configuration:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span><span class="o">&gt;</span> <span class="no">System</span><span class="o">.</span><span class="n">app_env</span><span class="p">(</span><span class="ss">:myapp</span><span class="p">,</span> <span class="ss">:histogram_buckets</span><span class="p">)</span>
<span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">10</span><span class="p">,</span> <span class="mi">30</span><span class="p">,</span> <span class="mi">60</span><span class="p">]</span>
<span class="n">iex</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span><span class="o">&gt;</span> <span class="no">Myapp</span><span class="o">.</span><span class="no">Config</span><span class="o">.</span><span class="n">buckets</span><span class="p">()</span>
<span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">10</span><span class="p">,</span> <span class="mi">30</span><span class="p">,</span> <span class="mi">60</span><span class="p">]}</span>
</code></pre></div></div>

<h2 id="required-variables">Required variables</h2>

<p>Skogsrå provides an option for making configuration variables mandatory. This is useful when there is no default value for our variable and Skogsrå it’s expected to find a value in either an OS environment variable or the application configuration e.g. given the following config module:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">MyApp</span><span class="o">.</span><span class="no">Config</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">Skogsra</span>

  <span class="nv">@envdoc</span> <span class="s2">"Server port."</span>
  <span class="n">app_env</span> <span class="ss">:port</span><span class="p">,</span> <span class="ss">:myapp</span><span class="p">,</span> <span class="ss">:port</span><span class="p">,</span>
    <span class="ss">os_env:</span> <span class="s2">"PORT"</span><span class="p">,</span>
    <span class="ss">required:</span> <span class="no">true</span>
<span class="k">end</span>
</code></pre></div></div>

<p>The function <code class="language-plaintext highlighter-rouge">Myapp.Config.port()</code> will error if <code class="language-plaintext highlighter-rouge">PORT</code> is undefined and
the application configuration is not found:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span><span class="o">&gt;</span> <span class="no">System</span><span class="o">.</span><span class="n">get_env</span><span class="p">(</span><span class="s2">"PORT"</span><span class="p">)</span>
<span class="no">nil</span>
<span class="n">iex</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span><span class="o">&gt;</span> <span class="no">Application</span><span class="o">.</span><span class="n">get_env</span><span class="p">(</span><span class="ss">:myapp</span><span class="p">,</span> <span class="ss">:port</span><span class="p">)</span>
<span class="no">nil</span>
<span class="n">iex</span><span class="p">(</span><span class="mi">3</span><span class="p">)</span><span class="o">&gt;</span> <span class="no">MyApp</span><span class="o">.</span><span class="no">Config</span><span class="o">.</span><span class="n">port</span><span class="p">()</span>
<span class="p">{</span><span class="ss">:error</span><span class="p">,</span> <span class="s2">"Variable port in app myapp is undefined"</span><span class="p">}</span>
</code></pre></div></div>

<h2 id="strong-typing">Strong typing</h2>

<p>All the configuration variables will have the correct function <code class="language-plaintext highlighter-rouge">@spec</code> definition e.g. given the following definition:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">Myapp</span><span class="o">.</span><span class="no">Config</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">Skogsra</span>

  <span class="nv">@envdoc</span> <span class="s2">"PostgreSQL hostname"</span>
  <span class="n">app_env</span> <span class="ss">:db_port</span><span class="p">,</span> <span class="ss">:myapp</span><span class="p">,</span> <span class="p">[</span><span class="ss">:postgres</span><span class="p">,</span> <span class="ss">:port</span><span class="p">],</span>
    <span class="ss">default:</span> <span class="mi">5432</span>
<span class="k">end</span>
</code></pre></div></div>

<p>The generated function <code class="language-plaintext highlighter-rouge">Myapp.Config.db_port/0</code> will have the following <code class="language-plaintext highlighter-rouge">@spec</code>:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">@spec</span> <span class="n">db_port</span><span class="p">()</span> <span class="p">::</span> <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">integer</span><span class="p">()}</span> <span class="o">|</span> <span class="p">{</span><span class="ss">:error</span><span class="p">,</span> <span class="n">binary</span><span class="p">()}</span>
</code></pre></div></div>

<p>The type is derived from:</p>

<ul>
  <li>The <code class="language-plaintext highlighter-rouge">default</code> value (in this case the integer <code class="language-plaintext highlighter-rouge">5432</code>)</li>
  <li>The <code class="language-plaintext highlighter-rouge">type</code> configuration value (see the previous <a href="#explicit-type-casting">Explicit type casting</a> section).</li>
</ul>

<h2 id="conclusion">Conclusion</h2>

<p><a href="https://hexdocs.pm/skogsra/readme.html">Skogsra</a> provides a simple way to handle your Elixir application configurations in a type-safe and organized way. Big projects can certainly benefit from using it.</p>

<p>Hope you found this article useful. Happy coding!</p>

<p><img src="https://media.giphy.com/media/PiQejEf31116URju4V/giphy.gif" alt="coding" /></p>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>Yggdrasil: Easy Pub-Sub in Elixir</title>
          <description>An overview of Yggdrasil's capabilities.</description>
          <guid>https://thebroken.link/yggdrasil-easy-pub-sub-in-elixir/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Sat, 06 Jul 2019 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/yggdrasil-easy-pub-sub-in-elixir/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p>When I started coding in Elixir (around 2016), I was working for a financial company. Our product automatically invested money in the Forex market by copying traders’ actions (<em>market orders</em>) in real time. We had the following:</p>

<p>
  <figure class="flex flex-col items-center w-full h-auto mx-auto">
    <img src="/yggdrasil-easy-pub-sub-in-elixir/system.png" alt="Our System" />
    <figcaption class="text-center">
      <p>Our System</p>

    </figcaption>
  </figure>
</p>

<p>In other words, our system:</p>

<ol>
  <li><strong>Subscribed</strong> to PostgreSQL for receiving <em>trader actions</em>.</li>
  <li><strong>Published</strong> to RabbitMQ for:
    <ul>
      <li>Categorizing <em>trader actions</em>.</li>
      <li>And enqueuing <em>trader actions</em> in the proper queue.</li>
    </ul>
  </li>
  <li><strong>Subscribed</strong> to Redis for receiving updates on prices.</li>
  <li><strong>Subscribed</strong> to several RabbitMQ queues for:
    <ul>
      <li>Receiving the categorized <em>trader actions</em>.</li>
      <li>And deciding whether it should open/close some <em>market orders</em> or not.</li>
    </ul>
  </li>
  <li>Opened and closed <em>market orders</em>.</li>
</ol>

<p>We needed to be able to communicate with three systems (PostgreSQL, RabbitMQ and Redis). However, in general, we only needed three actions:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">subscribe/1</code> to a channel.</li>
  <li><code class="language-plaintext highlighter-rouge">publish/2</code> a message in a channel.</li>
  <li><code class="language-plaintext highlighter-rouge">unsubscribe/1</code> from a channel.</li>
</ul>

<p>If we could generalize those three actions into an API, we could then implement three individual adapters for every system to handle the annoying stuff like disconnections, failures, resource management, protocols, etc.</p>

<p><img src="https://media.giphy.com/media/aih5IZkussTiE/giphy.gif" alt="hard" /></p>

<h2 id="table-of-contents">Table of Contents</h2>

<ul>
  <li>
    <p><a href="#0">Chapter I: Meet Yggdrasil</a></p>
  </li>
  <li>
    <p><a href="#1">Chapter II: Yggdrasil and PostgreSQL Notifications</a></p>
  </li>
  <li>
    <p><a href="#2">Chapter III: Yggdrasil and RabbitMQ Subscriptions</a></p>
  </li>
  <li>
    <p><a href="#3">Chapter IV: Yggdrasil as Distributed PubSub</a></p>
  </li>
  <li>
    <p><a href="#4">In the end: One API to Rule Them All</a></p>
  </li>
</ul>

<div class="chapter">
    <h2 id="0">
      Chapter I: Meet Yggdrasil
    </h2>
  </div>

<p>Handling subscriptions should be easy and, in an ideal world, we would only need to know <em>where</em> to connect and <em>start receiving</em> messages right away.</p>

<p>We shouldn’t need to worry about secondary (yet relevant) things like disconnections, failures and managing resources.</p>

<p><img src="https://raw.githubusercontent.com/gmtprime/yggdrasil/master/priv/static/yggdrasil.png" alt="Yggdrasil" /></p>

<blockquote>
  <p><em>Yggdrasil</em> is an immense mythical tree that connects the nine worlds in Norse cosmology.</p>
</blockquote>

<p><a href="https://github.com/gmtprime/yggdrasil">Yggdrasil</a> was our pub-sub generalization. Using the strong foundations of Phoenix pub-sub library, we built an agnostic publisher/subscriber application that has:</p>

<ul>
  <li>Multi node support.</li>
  <li>Simple API: <code class="language-plaintext highlighter-rouge">subscribe/1</code>, <code class="language-plaintext highlighter-rouge">unsubscribe/1</code> and <code class="language-plaintext highlighter-rouge">publish/2</code>.</li>
  <li>A <code class="language-plaintext highlighter-rouge">GenServer</code> wrapper for handling subscriber events easily.</li>
  <li>A basic adapter for using Elixir message distribution.</li>
  <li>Fault-tolerant adapters for:
    <ul>
      <li><a href="https://github.com/gmtprime/yggdrasil_redis">Redis</a>.</li>
      <li><a href="https://github.com/gmtprime/yggdrasil_postgres">PostgreSQL</a>.</li>
      <li><a href="https://github.com/gmtprime/yggdrasil_rabbitmq">RabbitMQ</a>.</li>
    </ul>
  </li>
</ul>

<h3 id="one-api-to-rule-them-all">One API to rule them all</h3>

<p>Yggdrasil’s API is very simple:</p>

<ul>
  <li>
    <p>A process subscribes to <code class="language-plaintext highlighter-rouge">"my_channel"</code>:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">iex</span><span class="o">&gt;</span> <span class="ss">:ok</span> <span class="o">=</span> <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">subscribe</span><span class="p">(</span><span class="ss">name:</span> <span class="s2">"my_channel"</span><span class="p">)</span>
 <span class="n">iex</span><span class="o">&gt;</span> <span class="n">flush</span><span class="p">()</span>
 <span class="p">{</span><span class="ss">:Y_CONNECTED</span><span class="p">,</span> <span class="p">%</span><span class="no">Yggdrasil</span><span class="o">.</span><span class="no">Channel</span><span class="p">{</span><span class="o">...</span><span class="p">}}</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>A process (in this case the same process) publishes the message <code class="language-plaintext highlighter-rouge">"my message"</code> in <code class="language-plaintext highlighter-rouge">"my_channel"</code>.</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">iex</span><span class="o">&gt;</span> <span class="ss">:ok</span> <span class="o">=</span> <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">publish</span><span class="p">([</span><span class="ss">name:</span> <span class="s2">"my_channel"</span><span class="p">],</span> <span class="s2">"my message"</span><span class="p">)</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>The message should be in the mailbox of the subscriber process:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">iex</span><span class="o">&gt;</span> <span class="n">flush</span><span class="p">()</span>
 <span class="p">{</span><span class="ss">:Y_EVENT</span><span class="p">,</span> <span class="p">%</span><span class="no">Yggdrasil</span><span class="o">.</span><span class="no">Channel</span><span class="p">{</span><span class="o">...</span><span class="p">},</span> <span class="s2">"my message"</span><span class="p">}</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>The subscriber can unsubscribe from the channel to stop receiving messages:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">iex</span><span class="o">&gt;</span> <span class="ss">:ok</span> <span class="o">=</span> <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">unsubscribe</span><span class="p">(</span><span class="ss">name:</span> <span class="s2">"my_channel"</span><span class="p">)</span>
 <span class="n">iex</span><span class="o">&gt;</span> <span class="n">flush</span><span class="p">()</span>
 <span class="p">{</span><span class="ss">:Y_DISCONNECTED</span><span class="p">,</span> <span class="p">%</span><span class="no">Yggdrasil</span><span class="o">.</span><span class="no">Channel</span><span class="p">{</span><span class="o">...</span><span class="p">}}</span>
</code></pre></div>    </div>
  </li>
</ul>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">flush()</code> cleans the IEx process mailbox. In general, receiving Yggdrasil messages should be the same as receiving messages when the sender uses <code class="language-plaintext highlighter-rouge">send/2</code>.</p>
</blockquote>

<p><img src="https://media.giphy.com/media/cdGQHR4Qzefx6/giphy.gif" alt="So easy!" /></p>

<h3 id="yggdrasil-behaviour">Yggdrasil behaviour</h3>

<p>Yggdrasil provides a <code class="language-plaintext highlighter-rouge">behaviour</code> for writing subscribers easily. Following the previous example, the subscriber could be written as follows:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">Subscriber</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">Yggdrasil</span>

  <span class="k">def</span> <span class="n">start_link</span> <span class="k">do</span>
    <span class="n">channel</span> <span class="o">=</span> <span class="p">[</span><span class="ss">name:</span> <span class="s2">"my_channel"</span><span class="p">]</span>
    <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">start_link</span><span class="p">(</span><span class="bp">__MODULE__</span><span class="p">,</span> <span class="p">[</span><span class="n">channel</span><span class="p">])</span>
  <span class="k">end</span>

  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="n">_channel</span><span class="p">,</span> <span class="n">message</span><span class="p">,</span> <span class="n">_state</span><span class="p">)</span> <span class="k">do</span>
    <span class="no">IO</span><span class="o">.</span><span class="n">inspect</span> <span class="p">{</span><span class="ss">:mailbox</span><span class="p">,</span> <span class="n">message</span><span class="p">}</span>
    <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="no">nil</span><span class="p">}</span>
  <span class="k">end</span>
<span class="k">end</span>
</code></pre></div></div>

<p>This subscriber will print the message as it receives it:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="o">&gt;</span> <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">_pid</span><span class="p">}</span> <span class="o">=</span> <span class="no">Subscriber</span><span class="o">.</span><span class="n">start_link</span><span class="p">()</span>
<span class="n">iex</span><span class="o">&gt;</span> <span class="ss">:ok</span> <span class="o">=</span> <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">publish</span><span class="p">([</span><span class="ss">name:</span> <span class="s2">"my_channel"</span><span class="p">],</span> <span class="s2">"my message"</span><span class="p">)</span>
<span class="p">{</span><span class="ss">:mailbox</span><span class="p">,</span> <span class="s2">"my_message"</span><span class="p">}</span>
</code></pre></div></div>

<blockquote>
  <p>An interesting side-effect is that now we can send messages to any process as long as they are subscribed to the right channel without needing to know the process PID or name.</p>
</blockquote>

<div class="chapter">
    <h2 id="1">
      Chapter II: Yggdrasil and PostgreSQL Notifications
    </h2>
  </div>

<p>One thing I really like about PostgreSQL is its notifications via <code class="language-plaintext highlighter-rouge">pg_notify</code>. This feature is very useful when trying to get real-time notifications for certain changes in a databases.</p>

<h3 id="postgresql-notifications">PostgreSQL notifications</h3>

<p>Creating notifications in PostgreSQL is very easy e.g. let’s say we have a table for books:</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- User table creation</span>
<span class="k">CREATE</span> <span class="k">TABLE</span> <span class="n">books</span> <span class="p">(</span>
  <span class="n">id</span> <span class="nb">SERIAL</span> <span class="k">PRIMARY</span> <span class="k">KEY</span><span class="p">,</span>
  <span class="n">title</span> <span class="nb">TEXT</span> <span class="k">NOT</span> <span class="k">NULL</span> <span class="k">UNIQUE</span>
<span class="p">);</span>
</code></pre></div></div>

<p>and we want JSON notifications in the channel <code class="language-plaintext highlighter-rouge">new_books</code> every time a new book is created in our database e.g:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w">
  </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Animal Farm"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>The trigger could be implemented as follows:</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- Trigger function creation</span>
<span class="k">CREATE</span> <span class="k">OR</span> <span class="k">REPLACE</span> <span class="k">FUNCTION</span> <span class="n">trigger_new_book</span><span class="p">()</span>
  <span class="k">RETURNS</span> <span class="k">TRIGGER</span> <span class="k">AS</span> <span class="err">$$</span>
  <span class="k">DECLARE</span>
    <span class="n">payload</span> <span class="n">JSON</span><span class="p">;</span>
  <span class="k">BEGIN</span>
    <span class="n">payload</span> <span class="p">:</span><span class="o">=</span> <span class="n">json_build_object</span><span class="p">(</span>
      <span class="s1">'id'</span><span class="p">,</span> <span class="k">NEW</span><span class="p">.</span><span class="n">id</span><span class="p">,</span>
      <span class="s1">'title'</span><span class="p">,</span> <span class="k">NEW</span><span class="p">.</span><span class="n">title</span>
    <span class="p">);</span>

    <span class="n">PERFORM</span> <span class="n">pg_notify</span><span class="p">(</span><span class="s1">'new_books'</span><span class="p">,</span> <span class="n">payload</span><span class="p">::</span><span class="nb">TEXT</span><span class="p">);</span>
    <span class="k">RETURN</span> <span class="k">NEW</span><span class="p">;</span>
  <span class="k">END</span><span class="p">;</span>
  <span class="err">$$</span> <span class="k">LANGUAGE</span> <span class="n">plpgsql</span><span class="p">;</span>

<span class="c1">-- Sets the trigger function in 'books' table</span>
<span class="k">CREATE</span> <span class="k">TRIGGER</span> <span class="n">books_notify_new_book</span>
  <span class="k">BEFORE</span> <span class="k">INSERT</span> <span class="k">ON</span> <span class="n">books</span>
  <span class="k">FOR</span> <span class="k">EACH</span> <span class="k">ROW</span>
  <span class="k">EXECUTE</span> <span class="k">PROCEDURE</span> <span class="n">trigger_new_book</span><span class="p">();</span>
</code></pre></div></div>

<p>Then, the following query would trigger our JSON message in the channel <code class="language-plaintext highlighter-rouge">new_books</code>:</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">INSERT</span> <span class="k">INTO</span> <span class="n">books</span> <span class="p">(</span><span class="n">title</span><span class="p">)</span> <span class="k">VALUES</span> <span class="p">(</span><span class="s1">'Animal Farm'</span><span class="p">);</span>
</code></pre></div></div>

<h3 id="the-problem">The Problem</h3>

<p>Though subscribing to our database notifications can be done easily with <a href="https://github.com/elixir-ecto/postgrex">Postgrex</a> library, handling the connections to the database is a bit of a hassle. We need to ensure:</p>

<ul>
  <li><strong>Connection multiplexing</strong>: avoiding over consuming database resources.</li>
  <li><strong>Fault-tolerant connections</strong>: supporting re-connections in case of failure or disconnection.</li>
  <li><strong>Re-connection back-off time</strong>: avoiding overloading the database on multiple re-connections.</li>
</ul>

<p><img src="https://media.giphy.com/media/FrLKYbLI0djKU/giphy.gif" alt="problem" /></p>

<h3 id="the-solution">The Solution</h3>

<p><a href="https://github.com/gmtprime/yggdrasil_postgres">Yggdrasil for PostgreSQL</a> is an adapter that supports all the features mentioned above while maintaining Yggdrasil’s simple API e.g:</p>

<p>For our example, we could subscribe to the database messages by doing the following:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="o">&gt;</span> <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">subscribe</span><span class="p">(</span><span class="ss">name:</span> <span class="s2">"new_books"</span><span class="p">,</span> <span class="ss">adapter:</span> <span class="ss">:postgres</span><span class="p">,</span> <span class="ss">transformer:</span> <span class="ss">:json</span><span class="p">)</span>
<span class="n">iex</span><span class="o">&gt;</span> <span class="n">flush</span><span class="p">()</span>
<span class="p">{</span><span class="ss">:Y_CONNECTED</span><span class="p">,</span> <span class="p">%</span><span class="no">Yggdrasil</span><span class="o">.</span><span class="no">Channel</span><span class="p">{</span><span class="o">...</span><span class="p">}}</span>
</code></pre></div></div>

<p>Running the following query:</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">INSERT</span> <span class="k">INTO</span> <span class="n">books</span> <span class="p">(</span><span class="n">title</span><span class="p">)</span> <span class="k">VALUES</span> <span class="p">(</span><span class="s1">'1984'</span><span class="p">);</span>
</code></pre></div></div>

<p>We will get the following message in IEx:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="o">&gt;</span> <span class="n">flush</span><span class="p">()</span>
<span class="p">{</span><span class="ss">:Y_EVENT</span><span class="p">,</span> <span class="p">%</span><span class="no">Yggdrasil</span><span class="o">.</span><span class="no">Channel</span><span class="p">{</span><span class="o">...</span><span class="p">},</span> <span class="p">%{</span><span class="s2">"id"</span> <span class="o">=&gt;</span> <span class="mi">2</span><span class="p">,</span> <span class="s2">"title"</span> <span class="o">=&gt;</span> <span class="s2">"1984"</span><span class="p">}}</span>
</code></pre></div></div>

<blockquote>
  <p><strong>Note</strong>: <code class="language-plaintext highlighter-rouge">Yggdrasil</code> comes with built-in message transformers. We’ve used
<code class="language-plaintext highlighter-rouge">:json</code> transformer for this example in order to get a map from the JSON
data.</p>
</blockquote>

<p>Additionally, our subscriber could also be an <code class="language-plaintext highlighter-rouge">Yggdrasil</code> process e.g:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">Books</span><span class="o">.</span><span class="no">Subscriber</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">Yggdrasil</span>

  <span class="k">def</span> <span class="n">start_link</span><span class="p">(</span><span class="n">options</span> <span class="p">\\</span> <span class="p">[])</span> <span class="k">do</span>
    <span class="n">channel</span> <span class="o">=</span> <span class="p">[</span>
      <span class="ss">name:</span> <span class="s2">"new_books"</span><span class="p">,</span>
      <span class="ss">adapter:</span> <span class="ss">:postgres</span><span class="p">,</span>
      <span class="ss">transformer:</span> <span class="ss">:json</span>
    <span class="p">]</span>

    <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">start_link</span><span class="p">(</span><span class="bp">__MODULE__</span><span class="p">,</span> <span class="p">[</span><span class="n">channel</span><span class="p">],</span> <span class="n">options</span><span class="p">)</span>
  <span class="k">end</span>

  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="n">_channel</span><span class="p">,</span> <span class="p">%{</span><span class="s2">"id"</span> <span class="o">=&gt;</span> <span class="n">id</span><span class="p">,</span> <span class="s2">"title"</span> <span class="o">=&gt;</span> <span class="n">title</span><span class="p">},</span> <span class="n">_state</span><span class="p">)</span> <span class="k">do</span>
    <span class="o">...</span> <span class="n">handle</span> <span class="n">event</span> <span class="o">...</span>
    <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="no">nil</span><span class="p">}</span>
  <span class="k">end</span>
<span class="k">end</span>
</code></pre></div></div>

<p>It’s also possbible to use <code class="language-plaintext highlighter-rouge">Yggdrasil.publish/2</code> with PostgreSQL:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="o">&gt;</span> <span class="n">message</span> <span class="o">=</span> <span class="p">%{</span><span class="s2">"id"</span> <span class="o">=&gt;</span> <span class="mi">3</span><span class="p">,</span> <span class="s2">"title"</span> <span class="o">=&gt;</span> <span class="s2">"A Brave New World"</span><span class="p">}</span>
<span class="n">iex</span><span class="o">&gt;</span> <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">publish</span><span class="p">([</span><span class="ss">name:</span> <span class="s2">"new_books"</span><span class="p">,</span> <span class="ss">adapter:</span> <span class="ss">:postgres</span><span class="p">,</span> <span class="ss">transformer:</span> <span class="ss">:json</span><span class="p">],</span> <span class="n">message</span><span class="p">)</span>
</code></pre></div></div>

<p><img src="https://media.giphy.com/media/zcCGBRQshGdt6/giphy.gif" alt="Too easy!" /></p>

<div class="chapter">
    <h2 id="2">
      Chapter III: Yggdrasil and RabbitMQ Subscriptions
    </h2>
  </div>

<p>One of the features I really like about RabbitMQ is its queue routing. Its flexibility allows you to do interesting things without much of a hassle. But before I dig deep into RabbitMQ’s routing capabilities, I would like to mention some concepts.</p>

<h3 id="connections-and-channels">Connections and Channels</h3>

<p>RabbitMQ uses not only <strong>connections</strong>, but virtual connections called <strong>channels</strong>. The idea of channels is to introduce multiplexing in a single connection. A small system could establish only one connection with RabbitMQ while opening a channel for every single execution thread e.g:&lt;p&gt;</p>
<figure class="flex flex-col items-center w-full h-auto mx-auto">
    <img src="/yggdrasil-easy-pub-sub-in-elixir/rabbitmq-connection.png" alt="RabbitMQ channel multiplexing" />
    <figcaption class="text-center">
      <p>A regular RabbitMQ connection</p>

    </figcaption>
  </figure>
<p>&lt;/p&gt;
The rule of thumb would be to use:</p>

<ul>
  <li>One connection per application.</li>
  <li>One channel per process in the application.</li>
</ul>

<blockquote>
  <p><strong>Note</strong>: Once our connection starts to be overloaded, we can start adding more connections to our connection pool.</p>
</blockquote>

<p>With a normal RabbitMQ setup, we need to deal with:</p>

<ul>
  <li><strong>Connection pools</strong>: avoiding over consuming resources.</li>
  <li><strong>Channel cleaning</strong>: avoiding channel memory leaks when they are not closed properly.</li>
  <li><strong>Fault-tolerant connections</strong>: supporting re-connections in case of failure or disconnection.</li>
  <li><strong>Re-connection back-off time</strong>: avoiding overloading the database on multiple re-connections.</li>
</ul>

<h3 id="exchanges-and-queues">Exchanges and Queues</h3>

<p>An <strong>exchange</strong> is a message router. Every <strong>queue</strong> attached to it will be identified by a <strong>routing key</strong>. Typically, routing keys are words separated by dots e.g. <code class="language-plaintext highlighter-rouge">spain.barcelona.gracia</code>.</p>

<p>Additionally, routing keys support wildcards, for example: <code class="language-plaintext highlighter-rouge">spain.barcelona.*</code> will match messages with routing keys like <code class="language-plaintext highlighter-rouge">spain.barcelona.gracia</code> and <code class="language-plaintext highlighter-rouge">spain.barcelona.raval</code>.</p>

<p>It’s easier to see these concepts with an image example:&lt;p&gt;</p>
<figure class="flex flex-col items-center w-full h-auto mx-auto">
    <img src="/yggdrasil-easy-pub-sub-in-elixir/rabbitmq-exchange.png" alt="RabbitMQ exchange routing" />
    <figcaption class="text-center">
      <p>A RabbitMQ exchange</p>

    </figcaption>
  </figure>
<p>&lt;/p&gt;
In the previous image:</p>

<ul>
  <li><strong>__Publisher X__</strong> and <strong>__Publisher Y__</strong> are sending messages to <strong><em>Exchange logs</em></strong>.</li>
  <li><strong>__Subscriber A__</strong> is subscribed to <code class="language-plaintext highlighter-rouge">logs.*</code>.</li>
  <li><strong>__Subscriber B__</strong> is subscribed to <code class="language-plaintext highlighter-rouge">logs.error</code>.</li>
</ul>

<p>Then:</p>

<ul>
  <li><strong>__Publisher X__</strong> message will end up in <strong><em>Queue</em></strong> <code class="language-plaintext highlighter-rouge">logs.info</code>.</li>
  <li><strong>__Publisher Y__</strong> message will end up in <strong><em>Queue</em></strong> <code class="language-plaintext highlighter-rouge">logs.error</code>.</li>
  <li><strong>__Subscriber A__</strong> will receive <strong>__Publisher X__</strong> and <strong>__Publisher Y__</strong>’s messages.</li>
  <li><strong>__Subscriber B__</strong> will receive <strong>__Publisher Y__</strong>’s message.</li>
</ul>

<p><img src="https://media.giphy.com/media/3o6gDSdED1B5wjC2Gc/giphy.gif" alt="Information Overload" /></p>

<h3 id="handling-subscriptions-in-yggdrasil">Handling Subscriptions in Yggdrasil</h3>

<p>Handling RabbitMQ’s complexity might be intimidating. Fortunately, <a href="https://github.com/gmtprime/yggdrasil_rabbitmq">Yggdrasil for RabbitMQ</a> generalizes the complexity in order to have a simpler API.</p>

<p>The biggest difference with previous adapters is the channel name. Instead of being a string, it’s a tuple with the exchange name and the routing key e.g:</p>

<p>A subscriber would connect to the exchange <code class="language-plaintext highlighter-rouge">amq.topic</code> using the routing key <code class="language-plaintext highlighter-rouge">logs.*</code> as follows:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="n">subscriber</span><span class="p">)</span><span class="o">&gt;</span> <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">subscribe</span><span class="p">(</span><span class="ss">name:</span> <span class="p">{</span><span class="s2">"amq.topic"</span><span class="p">,</span> <span class="s2">"logs.*"</span><span class="p">},</span> <span class="ss">adapter:</span> <span class="ss">:rabbitmq</span><span class="p">)</span>
<span class="n">iex</span><span class="p">(</span><span class="n">subscriber</span><span class="p">)</span><span class="o">&gt;</span> <span class="n">flush</span><span class="p">()</span>
<span class="p">{</span><span class="ss">:Y_CONNECTED</span><span class="p">,</span> <span class="p">%</span><span class="no">Yggdrasil</span><span class="o">.</span><span class="no">Channel</span><span class="p">{</span><span class="o">...</span><span class="p">}}</span>
</code></pre></div></div>

<blockquote>
  <p><strong>Note</strong>: The exchange must exist and its type should be <code class="language-plaintext highlighter-rouge">topic</code>. The exchange <code class="language-plaintext highlighter-rouge">amq.topic</code> is created by default in RabbitMQ.</p>
</blockquote>

<p>Then a publisher could send a message to the exchange <code class="language-plaintext highlighter-rouge">amq.topic</code> using <code class="language-plaintext highlighter-rouge">logs.info</code> as routing key:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="n">publisher</span><span class="p">)</span><span class="o">&gt;</span> <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">publish</span><span class="p">([</span><span class="ss">name:</span> <span class="p">{</span><span class="s2">"amq.topic"</span><span class="p">,</span> <span class="s2">"logs.info"</span><span class="p">},</span> <span class="ss">adapter:</span> <span class="ss">:rabbitmq</span><span class="p">],</span> <span class="s2">"Some message"</span><span class="p">)</span>
<span class="ss">:ok</span>
</code></pre></div></div>

<p>Finally, the subscriber would receive the message:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="n">subscriber</span><span class="p">)</span><span class="o">&gt;</span> <span class="n">flush</span><span class="p">()</span>
<span class="p">{</span><span class="ss">:Y_EVENT</span><span class="p">,</span> <span class="p">%</span><span class="no">Yggdrasil</span><span class="o">.</span><span class="no">Channel</span><span class="p">{</span><span class="o">...</span><span class="p">},</span> <span class="s2">"Some message"</span><span class="p">}</span>
</code></pre></div></div>

<p>Additionally, the subscriber can be written using the <code class="language-plaintext highlighter-rouge">Yggdrasil</code> behaviour:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">Logs</span><span class="o">.</span><span class="no">Subscriber</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">Yggdrasil</span>

  <span class="k">def</span> <span class="n">start_link</span><span class="p">(</span><span class="n">options</span> <span class="p">\\</span> <span class="p">[])</span> <span class="k">do</span>
    <span class="n">channel</span> <span class="o">=</span> <span class="p">[</span>
      <span class="ss">name:</span> <span class="p">{</span><span class="s2">"amq.topic"</span><span class="p">,</span> <span class="s2">"logs.*"</span><span class="p">},</span>
      <span class="ss">adapter:</span> <span class="ss">:rabbitmq</span>
    <span class="p">]</span>

    <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">start_link</span><span class="p">(</span><span class="bp">__MODULE__</span><span class="p">,</span> <span class="p">[</span><span class="n">channel</span><span class="p">],</span> <span class="n">options</span><span class="p">)</span>
  <span class="k">end</span>

  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="n">_channel</span><span class="p">,</span> <span class="n">message</span><span class="p">,</span> <span class="n">_state</span><span class="p">)</span> <span class="k">do</span>
    <span class="o">...</span> <span class="n">handle</span> <span class="n">event</span> <span class="o">...</span>
    <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="no">nil</span><span class="p">}</span>
  <span class="k">end</span>
<span class="k">end</span>
</code></pre></div></div>

<h3 id="lost-messages">Lost Messages</h3>

<p>Yggdrasil will acknowledge the messages as soon as they arrive to the adapter, then it will broadcast them to all the subscribers. If the adapter is alive while the subscribers are restarting/failing, some messages might be lost.</p>

<p>Though it’s possible to overcome this problem with exclusive queues, this feature is not implemented yet.</p>

<p><img src="https://media.giphy.com/media/5YuhLwDgrgtRVwI7OY/giphy.gif" alt="Penguin's queueing" /></p>

<div class="chapter">
    <h2 id="3">
      Chapter IV: Yggdrasil as Distributed PubSub
    </h2>
  </div>

<p>Yggdrasil’s default adapter supports multi-node subscriptions out-of-the-box thanks to <a href="https://github.com/phoenixframework/phoenix_pubsub">Phoenix PubSub</a>. This distributed capabilities can be extended to any adapter compatible with Yggdrasil v5.0 without writing a single line of extra code.</p>

<p><img src="https://media.giphy.com/media/Qtpdtlk9rpb8PKK0ih/giphy.gif" alt="Spock is interested!" /></p>

<h2 id="before-we-start">Before We Start</h2>

<p>I’ve used <a href="https://https://github.com/alexdesousa/alexdesousa.github.io/tree/blog/examples/matrix">this example project</a>
 for the code in this article. You can skip this section safely as long as you remember the following:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Basic</code> project has only Yggdrasil.</li>
  <li><code class="language-plaintext highlighter-rouge">Rabbit</code> project has Yggdrasil for RabbitMQ.</li>
  <li>A RabbitMQ server is available.</li>
  <li>The host name is called <code class="language-plaintext highlighter-rouge">matrix</code>. Your machine’s will be different.</li>
</ul>

<p>If you want to follow along with the examples in this article, you can download the example project using the following command:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git clone <span class="nt">--depth</span> 2 <span class="nt">-b</span> blog https://https://github.com/alexdesousa/alexdesousa.github.io examples <span class="o">&amp;&amp;</span> <span class="nb">cd </span>examples <span class="o">&amp;&amp;</span> git filter-branch <span class="nt">--prune-empty</span> <span class="nt">--subdirectory-filter</span> examples/matrix HEAD
</code></pre></div></div>

<p>In the folder you’ll find:</p>

<ul>
  <li>
    <p><a href="https://https://github.com/alexdesousa/alexdesousa.github.io/tree/blog/examples/matrix/basic">Basic project</a></p>

    <p>that has a basic version of <a href="https://github.com/gmtprime/yggdrasil">Yggdrasil</a>.</p>
  </li>
  <li>
    <p><a href="https://https://github.com/alexdesousa/alexdesousa.github.io/tree/blog/examples/matrix/rabbit">Rabbit project</a></p>

    <p>that has <a href="https://github.com/gmtprime/yggdrasil_rabbitmq">Yggdrasil for RabbitMQ</a>.</p>
  </li>
  <li>
    <p>A docker compose with a RabbitMQ server:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nv">$ </span><span class="nb">cd </span>rabbit <span class="o">&amp;&amp;</span> docker-compose up
</code></pre></div>    </div>
  </li>
</ul>

<p><img src="https://media.giphy.com/media/bKnEnd65zqxfq/giphy.gif" alt="Make it so" /></p>

<h2 id="basic-message-distribution">Basic Message Distribution</h2>

<p>Yggdrasil’s default adapter piggybacks on Phoenix PubSub for the message delivery, inheriting its distributed capabilities e.g. let’s say we have the following:</p>

<ul>
  <li>
    <p>The node <code class="language-plaintext highlighter-rouge">:neo@matrix</code> using <code class="language-plaintext highlighter-rouge">Basic</code> project:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nv">$ </span>iex <span class="nt">--sname</span> neo <span class="nt">-S</span> mix
</code></pre></div>    </div>
  </li>
  <li>
    <p>The node <code class="language-plaintext highlighter-rouge">:smith@matrix</code> also using <code class="language-plaintext highlighter-rouge">Basic</code> project:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nv">$ </span>iex <span class="nt">--sname</span> smith <span class="nt">-S</span> mix
</code></pre></div>    </div>
  </li>
  <li>
    <p>Both nodes are interconnected:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">iex</span><span class="p">(</span><span class="n">smith</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">1</span><span class="o">&gt;</span> <span class="no">Node</span><span class="o">.</span><span class="n">connect</span><span class="p">(</span><span class="ss">:neo</span><span class="nv">@matrix</span><span class="p">)</span>
 <span class="no">true</span>
</code></pre></div>    </div>
  </li>
</ul>

<p>Then <code class="language-plaintext highlighter-rouge">:smith@matrix</code> can subscribe to any channel where <code class="language-plaintext highlighter-rouge">:neo@matrix</code> is publishing messages e.g:</p>

<p>In <code class="language-plaintext highlighter-rouge">:smith@matrix</code>, we subscribe to the channel <code class="language-plaintext highlighter-rouge">"private"</code>:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="n">smith</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">2</span><span class="o">&gt;</span> <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">subscribe</span><span class="p">(</span><span class="ss">name:</span> <span class="s2">"private"</span><span class="p">)</span>
<span class="ss">:ok</span>
<span class="n">iex</span><span class="p">(</span><span class="n">smith</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">3</span><span class="o">&gt;</span> <span class="n">flush</span><span class="p">()</span>
<span class="p">{</span><span class="ss">:Y_CONNECTED</span><span class="p">,</span> <span class="p">%</span><span class="no">Yggdrasil</span><span class="o">.</span><span class="no">Channel</span><span class="p">{</span><span class="o">...</span><span class="p">}}</span>
<span class="ss">:ok</span>
</code></pre></div></div>

<p>In <code class="language-plaintext highlighter-rouge">:neo@matrix</code>, we publish a message in the channel <code class="language-plaintext highlighter-rouge">"private"</code>:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="n">neo</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">1</span><span class="o">&gt;</span> <span class="n">channel</span> <span class="o">=</span> <span class="p">[</span><span class="ss">name:</span> <span class="s2">"private"</span><span class="p">]</span>
<span class="n">iex</span><span class="p">(</span><span class="n">neo</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">2</span><span class="o">&gt;</span> <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">publish</span><span class="p">(</span><span class="n">channel</span><span class="p">,</span> <span class="s2">"What's the Matrix?"</span><span class="p">)</span>
<span class="ss">:ok</span>
</code></pre></div></div>

<p>Finally, we can flush <code class="language-plaintext highlighter-rouge">:smith@matrix</code> mailbox and find our message:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="n">smith</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">4</span><span class="o">&gt;</span> <span class="n">flush</span><span class="p">()</span>
<span class="p">{</span><span class="ss">:Y_EVENT</span><span class="p">,</span> <span class="p">%</span><span class="no">Yggdrasil</span><span class="o">.</span><span class="no">Channel</span><span class="p">{</span><span class="o">...</span><span class="p">},</span> <span class="s2">"What's the Matrix?"</span><span class="p">}</span>
<span class="ss">:ok</span>
</code></pre></div></div>

<p>Distributed pubsub as simple as that.</p>

<p><img src="https://media.giphy.com/media/dWy2WwcB3wvX8QA1Iu/giphy.gif" alt="Easy" /></p>

<h2 id="bridged-message-distribution">Bridged Message Distribution</h2>

<p>The bridge adapter makes a <em>bridge</em> between any Yggdrasil adapter and the default adapter. This allows adapters to inherit the distributed capabilities of the default adapter e.g. let’s say we have the following:</p>

<ul>
  <li>
    <p>The node <code class="language-plaintext highlighter-rouge">:neo@matrix</code> using <code class="language-plaintext highlighter-rouge">Basic</code> project:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nv">$ </span>iex <span class="nt">--sname</span> neo <span class="nt">-S</span> mix
</code></pre></div>    </div>
  </li>
  <li>
    <p>The node <code class="language-plaintext highlighter-rouge">:trinity@matrix</code> using <code class="language-plaintext highlighter-rouge">Rabbit</code> project:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nv">$ </span>iex <span class="nt">--sname</span> trinity <span class="nt">-S</span> mix
</code></pre></div>    </div>
  </li>
  <li>The node <code class="language-plaintext highlighter-rouge">:trinity@matrix</code> has access to a RabbitMQ server.</li>
  <li>
    <p>Both nodes are interconnected:</p>

    <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">iex</span><span class="p">(</span><span class="n">trinity</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">1</span><span class="o">&gt;</span> <span class="no">Node</span><span class="o">.</span><span class="n">connect</span><span class="p">(</span><span class="ss">:neo</span><span class="nv">@matrix</span><span class="p">)</span>
 <span class="no">true</span>
</code></pre></div>    </div>
  </li>
</ul>

<p>So our final infrastructure would look like the following:&lt;p&gt;</p>
<figure class="flex flex-col items-center w-full h-auto mx-auto">
    <img src="/yggdrasil-easy-pub-sub-in-elixir/bridge-adapter-example.png" alt="A node using a bridge adapter to connect to RabbitMQ" />
    <figcaption class="text-center">
      <p>A node using a bridge adapter to connect to RabbitMQ</p>

    </figcaption>
  </figure>
<p>&lt;/p&gt;
Now that our nodes are connected, every adapter is available to them.</p>

<p>Through <code class="language-plaintext highlighter-rouge">:trinity@matrix</code>, the node <code class="language-plaintext highlighter-rouge">:neo@matrix</code> can now subscribe to
a RabbitMQ exchange:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="n">neo</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">1</span><span class="o">&gt;</span> <span class="n">channel</span> <span class="o">=</span> <span class="p">[</span><span class="ss">name:</span> <span class="p">{</span><span class="s2">"amq.topic"</span><span class="p">,</span> <span class="s2">"private"</span><span class="p">},</span> <span class="ss">adapter:</span> <span class="ss">:rabbitmq</span><span class="p">]</span>
<span class="n">iex</span><span class="p">(</span><span class="n">neo</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">2</span><span class="o">&gt;</span> <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">subscribe</span><span class="p">(</span><span class="n">channel</span><span class="p">)</span>
<span class="ss">:ok</span>
<span class="n">iex</span><span class="p">(</span><span class="n">neo</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">3</span><span class="o">&gt;</span> <span class="n">flush</span><span class="p">()</span>
<span class="p">{</span><span class="ss">:Y_CONNECTED</span><span class="p">,</span> <span class="p">%</span><span class="no">Yggdrasil</span><span class="o">.</span><span class="no">Channel</span><span class="p">{</span><span class="o">...</span><span class="p">}}</span>
<span class="ss">:ok</span>
</code></pre></div></div>

<p>Or even publish messages:</p>

<div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">iex</span><span class="p">(</span><span class="n">neo</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">4</span><span class="o">&gt;</span> <span class="no">Yggdrasil</span><span class="o">.</span><span class="n">publish</span><span class="p">(</span><span class="n">channel</span><span class="p">,</span> <span class="s2">"What's the Matrix?"</span><span class="p">)</span>
<span class="ss">:ok</span>
<span class="n">iex</span><span class="p">(</span><span class="n">neo</span><span class="nv">@matrix</span><span class="p">)</span><span class="mi">3</span><span class="o">&gt;</span> <span class="n">flush</span><span class="p">()</span>
<span class="p">{</span><span class="ss">:Y_EVENT</span><span class="p">,</span> <span class="p">%</span><span class="no">Yggdrasil</span><span class="o">.</span><span class="no">Channel</span><span class="p">{</span><span class="o">...</span><span class="p">},</span> <span class="s2">"What's the Matrix?"</span><span class="p">}</span>
<span class="ss">:ok</span>
</code></pre></div></div>

<p>The good thing about this feature is that it works with any adapter that supports Yggdrasil v5.0.</p>

<p><img src="https://media.giphy.com/media/1zRd5ZNo0s6kLPifL1/giphy.gif" alt="The future is now!" /></p>

<div class="chapter">
    <h2 id="4">
      In the end: One API to Rule Them All
    </h2>
  </div>

<p><a href="https://github.com/gmtprime/yggdrasil">Yggdrasil</a> hides the complexity of a pub/sub and let’s you focus in what really matters: <strong>messages</strong>.</p>

<p>Hope you found this article useful. Happy coding!</p>

<p><img src="https://media.giphy.com/media/26xBENWdka2DSvvag/giphy.gif" alt="Heck yeah!" /></p>
<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item>
        <item>
          <title>Syntax highlighting in Jekyll</title>
          <description>Using Redcarpet markdown renderer to add pygments to Jekyll</description>
          <guid>https://thebroken.link/syntax-highlighting-in-jekyll/</guid>
          <dc:creator>Alex de Sousa</dc:creator>
          <pubDate>Sat, 01 Jun 2019 00:00:00 +0000</pubDate>
          <link>https://thebroken.link/syntax-highlighting-in-jekyll/</link>
          <content:encoded>
            <![CDATA[<!--kg-card-begin: markdown--><p>This is mainly a programming blog. Big part of picking a static site generator
involved researching for code syntax highlighters. When I found these
<a href="https://jwarby.github.io/jekyll-pygments-themes/languages/javascript.html">Pygments CSS themes</a>
I knew I wanted Pygments support in my static site generator.</p>

<p>Turns out Jekyll has amazing support for Pygments when used together with
Redcarpet markdown parser.</p>

<h2 id="enter-redcarpet">Enter Redcarpet</h2>

<p>Before using any of those CSS themes, first I needed to add <code class="language-plaintext highlighter-rouge">redcarpet</code> gem
to my <code class="language-plaintext highlighter-rouge">Gemfile</code> e.g:</p>

<div class="language-ruby highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">source</span> <span class="s2">"https://rubygems.org"</span>

<span class="n">gem</span> <span class="s2">"jekyll"</span>
<span class="n">gem</span> <span class="s2">"redcarpet"</span>
</code></pre></div></div>

<p>After running <code class="language-plaintext highlighter-rouge">bundle install</code>, I added the configuration for <code class="language-plaintext highlighter-rouge">redcarpet</code> in
your <code class="language-plaintext highlighter-rouge">_config.yml</code></p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># _config.yml</span>
<span class="na">markdown</span><span class="pi">:</span> <span class="s">redcarpet</span>

<span class="nn">...</span> <span class="s">rest of the config ...</span>
</code></pre></div></div>

<blockquote>
  <p>Redcarpet has many useful extensions and you can find out more about them
<a href="https://github.com/vmg/redcarpet">here</a>.</p>
</blockquote>

<h2 id="adding-syntax-highlighting-theme-to-jekyll">Adding syntax highlighting theme to Jekyll</h2>

<p>For <a href="https://thebroken.link">thebroken.link</a> I downloaded the
<a href="https://raw.githubusercontent.com/jwarby/pygments-css/master/monokai.css">Monokai theme</a>
for syntax highlighting.</p>

<p>I only needed to modify two files:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">assets/css/main.css</code>: my main CSS file included in all my layouts, including
<code class="language-plaintext highlighter-rouge">default</code>.</li>
  <li><code class="language-plaintext highlighter-rouge">_includes/css/pygments/monokai.css</code>: Monokai theme I downloaded.</li>
</ul>

<p>In order to include Monokai theme into into <code class="language-plaintext highlighter-rouge">assets/css/main.css</code>, I used
Liquid  directives:</p>

<div class="language-css highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">---</span>
<span class="nt">---</span>
<span class="p">{</span><span class="err">%-</span> <span class="err">comment</span> <span class="err">-%</span><span class="p">}</span> <span class="nt">File</span><span class="o">:</span> <span class="nt">assets</span><span class="o">/</span><span class="nt">css</span><span class="o">/</span><span class="nt">main</span><span class="nc">.css</span> <span class="p">{</span><span class="err">%-</span> <span class="err">endcomment</span> <span class="err">-%</span><span class="p">}</span>

<span class="p">{</span><span class="err">%-</span> <span class="err">include</span> <span class="err">css/pygments/monokai.css</span> <span class="err">-%</span><span class="p">}</span>

<span class="o">...</span> <span class="nt">more</span> <span class="nt">styles</span> <span class="o">...</span>

</code></pre></div></div>

<p>Having already included the file <code class="language-plaintext highlighter-rouge">assets/css/main.css</code> in my <code class="language-plaintext highlighter-rouge">default</code> layout,
I can do the following:</p>

<figure class="highlight"><pre><code class="language-markdown" data-lang="markdown"><span class="nn">---</span>
<span class="s">layout:default</span>
<span class="nn">---</span>

<span class="gh"># Lorem Ipsum</span>

<span class="p">```</span><span class="nl">elixir
</span><span class="no">IO</span><span class="o">.</span><span class="n">puts</span> <span class="s2">"Lorem Ipsum"</span>
<span class="p">```</span></code></pre></figure>

<h2 id="conclusion">Conclusion</h2>

<p>Now you can have beautiful code in your programming posts!</p>

<p><img src="https://media.giphy.com/media/i5wNCqyMzY2Oc/giphy.gif" alt="B-e-a-utiful!" /></p>

<!--kg-card-end: markdown-->]]>
          </content:encoded>
        </item></channel></rss>
