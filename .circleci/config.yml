version: 2.1
commands:
  install:
    description: "Installs Jekyll"
    steps:
      - run: gem install bundler
      - run: bundle install
  commit:
    description: "Commits site"
    steps:
      - run: git clone https://github.com/alexdesousa/alexdesousa.github.io.git .
      - run: git config --global user.email "$EMAIL"
      - run: git config --global user.name "$NAME"
      - run: cp -a /tmp/site/. .
      - run: git add .
      - run: git commit -m "autogenerated commit"

jobs:
  build:
    docker:
      - image: circleci/ruby:2.6
    working_directory: ~/repo
    steps:
      - checkout
      - install
      - run: bundle exec jekyll build
      - run: mkdir -p /tmp/site
      - run: cp -a _site/. /tmp/site
      - persist_to_workspace:
          root: /tmp/site
          paths:
            - .
  deploy:
    docker:
      - image: circleci/ruby:2.6
    working_directory: /tmp/repo
    steps:
      - attach_workspace:
          at: /tmp/site
      - add_ssh_keys:
          fingerprints:
            - "$FINGERPRINT"
      - commit
      - run: git push origin master

workflows:
  version: 2
  blog:
    jobs:
      - build:
          filters:
            branches:
              only: gh-pages
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: gh-pages
