language: ruby
rvm:
  - 2.6
sudo: false
branches:
  only:
    - gh-pages
before_script:
  - gem update --system
  - gem install bundler
  - bundle install
  - bundle exec jekyll build
script:
  - git status
